node_modules/
.env

# Next.js
/.next/
next-env.d.ts

# TypeScript incremental build cache
*.tsbuildinfo

# Local dev data (D-09): SQLite DB + WAL/SHM sidecars + uploads live under /data
/data/
*.db
*.db-wal
*.db-shm

# esbuild bundle artifacts (08-01): build output emitted at the repo root, not
# source. Produced by `npm run build:worker` / `build:migrate` and by the
# Docker build stage — never committed.
/worker.js
/migrate.js
