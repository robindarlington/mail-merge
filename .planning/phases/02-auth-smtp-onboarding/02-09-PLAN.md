---
phase: 02-auth-smtp-onboarding
plan: 09
type: execute
wave: 2
depends_on: ["02-08"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [SMTP-04]

must_haves:
  truths:
    - "On a live SMTP server, a signed-in user can open Settings → SMTP, change ONLY a connection field (host or port) with the password field left blank, click 'Verify & continue', and the config re-verifies against the stored password and saves — no 'Password is required' error appears under the field that says it can be left blank"
  artifacts: []
  key_links: []
---

## Phase Goal

**As a** signed-in user, **I want to** onboard and persist my own SMTP server proven functional by a live connection check, **so that** my credentials are stored encrypted at rest, reused across sessions, and never exposed to the client or logs.

<objective>
Confirm the CR-01 fix end-to-end against a real SMTP server via a live wizard walkthrough — the one confirmation 02-VERIFICATION.md's `human_verification` block flagged as requiring a human. The automated tests in 02-08 prove the merge seam; this checkpoint proves the full client → Server Action → verify → save path works on a live server.

Purpose: Verify the "reused across sessions" edit path (SMTP-04 / D-07 / D-08) actually works for a returning user before marking the gap closed.
Output: Human confirmation (or a defect report) for the blank-password edit flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-smtp-onboarding/02-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live blank-password edit walkthrough</name>
  <action>Pause for the user to verify the CR-01 fix live on a real SMTP server: edit only a connection field (host/port) with the password left blank and confirm the wizard re-verifies against the stored password and saves without a "Password is required" error. This is a human-verification checkpoint — no code changes; the executor automates nothing further and waits for the resume-signal below.</action>
  <what-built>
    Plan 02-08 added an edit-mode schema variant (smtpEditFormSchema), a server-side stored-password merge in applyVerifiedConfig (decrypts the caller's saved password when the submitted password is blank), and switched the wizard resolver to the edit schema in edit mode. Together these let a returning user change a connection field without re-typing their password — honoring the "Leave blank to keep your current password" copy the UI already shows.
  </what-built>
  <how-to-verify>
    1. Sign in on staging (https://mailmerge.robindarlington.com) — or local dev — as a user who ALREADY has a saved, verified SMTP config (run through onboarding first if needed, using a real SMTP server you control).
    2. Navigate to Settings → SMTP. Confirm the form prefills host/port/TLS mode/username/from-fields, and the Password field is BLANK with "Leave blank to keep your current password" help text.
    3. Change ONLY the Port (or Host) to another valid value for the same server. Do NOT type anything in the Password field.
    4. Click "Verify & continue".
    5. Expected: the connection re-verifies (spinner, up to ~15s) against your STORED password and advances to the test-send step / saves successfully. You should NOT see a "Password is required" error under the password field.
    6. Negative sanity check: reload Settings → SMTP, change a connection field again, but this time type a WRONG password, and confirm the verify fails with an auth-anchored error (proving a typed password still replaces the stored one).
  </how-to-verify>
  <verify>
    <human-check>Blank-password connection-field edit re-verifies and saves with no "Password is required" error; a wrong typed password still fails verify.</human-check>
  </verify>
  <done>Human confirms the live edit flow saves a connection change with a blank password and rejects a wrong typed password.</done>
  <resume-signal>Type "approved" if the blank-password edit re-verifies and saves without a "Password is required" error; otherwise describe exactly what appeared (screenshot of any error under the password field is ideal).</resume-signal>
</task>

</tasks>

<verification>
- Human confirms the live blank-password connection-field edit saves without a "Password is required" error
- Human confirms a wrong typed password still fails verify (typed value replaces stored)
</verification>

<success_criteria>
- The CR-01 / D-07 / D-08 edit flow is confirmed working against a live SMTP server, closing the last Phase 2 verification gap
</success_criteria>

<output>
Create `.planning/phases/02-auth-smtp-onboarding/02-09-SUMMARY.md` when done
</output>
