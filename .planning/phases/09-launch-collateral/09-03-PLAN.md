---
phase: 09-launch-collateral
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - scripts/smoke-public-routes.mjs
  - docs/screenshots/landing.png
  - README.md
  - docs/writeup.md
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "A public README exists at the repo root with at least one screenshot and run/deploy instructions, linking the public repo"
    - "A route-probe smoke asserts the four public routes are reachable signed-out and authed routes are still protected"
    - "A how-it-was-built write-up draft is committed at docs/writeup.md"
    - "The README carries Robin Darlington attribution + the live hire-me link (BRAND-01 in the repo artifact)"
  artifacts:
    - path: "README.md"
      provides: "Repo-root README: what/screenshot/niches/features/quickstart/self-host+CLI pointers/MIT/attribution"
      contains: "github.com/robindarlington/mail-merge"
    - path: "docs/writeup.md"
      provides: "How-it-was-built narrative draft for robindarlington.com/thoughts/"
      min_lines: 40
    - path: "docs/screenshots/landing.png"
      provides: "At least one screenshot referenced by the README (relative path)"
    - path: "scripts/smoke-public-routes.mjs"
      provides: "Dependency-free route-probe: 200 for public routes, redirect/401 for authed routes"
      contains: "self-host"
  key_links:
    - from: "README.md"
      to: "docs/screenshots/landing.png"
      via: "relative-path Markdown image so it renders on GitHub"
      pattern: "docs/screenshots/"
    - from: "README.md"
      to: "https://robindarlington.com/contact/"
      via: "attribution / hire-me link (BRAND-01)"
      pattern: "robindarlington.com/contact"
    - from: "scripts/smoke-public-routes.mjs"
      to: "app/(marketing)/*"
      via: "HTTP probe of /, /docs, /self-host, /agents against next start"
      pattern: "agents"
---

<objective>
Produce the repo-facing collateral now that all four public routes exist: capture at least one public-page screenshot, write the repo-root `README.md` (with the screenshot, the two niches, quickstart, self-host + CLI/MCP pointers, MIT, and attribution), draft the "how it was built" write-up at `docs/writeup.md`, and add a dependency-free route-probe smoke script that is the phase's automated end-to-end gate (public routes reachable signed-out, authed routes still protected — proving the Plan 01 allowlist edit is safe).

Purpose: Satisfy success criteria #1 (README + screenshot + repo link) and #3 (write-up draft), and give the phase a real route-level automated check without adding any test framework (locked "no new dependencies").

Output: `scripts/smoke-public-routes.mjs`, `docs/screenshots/landing.png` (+ any additional public captures), `README.md`, `docs/writeup.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-launch-collateral/09-CONTEXT.md
@.planning/phases/09-launch-collateral/09-RESEARCH.md
@.planning/phases/09-launch-collateral/09-PATTERNS.md
@packages/cli/README.md
@.env.example

<interfaces>
<!-- All four public routes exist after Plans 01-02. -->
Screenshot tooling (verified available): headless Chrome at
  "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
  invoked with --headless --disable-gpu --screenshot=<out.png> --window-size=1280,900 --hide-scrollbars <url>
Dev server: `npm run dev` serves next dev on http://localhost:3000 (public routes need no session).
Route-probe: `npm run build && npm run start` serves next start on http://localhost:3000; probe with Node's global fetch (no dependency).
Repo: https://github.com/robindarlington/mail-merge (git remote origin).
CLI README author idiom (packages/cli/README.md:184-192) — mirror structure but point hire-me at https://robindarlington.com/contact/ (not the GitHub placeholder the CLI README uses).
README skeleton: 09-RESEARCH.md lines 308-319.
LICENSE: MIT (LICENSE file already present at repo root).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture public-page screenshots + route-probe smoke script</name>
  <files>docs/screenshots/landing.png, scripts/smoke-public-routes.mjs</files>
  <read_first>
    - proxy.ts (post-Plan-01 allowlist — the routes the probe asserts as public + the authed routes it asserts stay protected)
    - app/(marketing)/page.tsx (landing being captured)
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (Validation Architecture + Security "Verification step": authed routes must still redirect; Open Q1 screenshot fallback)
    - .planning/phases/09-launch-collateral/09-VALIDATION.md (sampling posture)
  </read_first>
  <action>
    Screenshots: build the app, start it (`npm run build && npm run start`, or `npm run dev`) in the background on port 3000, wait for it to accept connections, then use headless Chrome (path in interfaces) to capture the four PUBLIC pages signed-out — `/`, `/docs`, `/self-host`, `/agents` — into `docs/screenshots/` as `landing.png`, `docs.png`, `self-host.png`, `agents.png` at `--window-size=1280,900`. The mandatory minimum is `docs/screenshots/landing.png` (README requires ≥1 screenshot — SC#1). If headless Chrome capture fails for any reason, fall back to writing a small committed placeholder `docs/screenshots/landing.png` and record a queued item for Rob (documented in Plan 04's queued human-verify) to replace it — but still produce the file so the README image resolves. Do NOT capture authed screens here (they need a Clerk session) and do NOT add Playwright/Puppeteer (out of scope). Stop the background server afterward.
    Smoke script: create `scripts/smoke-public-routes.mjs` as a plain Node ESM script with NO imports beyond Node built-ins (uses global `fetch`). It reads a base URL from `process.env.SMOKE_BASE_URL` defaulting to `http://localhost:3000`, then, with redirects disabled (`redirect: "manual"`), asserts: `/`, `/docs`, `/self-host`, `/agents` each return HTTP 200; and `/dashboard`, `/settings/smtp`, and `/api/health` (or any `/api/*` path) each return a redirect (3xx to the sign-in URL) or 401/404-not-200 — i.e. NOT a 200 render (proving the allowlist did not over-expose authed routes, threat T-09-01). Print a line per route and `process.exit(1)` on any failed assertion, `SMOKE_PASS` + exit 0 on success. The script assumes a server is already running at the base URL (the caller starts `next start`).
  </action>
  <verify>
    <automated>test -f docs/screenshots/landing.png && node --check scripts/smoke-public-routes.mjs && grep -q "self-host" scripts/smoke-public-routes.mjs && grep -q "dashboard" scripts/smoke-public-routes.mjs && grep -q "redirect" scripts/smoke-public-routes.mjs && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/screenshots/landing.png` exists and is a non-empty PNG (`test -s docs/screenshots/landing.png`).
    - `node --check scripts/smoke-public-routes.mjs` passes (valid ESM, no syntax error) and the script imports nothing outside Node built-ins (`grep -Eq "require\(|from \"[a-z]" scripts/smoke-public-routes.mjs` finds no third-party import).
    - The script references all four public routes and the protected routes (`grep` for `self-host`, `agents`, `dashboard`).
    - When run against a live `next start` server, the script prints `SMOKE_PASS` and exits 0 (public routes 200, authed routes not-200). Run: start the built app, then `node scripts/smoke-public-routes.mjs`.
  </acceptance_criteria>
  <done>At least one public screenshot is captured (or a placeholder committed + queued for Rob), and a dependency-free route-probe smoke asserts public reachability + authed protection.</done>
</task>

<task type="auto">
  <name>Task 2: Repo-root README.md + docs/writeup.md draft</name>
  <files>README.md, docs/writeup.md</files>
  <read_first>
    - packages/cli/README.md (structure/tone + Author/License idiom to mirror; note the hire-me link must point at the real contact URL, not the CLI README's GitHub placeholder)
    - .env.example (quickstart env pointer)
    - CLAUDE.md (project summary, core value, constraints for the write-up narrative)
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (README skeleton lines 308-319; write-up brief)
    - .planning/phases/09-launch-collateral/09-CONTEXT.md (write-up brief + README section list)
    - .planning/phases/09-launch-collateral/09-PATTERNS.md (sections "README.md" and "docs/writeup.md")
  </read_first>
  <action>
    Create `README.md` at the repo root with these sections: a title + the repo link `https://github.com/robindarlington/mail-merge`; a one-line what-it-is (CSV-driven, plain-text mail merge over your own SMTP); at least one screenshot referenced by RELATIVE path `![Landing](docs/screenshots/landing.png)` with descriptive alt (relative path so it renders on GitHub — NOT `/public`, Pitfall 6); a "who it's for" section naming the two niches (credential delivery · per-row documents like payslips/certificates/invoices); a feature list; a Quickstart (local dev) section (Node >= 24, `npm install`, copy `.env.example` to `.env` and fill values, `npm run dev`); a Self-host section pointing at the `/self-host` page and `docs/`; a CLI & MCP section pointing at `packages/cli/README.md` and the `/agents` page; a License section (MIT); and an Author section reading "Built by Robin Darlington" with the live hire-me link `https://robindarlington.com/contact/`. Sentence case, no fabricated metrics; claims accurate to the shipped app.
    Create `docs/writeup.md` — a "how it was built" draft (publishable with light editing; Rob publishes manually at robindarlington.com/thoughts/). Narrative brief: the story of generalizing the one-off credential-delivery CLI (`send-credentials.ts`) into a self-serve multi-tenant web product; the architecture choices (Next.js full-stack + Clerk auth + one WAL'd SQLite file shared by web + a long-lived Node worker on Coolify/VPS; BYO-SMTP with AES-256-GCM credentials at rest; the per-recipient `send_record` state machine as the idempotency/progress linchpin); and the spec-driven, AI-assisted build process. Honest, plain-spoken tone; no fabricated numbers or guarantees. Aim for a substantial draft (~40+ lines).
  </action>
  <verify>
    <automated>test -f README.md && test -f docs/writeup.md && grep -q "github.com/robindarlington/mail-merge" README.md && grep -q "docs/screenshots/landing.png" README.md && grep -q "robindarlington.com/contact" README.md && grep -qi "MIT" README.md && [ "$(grep -vc '^\s*$' docs/writeup.md)" -ge 40 ] && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the repo root and contains the repo URL `github.com/robindarlington/mail-merge`.
    - README references the screenshot via the relative path `docs/screenshots/landing.png`.
    - README names both niches (grep for `credential` and `payslip`-or-`per-row`), includes a Quickstart (`npm run dev`), self-host + CLI/MCP pointers, `MIT`, and the live hire-me link `robindarlington.com/contact`.
    - `docs/writeup.md` exists with ≥40 non-blank lines and mentions the core architecture terms (grep for `SQLite`, `SMTP`, `Coolify`/`worker`).
    - No fabricated metrics or testimonials appear in either file.
  </acceptance_criteria>
  <done>README (with ≥1 relative-path screenshot, repo link, quickstart, pointers, MIT, live attribution) and the write-up draft are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo README / write-up → public GitHub reader | World-readable repo artifacts; leaked secrets or false claims are public |
| smoke probe → running app | Probe validates the privilege boundary set by the Plan 01 allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | scripts/smoke-public-routes.mjs | mitigate | The probe asserts `/dashboard`, `/settings/smtp`, `/api/*` do NOT return 200 signed-out — a positive regression test that the allowlist stayed narrow |
| T-09-02 | Information Disclosure | README.md / docs/writeup.md | mitigate | Self-host pointer links the `/self-host` page rather than re-printing secrets; no real key/`.env` in either file |
| T-09-03 | Repudiation / trust | README.md / docs/writeup.md | mitigate | Only accurate claims (BYO-SMTP, AES-256-GCM at rest, verify-before-send, per-recipient records); no fabricated metrics/testimonials |
| T-09-05 | Tampering (downstream) | README CLI/MCP pointer | mitigate | README points at `packages/cli/README.md`/`/agents` for snippets rather than duplicating (drifted duplicates avoided) |
| T-09-SC | Tampering | npm installs | accept | Zero packages installed; screenshots use system Chrome, smoke uses Node built-in fetch — no supply-chain surface |
</threat_model>

<verification>
- `npm test && npm run build` green (wave-merge gate).
- File-existence + content assertions verified by grep per task.
- Run `scripts/smoke-public-routes.mjs` against a live `next start`: expect `SMOKE_PASS` (public 200, authed not-200) — the phase's automated route-level gate.
- Re-diff `/agents` snippets vs `packages/cli/README.md` if the CLI README changed since Plan 02.
</verification>

<success_criteria>
- README.md at repo root with ≥1 screenshot (relative path), repo link, quickstart, self-host + CLI pointers, MIT, live attribution (SC#1, BRAND-01).
- docs/writeup.md draft committed (SC#3).
- Route-probe smoke passes against the built app.
- `npm test && npm run build` pass.
</success_criteria>

<output>
Create `.planning/phases/09-launch-collateral/09-03-SUMMARY.md` when done.
</output>
