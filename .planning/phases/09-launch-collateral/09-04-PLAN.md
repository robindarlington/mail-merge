---
phase: 09-launch-collateral
plan: 04
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03]
files_modified: []
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "The Phase 9 slice is pushed to GitHub, triggering the Coolify compose redeploy of the standing staging environment"
    - "A queued human-verify item records the staging checks that only Rob can perform (live URL, signed-in redirect, hire-me link, authed screenshots)"
    - "No build-pack change is made — the deploy stays on the Docker Compose build pack"
  artifacts:
    - path: ".planning/phases/09-launch-collateral/09-04-SUMMARY.md"
      provides: "Deploy record + the queued human-verify checklist for Rob"
      contains: "staging"
  key_links:
    - from: "git push origin master"
      to: "Coolify compose build pack"
      via: "GitHub push triggers redeploy of the same Next.js image (marketing pages included)"
      pattern: "master"
---

<objective>
Ship the Phase 9 slice to the standing staging environment and queue the human-only verification for Rob. All code is additive marketing pages inside the existing Next.js image — no Docker/compose change is needed. This plan verifies the full local gate is green, then pushes to GitHub (Coolify redeploys from the Docker Compose build pack), and records the phase-end staging checks as a queued, non-blocking human-verify item (autonomous overnight run — do not block on Rob).

Purpose: Satisfy success criterion #5 (slice deployed to the standing staging URL and works there) up to the human-only live-URL confirmation, which is queued rather than blocking.

Output: A push to `origin master` and `09-04-SUMMARY.md` carrying the queued human-verify checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-launch-collateral/09-RESEARCH.md
@docker-compose.yml
@Dockerfile

<interfaces>
<!-- Deploy contract from prior phases + repo memory (2026-07-18). -->
Coolify staging is a Docker Compose build-pack app; it redeploys on git push to GitHub (git remote origin = git@github.com:robindarlington/mail-merge.git).
A plain Dockerfile build pack would silently drop the worker + compose env — DO NOT change the build pack (RESEARCH Pitfall 7 / repo memory).
The marketing pages are inside the same Next.js image — no docker-compose.yml, Dockerfile, or env change is required by this phase.
Route-probe: scripts/smoke-public-routes.mjs (from Plan 03) can be run against the staging URL via SMOKE_BASE_URL=<staging-url> node scripts/smoke-public-routes.mjs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify local gate green, then push to trigger the Coolify redeploy</name>
  <files></files>
  <read_first>
    - docker-compose.yml (confirm no change is needed — marketing pages ride the existing web image)
    - Dockerfile (confirm the standalone build is unaffected by additive pages)
    - scripts/smoke-public-routes.mjs (the route-probe used for the local pre-push gate and, later, the staging probe)
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (Deploy / Pitfall 7 — compose build pack, push-to-deploy)
  </read_first>
  <action>
    Confirm the full local gate is green: run `npm test` and `npm run build`, then start the built app (`npm run start`) and run `node scripts/smoke-public-routes.mjs` (expect `SMOKE_PASS` — public routes 200, authed routes not-200), then stop the server. Confirm NO changes were made to docker-compose.yml, Dockerfile, or any env contract (this phase is additive app pages only; the build pack stays Docker Compose). Ensure all Phase 9 code from Plans 01-03 is committed on `master`, then push with `git push origin master` to trigger the Coolify compose redeploy of the standing staging environment. Do not change the Coolify build pack, do not add env vars. If the push or a pre-push commit is blocked, record the blocker in the summary and still queue the human-verify item — do not halt the overnight run.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && git diff --quiet HEAD -- docker-compose.yml Dockerfile && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` and `npm run build` both pass locally.
    - `scripts/smoke-public-routes.mjs` prints `SMOKE_PASS` against the local built app (public routes reachable, authed routes protected).
    - `git diff --quiet HEAD -- docker-compose.yml Dockerfile` succeeds (no packaging/build-pack change).
    - `git push origin master` completes (or, if blocked, the blocker is recorded in the summary and the human-verify item is still queued).
  </acceptance_criteria>
  <done>Local gate is green, no packaging change, and the slice is pushed to GitHub to trigger the Coolify redeploy.</done>
</task>

<task type="auto">
  <name>Task 2: Record the queued human-verify checklist for Rob (non-blocking)</name>
  <files>.planning/phases/09-launch-collateral/09-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-launch-collateral/09-VALIDATION.md (Manual-Only Verifications table — the exact staging checks)
    - .planning/phases/09-launch-collateral/09-UI-SPEC.md (Open item: authed-screen screenshots queued for Rob)
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (Open Q1 — authed captures need a Clerk session)
  </read_first>
  <action>
    In the plan summary, record a clearly labelled "Queued for Rob (human-only, non-blocking)" section capturing the staging + authed checks that could not be automated in an overnight run: (1) open the staging URL's `/`, `/docs`, `/self-host`, `/agents` in a private window and confirm no auth redirect (optionally run `SMOKE_BASE_URL=<staging-url> node scripts/smoke-public-routes.mjs`); (2) sign in, visit `/`, and confirm the redirect to `/dashboard` with no landing flash; (3) click the footer "Hire me for custom tools" link and confirm it loads `https://robindarlington.com/contact/`; (4) capture authed-screen screenshots (dashboard, compose, campaign progress) into `docs/screenshots/` and replace the public-only/placeholder README image set — the executor could not obtain a Clerk session headlessly. This is a queued item, NOT a blocking checkpoint: do not pause the run waiting for Rob. Also note whether the automated public-page screenshots in Plan 03 succeeded or fell back to a placeholder.
  </action>
  <verify>
    <human-check>Rob performs the four queued staging/authed checks above at his convenience; the run does not block on them.</human-check>
    <automated>test -f .planning/phases/09-launch-collateral/09-04-SUMMARY.md && grep -qi "queued for rob" .planning/phases/09-launch-collateral/09-04-SUMMARY.md && grep -q "robindarlington.com/contact/" .planning/phases/09-launch-collateral/09-04-SUMMARY.md && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `09-04-SUMMARY.md` exists and contains a "Queued for Rob" section listing the four human-only checks (staging routes signed-out, signed-in `/` redirect, hire-me link, authed screenshots).
    - The summary records whether the Plan 03 public screenshots were captured or placeholder-fallback.
    - The item is explicitly marked non-blocking (the run continues without Rob).
  </acceptance_criteria>
  <done>The phase-end human-only checks are queued for Rob in the summary; the overnight run completes without blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub → Coolify VPS | A push triggers a production-adjacent redeploy of the staging environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Denial of Service / misconfig | Coolify build pack | mitigate | Do NOT change the build pack from Docker Compose (a Dockerfile pack silently drops the worker + env — repo memory / Pitfall 7); assert no docker-compose.yml/Dockerfile diff before push |
| T-09-01 | Elevation of Privilege | staging allowlist | mitigate | Queued human-verify (and optional `SMOKE_BASE_URL` probe) re-confirm authed routes stay protected on the live URL |
| T-09-02 | Information Disclosure | env/secrets on deploy | accept | No env var is added or changed this phase; existing Coolify secrets are untouched |
| T-09-SC | Tampering | npm installs | accept | Zero packages installed; deploy uses the unchanged existing image |
</threat_model>

<verification>
- `npm test` + `npm run build` green pre-push; no packaging diff.
- Local `scripts/smoke-public-routes.mjs` → `SMOKE_PASS`.
- Push to `origin master` triggers the Coolify compose redeploy.
- Queued human-verify checklist recorded; live-URL confirmation performed by Rob (non-blocking).
</verification>

<success_criteria>
- Phase 9 slice pushed to GitHub; Coolify redeploys the standing staging environment (SC#5, up to the human-only live confirmation).
- No build-pack / compose / env change.
- Queued human-verify checklist (staging routes, signed-in redirect, hire-me link, authed screenshots) recorded for Rob — non-blocking.
</success_criteria>

<output>
Create `.planning/phases/09-launch-collateral/09-04-SUMMARY.md` when done (includes the "Queued for Rob" checklist).
</output>
