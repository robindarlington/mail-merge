---
phase: 09-launch-collateral
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - app/(marketing)/docs/page.tsx
  - app/(marketing)/self-host/page.tsx
  - app/(marketing)/agents/page.tsx
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "A signed-out visitor can read step-by-step usage instructions at /docs"
    - "A signed-out visitor can read host-your-own instructions at /self-host with an environment-variable reference"
    - "A signed-out visitor can read CLI + MCP agent instructions at /agents with copy-paste-working snippets"
    - "Every content page renders the attribution footer via the marketing layout (BRAND-01 across the public surface)"
    - "No real secret value appears on /self-host or anywhere in the marketing pages"
  artifacts:
    - path: "app/(marketing)/docs/page.tsx"
      provides: "7-step usage guide + agents pointer"
      contains: "Using Mail Merge"
    - path: "app/(marketing)/self-host/page.tsx"
      provides: "Docker/Coolify self-host guide + env-var table mirroring .env.example"
      contains: "CREDENTIAL_ENC_KEY"
    - path: "app/(marketing)/agents/page.tsx"
      provides: "CLI + MCP instructions with verbatim snippets from packages/cli/README.md"
      contains: "@robindarlington/mail-merge"
  key_links:
    - from: "app/(marketing)/agents/page.tsx"
      to: "packages/cli/README.md"
      via: "verbatim copy of npx command + MCP mcpServers JSON snippet"
      pattern: "mcpServers"
    - from: "app/(marketing)/self-host/page.tsx"
      to: ".env.example"
      via: "env-var names/semantics mirrored (placeholders + generator command only)"
      pattern: "CREDENTIAL_ENC_KEY"
    - from: "app/(marketing)/docs/page.tsx"
      to: "app/(marketing)/agents/page.tsx"
      via: "neutral inline Link href=/agents pointer"
      pattern: "/agents"
---

<objective>
Flesh out the public surface with the three supporting content pages under the marketing shell created in Plan 01: `/docs` (step-by-step usage), `/self-host` (Docker/Coolify + env-var reference mirroring `.env.example`), and `/agents` (CLI + MCP instructions whose code blocks are verbatim from `packages/cli/README.md`). Each page inherits the attribution footer from the marketing layout, extending BRAND-01 across the whole public surface.

Purpose: Complete success criterion #2 (all four public signed-out routes exist and frame the product for the target niches) and give the README (Plan 03) real pages to point at.

Output: Three new RSC pages under `app/(marketing)/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-launch-collateral/09-CONTEXT.md
@.planning/phases/09-launch-collateral/09-RESEARCH.md
@.planning/phases/09-launch-collateral/09-PATTERNS.md
@.planning/phases/09-launch-collateral/09-UI-SPEC.md
@packages/cli/README.md
@.env.example

<interfaces>
<!-- The marketing shell exists after Plan 01; these pages nest inside it. -->
From app/(marketing)/layout.tsx (created in Plan 01): header + <main>{children}</main> + <SiteFooter/>.
shadcn primitives (components/ui/): Button, Card (+ CardHeader/CardTitle/CardContent/CardDescription), Separator, Badge.
Icons: lucide-react (KeyRound, FileText, ShieldCheck, Terminal, Send).
Neutral inline-link idiom (from components/site-footer.tsx): className="underline underline-offset-4 hover:text-foreground".
Code/command block idiom (D-2, no font package): <pre className="bg-muted rounded-md p-4 text-sm font-mono overflow-x-auto">.
Heading idioms: page heading text-[28px] font-semibold leading-[1.2]; section heading text-xl font-semibold.
Container: mx-auto w-full max-w-3xl (prose) with px-6 and section stack flex flex-col gap-8 py-12.

Canonical snippets to reproduce VERBATIM from packages/cli/README.md:
  npx: npx @robindarlington/mail-merge --csv data.csv --template msg.txt
  MCP config JSON: { "mcpServers": { "mail-merge": { "command": "npx", "args": ["-y", "@robindarlington/mail-merge", "mcp"] } } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /docs — step-by-step usage guide</name>
  <files>app/(marketing)/docs/page.tsx</files>
  <read_first>
    - app/(app)/settings/smtp/page.tsx (heading + prose section idiom to mirror)
    - components/site-footer.tsx (neutral inline-link idiom)
    - .planning/phases/09-launch-collateral/09-UI-SPEC.md (Copywriting Contract → Docs table: exact page heading, intro, 7 step headings, CLI/MCP pointer)
    - .planning/phases/09-launch-collateral/09-PATTERNS.md (section "app/(marketing)/docs/page.tsx")
  </read_first>
  <action>
    Create a static RSC page (no `auth()`, no data fetch) in a `mx-auto w-full max-w-3xl flex flex-col gap-8 py-12` container. Page heading `Using Mail Merge` at `text-[28px] font-semibold leading-[1.2]`, then the intro paragraph `A run goes onboard → upload → compose → preview → test → confirm → send. Here is each step.` in `text-base text-muted-foreground`. Render the seven numbered step sections using the EXACT headings from the UI-SPEC Docs table (`1. Connect your SMTP server` through `7. Watch progress and download the record`) as `text-xl font-semibold` headings, each followed by a short `text-base` paragraph describing that step (keep the prose accurate to the shipped app — SMTP verify-before-save, CSV upload with email-column detection, {{merge-field}} autocomplete for subject and body, row-by-row preview with empty-value warnings, whole-batch test-send, confirmation gate, live progress + downloadable CSV record). Add a `<Separator />` then the CLI/MCP pointer paragraph `Prefer the command line or an AI agent? See the agents page.` with `agents page` as a neutral underlined `Link href="/agents"` (D-5, never accent). Sentence case, no exclamation marks.
  </action>
  <verify>
    <automated>set -o pipefail; npm run build 2>&1 | tail -5 && grep -q "Using Mail Merge" "app/(marketing)/docs/page.tsx" && grep -q 'href="/agents"' "app/(marketing)/docs/page.tsx" && grep -c "font-semibold" "app/(marketing)/docs/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `/docs` is reachable as a route in the build output.
    - `grep -q "Using Mail Merge" "app/(marketing)/docs/page.tsx"` succeeds (exact page heading).
    - All seven UI-SPEC step headings appear (grep each `1. Connect your SMTP server` … `7. Watch progress`).
    - A neutral `Link href="/agents"` pointer exists with the `underline underline-offset-4` idiom.
    - No accent-colored links (no `variant="default"`/primary styling on prose links).
  </acceptance_criteria>
  <done>/docs renders the seven-step usage guide + agents pointer inside the marketing shell; build green.</done>
</task>

<task type="auto">
  <name>Task 2: /self-host — Docker/Coolify guide + env-var reference (secret-safe)</name>
  <files>app/(marketing)/self-host/page.tsx</files>
  <read_first>
    - .env.example (authoritative env-var contract — names, semantics, build-vs-runtime split; DATABASE_PATH, UPLOADS_PATH, CREDENTIAL_ENC_KEY, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, NEXT_PUBLIC_CLERK_SIGN_IN_URL, CLERK_SECRET_KEY)
    - app/(app)/dashboard/page.tsx (Card section idiom)
    - .planning/phases/09-launch-collateral/09-UI-SPEC.md (Copywriting Contract → Self-host table + Security note: placeholders + generator command ONLY)
    - .planning/phases/09-launch-collateral/09-PATTERNS.md (section "app/(marketing)/self-host/page.tsx")
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (Security Domain: Information Disclosure — never render a real key)
  </read_first>
  <action>
    Create a static RSC page in the same `mx-auto w-full max-w-3xl flex flex-col gap-8 py-12` container. Page heading `Host your own`; intro paragraph exactly as UI-SPEC Self-host table (`Mail Merge is a single Next.js app plus a background worker, sharing one SQLite file on a Docker volume. You can run it on your own VPS with Docker Compose — for example via Coolify.`). Add an `Environment variables` section (`text-xl font-semibold`) with the muted note `These mirror .env.example, the complete contract. Build-time variables (Clerk publishable keys) are inlined at build; everything else is read at runtime.` Document the variables by NAME and SEMANTIC only, grouped in `Card`s, mirroring `.env.example`: DATABASE_PATH (runtime, `/data/app.db` in prod), UPLOADS_PATH (runtime, `/data/uploads` in prod), CREDENTIAL_ENC_KEY (RUNTIME SECRET — generated with `openssl rand -base64 32`, app fails to start if absent or not 32 bytes), NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (BUILD-TIME, inlined by `next build`), NEXT_PUBLIC_CLERK_SIGN_IN_URL etc. (BUILD-TIME), CLERK_SECRET_KEY (RUNTIME SECRET, never a build arg). Render the `openssl rand -base64 32` command and any variable names inside `<pre className="bg-muted rounded-md p-4 text-sm font-mono overflow-x-auto">` blocks. Include the secret warning `Generate CREDENTIAL_ENC_KEY with openssl rand -base64 32. The app fails to start if it is missing or not 32 bytes. Never commit a real key.` and the deploy note about Git-push compose rebuild. CRITICAL: show ONLY placeholder names and the generator command — NEVER a real key value, NEVER a full `.env` dump (threat T-09-02).
  </action>
  <verify>
    <automated>set -o pipefail; npm run build 2>&1 | tail -5 && grep -q "Host your own" "app/(marketing)/self-host/page.tsx" && grep -q "CREDENTIAL_ENC_KEY" "app/(marketing)/self-host/page.tsx" && grep -q "openssl rand -base64 32" "app/(marketing)/self-host/page.tsx" && ! grep -Eq "sk_(test|live)_[A-Za-z0-9]{8,}" "app/(marketing)/self-host/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `/self-host` is reachable.
    - `grep -q "Host your own"` and `grep -q "CREDENTIAL_ENC_KEY"` and `grep -q "openssl rand -base64 32"` all succeed.
    - The build-vs-runtime split is described (grep for both `BUILD-TIME`-equivalent and `runtime` wording; publishable key noted as build-time, secret key as runtime).
    - No real secret is present: `grep -Eq "sk_(test|live)_[A-Za-z0-9]{8,}"` FAILS, and no `pk_test`/`pk_live` real-looking key or full `.env` block appears (only placeholder names + generator command).
  </acceptance_criteria>
  <done>/self-host documents the env-var contract by name/semantic only (secret-safe) with the build-vs-runtime split; build green.</done>
</task>

<task type="auto">
  <name>Task 3: /agents — CLI + MCP instructions with verbatim snippets</name>
  <files>app/(marketing)/agents/page.tsx</files>
  <read_first>
    - packages/cli/README.md (CONTENT SOURCE — the npx command, the mcpServers JSON, the "zero install via npx; requires Node.js >= 18" line; snippets must be copied verbatim)
    - .planning/phases/09-launch-collateral/09-UI-SPEC.md (Copywriting Contract → Agents table + parity requirement)
    - .planning/phases/09-launch-collateral/09-PATTERNS.md (section "app/(marketing)/agents/page.tsx")
    - .planning/phases/09-launch-collateral/09-RESEARCH.md (Pattern 4 / Pitfall 4 — verbatim parity)
  </read_first>
  <action>
    Create a static RSC page in the same prose container. Page heading `CLI and MCP for agents`; intro `The same merge-and-send engine runs from the command line or as an MCP server an AI agent can drive. Zero install via npx; requires Node.js 18 or newer.` Section headings (`text-xl font-semibold`): `Quick start (CLI)`, `Dry-run, test, send`, `Use it from an MCP client`. Under Quick start, render the canonical dry-run invocation `npx @robindarlington/mail-merge --csv data.csv --template msg.txt` in a `<pre className="bg-muted rounded-md p-4 text-sm font-mono overflow-x-auto">` block, VERBATIM from packages/cli/README.md (including the package name and flag names). Under MCP, render the mcpServers config JSON block VERBATIM — `{ "mcpServers": { "mail-merge": { "command": "npx", "args": ["-y", "@robindarlington/mail-merge", "mcp"] } } }` — preserving the `-y` flag and exact package name. Briefly describe the four MCP tools (validate-csv, preview-merge, test-send, send) and the two-step send confirm gate in prose matching the README. Do NOT paraphrase the code blocks (a drifted snippet would fail for users — Pitfall 4). No syntax-highlighter dependency.
  </action>
  <verify>
    <automated>set -o pipefail; npm run build 2>&1 | tail -5 && grep -q "npx @robindarlington/mail-merge --csv data.csv --template msg.txt" "app/(marketing)/agents/page.tsx" && grep -q '"mcpServers"' "app/(marketing)/agents/page.tsx" && grep -q '"-y"' "app/(marketing)/agents/page.tsx" && grep -q "CLI and MCP for agents" "app/(marketing)/agents/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `/agents` is reachable.
    - The canonical npx invocation string appears verbatim (`grep -q "npx @robindarlington/mail-merge --csv data.csv --template msg.txt"`).
    - The MCP config contains `"mcpServers"`, the exact package `@robindarlington/mail-merge`, the `mcp` arg, and the `-y` flag (all grep-verified).
    - Snippet parity: the npx command and mcpServers JSON match the corresponding blocks in `packages/cli/README.md` (no package-name/flag drift).
    - Page heading `CLI and MCP for agents` present.
  </acceptance_criteria>
  <done>/agents renders CLI + MCP instructions with copy-paste-working snippets verbatim from the CLI README; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public /self-host content → external reader | Env-var documentation is world-readable; a leaked real secret is unrecoverable |
| /agents snippet → user's shell / MCP client | A wrong snippet runs on the reader's machine or agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | app/(marketing)/self-host/page.tsx | mitigate | Document only placeholder NAMES + the `openssl rand -base64 32` generator command; never a real key or full `.env`; acceptance criteria assert `sk_(test\|live)_…` patterns are absent |
| T-09-05 | Tampering (downstream) | app/(marketing)/agents/page.tsx | mitigate | Copy npx + mcpServers snippets VERBATIM from packages/cli/README.md so copy-paste works; parity grep in acceptance criteria; Plan 03 adds a snippet-parity diff |
| T-09-03 | Repudiation / trust | all three pages | mitigate | Claims accurate to the shipped app (verify-before-send, AES-256-GCM at rest, BYO-SMTP); no fabricated metrics |
| T-09-SC | Tampering | npm installs | accept | Zero packages installed this plan (no new runtime deps); no supply-chain surface |
</threat_model>

<verification>
- `npm run build` green after each task (all four public routes now compile).
- Exact-copy and secret-safety assertions verified by grep per task.
- Snippet parity against packages/cli/README.md re-checked in Plan 03's smoke/diff step.
</verification>

<success_criteria>
- `/docs`, `/self-host`, `/agents` all render signed-out inside the marketing shell (footer present on each — BRAND-01 coverage).
- `/self-host` documents env vars by name/generator only — no real secret.
- `/agents` snippets are verbatim from the CLI README (copy-paste-working).
- `npm run build` passes.
</success_criteria>

<output>
Create `.planning/phases/09-launch-collateral/09-02-SUMMARY.md` when done.
</output>
