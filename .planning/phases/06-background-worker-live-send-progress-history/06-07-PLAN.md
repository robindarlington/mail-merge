---
phase: 06-background-worker-live-send-progress-history
plan: 07
type: execute
wave: 3
depends_on: [06-04, 06-05, 06-06]
files_modified:
  - .planning/phases/06-background-worker-live-send-progress-history/06-STAGING-NOTES.md
autonomous: false
requirements: [SEND-01, SEND-05, SEND-06, HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "The worker + web slice is deployed to the standing Coolify staging URL and both containers are healthy"
    - "A real live send over a test SMTP delivers one email per recipient with live progress visible on staging"
    - "Killing the worker mid-batch and restarting it resends no already-sent recipient (zero duplicates)"
    - "History list, drill-down, and CSV download work on staging; a cross-tenant export id returns 404"
  artifacts:
    - path: ".planning/phases/06-background-worker-live-send-progress-history/06-STAGING-NOTES.md"
      provides: "recorded staging deploy + crash-resume smoke + walkthrough evidence"
      min_lines: 20
  key_links:
    - from: "Coolify staging"
      to: "worker container"
      via: "shared /data volume WAL'd SQLite (DB-as-queue)"
      pattern: "worker"
---

<objective>
Deploy the Phase 6 slice to the standing Coolify staging environment and verify it there: a real background live-send with live progress, the crash-resume no-double-send guarantee, and the history/drill-down/CSV-download surfaces — closing success criterion #7 for the phase.

Purpose: The worker's whole reason to exist is a background send that survives process death on the real VPS topology (two containers, one shared WAL'd SQLite volume). Unit tests prove the seams; only staging proves the deployed system. This is the phase's human-verified acceptance gate.
Output: `06-STAGING-NOTES.md` recording deploy + the three verifications, plus any config wired for the worker to run on staging.
</objective>

## Phase Goal (user story)

**As a** signed-in sender, **I want to** have my queued campaign sent in a crash-safe background job with live progress and a durable per-recipient record, **so that** I can reliably deliver my whole batch, watch it happen, resume it after any crash without double-sending, and later review or download exactly what was sent.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-background-worker-live-send-progress-history/06-RESEARCH.md
@.planning/phases/06-background-worker-live-send-progress-history/06-04-SUMMARY.md
@.planning/phases/06-background-worker-live-send-progress-history/06-05-SUMMARY.md
@.planning/phases/06-background-worker-live-send-progress-history/06-06-SUMMARY.md

<notes>
- The Phase-1 docker-compose skeleton already defines a `worker` service (tsx worker/index.ts) sharing the `/data` volume with web; the Phase-2 staging deploy pipeline is established. This plan wires the Phase-6 worker env (SEND_DELAY_MS / WORKER_POLL_MS / WORKER_LEASE_SEC) and redeploys.
- Docker `stop_grace_period` / PID-1 init tuning is Phase 8 — this checkpoint verifies crash-resume via a hard kill (SIGKILL) which is recoverable regardless of grace period (per-row synchronous commit + orphan sweep).
- A real SMTP server is BYO. Use a throwaway test SMTP account (or a captured-inbox test transport) reachable from the VPS; never commit credentials — inject via Coolify secrets.
- Autonomy: this is a queued human checkpoint. Per the operator's standing preference, later phases need not block on it — but it MUST be completed before Phase 6 is marked verified/shipped.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire worker env + redeploy the slice to Coolify staging</name>
  <files>.planning/phases/06-background-worker-live-send-progress-history/06-STAGING-NOTES.md</files>
  <read_first>
    - .planning/phases/06-background-worker-live-send-progress-history/06-04-SUMMARY.md (the env vars the worker reads)
    - .planning/phases/02-*/02-07-* (the established Coolify staging deploy pattern, if present)
    - worker/index.ts (SEND_DELAY_MS / WORKER_POLL_MS / WORKER_LEASE_SEC / WORKER_ID reads)
  </read_first>
  <action>
    Confirm the docker-compose `worker` service runs `tsx worker/index.ts` and shares the `/data` volume + DATABASE_PATH=/data/app.db + CREDENTIAL_ENC_KEY with web. In Coolify, set the Phase-6 worker env: SEND_DELAY_MS (e.g. 1000), WORKER_POLL_MS (e.g. 2000), WORKER_LEASE_SEC (e.g. 300), and a stable WORKER_ID. Push the current branch and trigger a Coolify redeploy of the standing staging environment. After deploy, confirm BOTH containers are healthy and the worker logged its pino "worker ready" line (check Coolify logs). Record the staging URL, the deployed commit SHA, both container states, and the worker readiness log line in 06-STAGING-NOTES.md. Do NOT print or commit any secret value — record only that the secret is set, not its value.
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-background-worker-live-send-progress-history/06-STAGING-NOTES.md" && grep -qi "worker ready" ".planning/phases/06-background-worker-live-send-progress-history/06-STAGING-NOTES.md"</automated>
  </verify>
  <acceptance_criteria>
    - 06-STAGING-NOTES.md records the staging URL, deployed commit SHA, and both container health states.
    - The worker container's "worker ready" pino line is captured in the notes.
    - No secret value appears in the notes or any committed file (grep the notes for obvious secret markers returns nothing).
  </acceptance_criteria>
  <done>The Phase-6 slice is deployed to staging with the worker running and readiness confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A crash-safe background live-send with live progress, history, drill-down, and CSV download, deployed to Coolify staging with a running worker.</what-built>
  <how-to-verify>
    1. Sign in on the staging URL, ensure a verified BYO test SMTP config, a small CSV (e.g. 5-8 rows, include one duplicate address and one address the SMTP will reject if possible), and a saved template.
    2. From /compose, clear the confirmation gate and start a live send. Open /campaigns → the new campaign appears; open it. CONFIRM: the live progress bar advances, sent/failed/remaining counts update every ~2s, and "Currently sending to {email}" tracks the active recipient.
    3. CRASH-RESUME: while the send is running mid-batch, hard-kill the worker container in Coolify (stop/kill), then restart it. CONFIRM after resume: the send completes; the per-recipient results show NO recipient sent twice (check the test inbox / SMTP logs for zero duplicates); any recipient that was mid-flight at the kill shows "Interrupted" (not double-sent); the campaign ends "Completed".
    4. HISTORY + DRILL-DOWN: /campaigns lists the campaign with correct status + "{sent}/{total} sent"; the detail page shows each recipient's status + reason + sent-at, with a rejected send styled destructive and an interrupted one styled muted.
    5. DOWNLOAD: click "Download results" → a `campaign-{id}-results.csv` downloads; open it and CONFIRM the columns are correct and any value starting with = + - @ is prefixed with a single quote (formula-injection-safe).
    6. IDOR: as a second user (or by editing the id in the URL), request GET /campaigns/{other-user-id}/export → CONFIRM a 404 (no data leak).
    Record pass/fail for each step (2-6) in 06-STAGING-NOTES.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public staging URL → app | Real (test) users + a real SMTP over the network on the VPS topology |
| Coolify secrets → containers | CREDENTIAL_ENC_KEY + SMTP creds injected at runtime, never in the image or repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Tampering (double-send) | crash-resume on the deployed worker | mitigate | Human-verified hard-kill-and-restart shows zero duplicates + interrupted rows (SEND-06); recorded in notes |
| T-06-21 | Information Disclosure / Elevation | IDOR export on staging | mitigate | Step 6 confirms a cross-tenant export id returns 404 |
| T-06-22 | Information Disclosure | secret leakage into logs/notes | mitigate | pino logs readiness/result only; notes record that secrets are set, never their values |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (06-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Both staging containers healthy; worker "worker ready" captured.
- Live-send progress, crash-resume zero-duplicate, history/drill-down, CSV download, and IDOR-404 all pass and are recorded in 06-STAGING-NOTES.md.
</verification>

<success_criteria>
Phase success criterion #7 met: the Phase-6 slice runs on the standing Coolify staging URL, a real background send delivers with live progress, resumes after a hard kill with zero double-sends, and the history + downloadable report surfaces work — human-verified and recorded.
</success_criteria>

<output>
Create `.planning/phases/06-background-worker-live-send-progress-history/06-07-SUMMARY.md` when done.
</output>
