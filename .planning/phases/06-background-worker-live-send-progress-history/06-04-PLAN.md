---
phase: 06-background-worker-live-send-progress-history
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - lib/worker/loop.ts
  - lib/worker/loop.test.ts
  - worker/index.ts
autonomous: true
requirements: [SEND-01, SEND-06]
must_haves:
  truths:
    - "A single tick claims one campaign then recovers orphans, materializes, sends, and finalizes it"
    - "A verify/config failure aborts the whole campaign as 'failed' with no rows sent"
    - "The worker process polls on an interval, logs readiness via pino, and stops claiming new work on SIGTERM"
    - "The inter-send throttle, poll interval, and lease come from env vars with safe defaults"
  artifacts:
    - path: "lib/worker/loop.ts"
      provides: "tick() — composes claim→recover→materialize→runCampaign→finalize with injectable deps"
      min_lines: 40
    - path: "worker/index.ts"
      provides: "composition root: pino logger + ref'd poll interval + SIGTERM/SIGINT handler"
      min_lines: 40
  key_links:
    - from: "lib/worker/loop.ts"
      to: "lib/worker claim/recover/materialize/process/finalize seams"
      via: "composed tick"
      pattern: "claimNextCampaign"
    - from: "worker/index.ts"
      to: "lib/worker/loop tick"
      via: "setInterval calling tick; process.on(SIGTERM)"
      pattern: "tick"
---

<objective>
Compose the tested seams into the worker's `tick()` (claim → recover → materialize → runCampaign → finalize) and replace the Phase-1 no-op `worker/index.ts` skeleton with the real composition root: a pino logger, a ref'd poll interval calling `tick()`, env-sourced throttle/poll/lease config, and a SIGTERM/SIGINT handler. This turns the seams into a live background sender (SEND-01) that resumes cleanly (SEND-06).

Purpose: SEND-01 ("survives the HTTP request lifecycle and a worker restart") is realized here — the long-lived worker process is what makes the send background and crash-safe. The seams already carry the correctness; this plan wires them and handles process signals + env config.
Output: `lib/worker/loop.ts` + test, and a rewritten `worker/index.ts`.
</objective>

## Phase Goal (user story)

**As a** signed-in sender, **I want to** have my queued campaign sent in a crash-safe background job with live progress and a durable per-recipient record, **so that** I can reliably deliver my whole batch, watch it happen, resume it after any crash without double-sending, and later review or download exactly what was sent.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-background-worker-live-send-progress-history/06-RESEARCH.md
@.planning/phases/06-background-worker-live-send-progress-history/06-PATTERNS.md
@.planning/phases/06-background-worker-live-send-progress-history/06-01-SUMMARY.md
@.planning/phases/06-background-worker-live-send-progress-history/06-02-SUMMARY.md

<interfaces>
<!-- Seams from Plans 01 + 02 (import from @/lib/worker/*). -->

claimNextCampaign(workerId, leaseSec) -> Campaign | undefined
recoverOrphanedSending(campaignId) -> number
materializeSendRecords(campaign) -> { inserted, total }
runCampaign(campaign, opts) -> { ok:true, sent, failed } | { ok:false, reason }
markCompleted(campaignId) -> void
markFailed(campaignId, reason) -> void

From worker/index.ts skeleton (KEEP its structure): single db import from @/lib/db; single structured readiness log; a .unref()'d interval in the bare-import case. Phase-6 replaces the no-op body with tick() and makes the running interval ref'd.

pino is installed (10.3.1). Env conventions (lib/db/client.ts:27 idiom): read process.env.X with a fallback default.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: tick() composition seam</name>
  <files>lib/worker/loop.ts, lib/worker/loop.test.ts</files>
  <read_first>
    - lib/worker/claim.ts, recover.ts, finalize.ts (Plan 01 seams)
    - lib/worker/materialize.ts, process.ts (Plan 02 seams)
    - lib/campaign/actions.ts (thin composition-wrapper role analog)
    - lib/data/campaigns.test.ts (temp-DB harness)
    - .planning/phases/06-background-worker-live-send-progress-history/06-RESEARCH.md (Pattern 6 lines 204-206; verify-abort → markFailed lines 375-378)
  </read_first>
  <behavior>
    - tick({ workerId, leaseSec, delayMs, transportOverride? }) claims the next campaign; if none, returns a "no work" result without side effects.
    - On a claimed campaign it runs, in order: recoverOrphanedSending → materializeSendRecords → runCampaign → then markCompleted on runCampaign ok, or markFailed(reason) on runCampaign { ok:false }.
    - A resumed (re-claimed stalled) campaign: recover sweeps orphans, materialize is a no-op, runCampaign sends only remaining pending rows, then markCompleted — no double-send.
    - tick returns a small summary ({ claimed: boolean, campaignId?, sent?, failed?, outcome }).
  </behavior>
  <action>
    Implement `tick(opts: { workerId: string; leaseSec: number; delayMs: number; transportOverride?: MailTransport }): Promise<TickResult>` in lib/worker/loop.ts, importing the five seams from their sibling modules. Call `claimNextCampaign(opts.workerId, opts.leaseSec)`; if undefined return `{ claimed:false }`. Else: `recoverOrphanedSending(c.id)`, `materializeSendRecords(c)`, then `const r = await runCampaign(c, { transportOverride: opts.transportOverride, delayMs: opts.delayMs, onHeartbeat })` where onHeartbeat bumps the campaign's lease (a single `UPDATE campaigns SET lease_expires_at = unixepoch() + opts.leaseSec WHERE id=?`). On `r.ok` call `markCompleted(c.id)` and return the sent/failed summary; on `!r.ok` call `markFailed(c.id, r.reason)`. Accept the transport override + delayMs strictly through opts so the test injects a stub and delayMs=0. Write the test first with the temp-DB harness + stub transport: assert the full happy path (queued→completed with all rows sent), the verify-abort path (stub verify throws → campaign 'failed', zero sent), and the resume path (pre-seed some 'sent' + an orphaned 'sending' row on a stalled 'running' campaign → tick re-claims, orphan becomes interrupted-failed, only pending sent, campaign completed, no row double-sent).
  </action>
  <verify>
    <automated>node --import tsx --test "lib/worker/loop.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test lib/worker/loop.test.ts` exits 0.
    - The happy-path test asserts the campaign ends 'completed' with sent_count == recipient count and failed_count 0.
    - The verify-abort test asserts the campaign ends 'failed' and the stub sendMail was never called.
    - The resume test asserts sendMail was called only for the pending rows and the orphaned 'sending' row ended 'failed' with the interrupted error string, campaign 'completed'.
    - loop.ts imports all five seams from @/lib/worker/* (grep) and calls markCompleted on ok, markFailed on !ok.
  </acceptance_criteria>
  <done>tick composes the full lifecycle with injectable deps; happy/abort/resume paths proven; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: worker/index.ts composition root (pino, poll, SIGTERM, env config)</name>
  <files>worker/index.ts</files>
  <read_first>
    - worker/index.ts (the current skeleton — keep its structure, readiness-log discipline, unref note)
    - lib/worker/loop.ts (tick signature)
    - lib/db/client.ts (env-with-default idiom, line 27)
    - .planning/phases/06-background-worker-live-send-progress-history/06-RESEARCH.md (Pattern 7 SIGTERM lines 208-215; A6 throttle env lines 363; Assumptions A6/A1 poll interval)
  </read_first>
  <action>
    Replace worker/index.ts. Build a pino logger (`import pino from "pino"`) replacing the skeleton's console.log readiness line — log a single structured "worker ready" line with component:"worker" and NO secrets. Read config from env with defaults: `SEND_DELAY_MS` (default 1000), `WORKER_POLL_MS` (default 2000), `WORKER_LEASE_SEC` (default 300), and a `workerId` (e.g. `worker-${process.pid}` or `WORKER_ID` env). Declare `let stopping = false;` and register `for (const sig of ["SIGTERM","SIGINT"]) process.on(sig, () => { stopping = true; logger.info(...); })`. Set a ref'd `setInterval` (NOT unref'd — the real worker must stay alive) that, each poll, if `!stopping` and no tick is currently in flight, calls `tick({ workerId, leaseSec, delayMs })` inside a guard preventing overlapping ticks (a simple `inFlight` boolean); log the tick outcome (claimed/sent/failed) via pino, never a secret; on a caught tick error log the message string only (never the raw Error object with a config). When `stopping` becomes true, clear the interval and let any in-flight tick finish, then exit 0. Keep the "bare readiness import exits cleanly" affordance only if trivial; the primary path is a long-lived ref'd loop. Do NOT log the SMTP password, config, or send bodies. Do NOT open a second DB (import db from @/lib/db only if needed for the lease bump — the lease bump lives in loop.ts, so index.ts need not touch the DB directly).
  </action>
  <verify>
    <automated>node --import tsx -e "import('./worker/index.ts').then(()=>{ setTimeout(()=>process.exit(0), 300); })" 2>&1 | grep -q "worker ready"</automated>
  </verify>
  <acceptance_criteria>
    - Importing worker/index.ts logs a single structured "worker ready" line (pino JSON) and does not throw.
    - worker/index.ts registers handlers for both "SIGTERM" and "SIGINT" that set a stopping flag (grep confirms both signals).
    - worker/index.ts reads SEND_DELAY_MS, WORKER_POLL_MS, and WORKER_LEASE_SEC from process.env with numeric defaults (grep confirms the three env reads).
    - worker/index.ts contains no reference to a password/secret field and no `console.log` of config; `grep -Ei "password|pass:|secret" worker/index.ts | grep -v '^#'` returns nothing.
    - worker/index.ts imports `tick` from @/lib/worker/loop and calls it on the interval.
  </acceptance_criteria>
  <done>The worker process boots, logs readiness via pino, polls tick() on a ref'd interval, honors SIGTERM/SIGINT, and sources config from env; no secrets logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS signals → worker | SIGTERM/SIGINT (Docker/Coolify stop) must not corrupt in-flight state |
| worker logs → operator | pino output must never carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering (mid-batch corruption) | ungraceful stop during a send | mitigate | SIGTERM sets a stopping flag; the loop stops claiming new work and lets the in-flight tick finish; every row is already committed synchronously (Plan 02) so even SIGKILL is recoverable via Plan 01 recover |
| T-06-12 | Information Disclosure | pino logging a secret | mitigate | Readiness/tick-outcome logs only; grep gate asserts no password/secret token in worker/index.ts; caught errors log message strings only |
| T-06-13 | Denial of Service | overlapping ticks claiming multiple campaigns per poll | mitigate | inFlight guard ensures at most one tick runs at a time; single-writer claim serializes anyway |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages (pino already installed since Phase 1); 06-RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
- `npm test` full suite green (loop.test.ts included).
- Worker boots and logs "worker ready" without a live SMTP or Clerk context.
- Docker `stop_grace_period` / PID-1 init tuning is explicitly OUT of scope (Phase 8) — this plan owns only the in-process SIGTERM handler.
</verification>

<success_criteria>
tick() composes the full crash-safe lifecycle and worker/index.ts runs it as a long-lived, signal-aware, pino-logging, env-configured background process — SEND-01 and SEND-06 realized end-to-end at the seam+process level.
</success_criteria>

<output>
Create `.planning/phases/06-background-worker-live-send-progress-history/06-04-SUMMARY.md` when done.
</output>
