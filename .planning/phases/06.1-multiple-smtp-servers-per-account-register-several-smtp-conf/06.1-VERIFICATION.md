---
phase: 06.1-multiple-smtp-servers-per-account-register-several-smtp-conf
verified: 2026-07-15T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification_completed: true # blocking walkthrough checkpoint (06.1-04 Task 3) performed and approved by the user 2026-07-15
---

# Phase 06.1: Multiple SMTP Servers Per Account — Verification Report

**Phase Goal:** A user can register several named SMTP servers on one account — each verified and encrypted independently — and pick which one a given campaign sends through, with existing single-server accounts migrating transparently.
**Verified:** 2026-07-15
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Multiple named servers; each verified before save; each stores its own AES-256-GCM password; verified status shown per row | ✓ VERIFIED | `lib/smtp/actions-core.ts:115-225` `applyVerifiedConfig` — parse → label-unique → `verifyFn` → persist ONLY on clean verify; `encrypt()` is `createCipheriv("aes-256-gcm", ...)` (`lib/crypto/index.ts:53`); single-row unique index dropped (`drizzle/0003_pale_wallow.sql` `DROP INDEX smtp_configs_user_uq`); per-row `Verified` badge conditional on `verified_at !== null` (`components/smtp/server-list.tsx:199-204`) |
| 2 | Compose flow lets user pick the server; single server auto-selected zero-click; `smtp_config_id` stamped on the campaign | ✓ VERIFIED | `components/campaign/send-card.tsx` — zero-server gate / single-server static "Sending over …" line (no dropdown) / multi-server `Select` with "Choose a server" placeholder; auto-select in `components/compose/compose-editor.tsx:102-106` (`initialSmtpConfigId`: default → sole server → null); stamped at prepare in `lib/campaign/actions-core.ts:365-372` (`smtp_config_id: cfg.id` on draft insert) |
| 3 | Send paths load config owner-scoped by id; cross-tenant/unknown → not_found (IDOR-safe) | ✓ VERIFIED | Sole by-id path is `getSmtpConfigByIdForUser` with `and(eq(id), eq(userId), isNull(deleted_at))` (`lib/data/smtp.ts:88-100`); compose test-send owner-re-resolves the proposed id (`lib/campaign/actions-core.ts:180-181`); confirm summary resolves `campaign.smtp_config_id`, never a client value (`:419-420`); wizard `sendTestEmail` resolves by owned id (`lib/smtp/actions.ts:202-205`); IDOR tested: `lib/campaign/actions-core.test.ts:416` (another tenant's server → not_found, nothing sent); no `getSmtpConfigForUser`/`upsertSmtpConfig` reference survives anywhere; Phase 6 worker contract set to `getSmtpConfigByIdForUser(campaign.userId, campaign.smtp_config_id)` in `06-02-PLAN.md` |
| 4 | Existing accounts migrate zero-action: surviving row becomes the default server | ✓ VERIFIED | `drizzle/0004_backfill_default_smtp.sql` stamps `label = COALESCE(label,'Default'), is_default = 1` on non-deleted rows; ordered after 0003 in `drizzle/meta/_journal.json`; integration-tested at `lib/db/migration.test.ts:118` ("SC4: surviving pre-06.1 row is backfilled…") plus partial-unique-index shape assertions (`:143`, `:176`) |
| 5 | Deleting a server never corrupts campaign history; a queued/running-referenced server cannot be silently removed | ✓ VERIFIED | Soft delete only (`lib/data/smtp.ts:245-257` stamps `deleted_at`, clears `is_default`; row survives on disk — tested `lib/data/smtp.test.ts:173-183`); in-use guard runs FIRST (`lib/smtp/actions-core.ts:303-316` `countActiveSendsForConfig > 0 → in_use`), counting only queued/running (`lib/data/smtp.ts:266-282`, tested `:195-231`); blocked-delete tested end-to-end (`lib/smtp/actions.test.ts:505`); UI surfaces the in-use error in-dialog and keeps the row (`components/smtp/server-list.tsx:124-133`) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/db/schema.ts` | label/is_default/deleted_at + partial unique index, no user_uq | ✓ VERIFIED | `smtp_configs_user_default_uq` partial index `WHERE is_default = 1` (lines 62-78); no per-user unique index remains |
| `drizzle/0003_pale_wallow.sql` | additive DDL migration | ✓ VERIFIED | DROP INDEX + 3× ALTER ADD + partial unique index; no table rebuild |
| `drizzle/0004_backfill_default_smtp.sql` | zero-action backfill | ✓ VERIFIED | COALESCE label + is_default=1 for non-deleted rows |
| `lib/data/smtp.ts` | id-scoped soft-delete-aware DAL | ✓ VERIFIED | All planned exports present incl. `toSmtpConfigDto` (password triple structurally absent from DTO); CR-01 fix present (`setDefaultSmtpConfig` throws sentinel to roll back the clear when target matches 0 rows, lines 208-236) with regression test (`lib/data/smtp.test.ts:140-154`) |
| `lib/db/migration.test.ts` | migration integration test | ✓ VERIFIED | Backfill + index shape + second-default-throws assertions |
| `lib/smtp/schema.ts` | required ≤60-char label | ✓ VERIFIED | `.min(1)` / `.max(60)` (lines 62-66); meta schema is a pick of the same |
| `lib/smtp/actions-core.ts` | id-aware create/update + WR-09 + setDefault/softDelete seams | ✓ VERIFIED | WR-09 rejects blank-password + changed host BEFORE decrypt (lines 161-168); case-insensitive label uniqueness excluding self; first-server auto-default |
| `lib/smtp/actions.ts` | "use server" wrappers | ✓ VERIFIED | `createServer`/`updateServer`/`setDefaultServer`/`deleteServer`/`updateServerMeta`/`sendTestEmail(id)`; every wrapper re-derives userId via `auth()` |
| `lib/campaign/schema.ts` | `smtpConfigIdSchema` | ✓ VERIFIED | `z.coerce.number().int().positive()` (line 42) |
| `lib/campaign/actions-core.ts` | smtpConfigId threaded through prepare/test-send; confirm by campaign FK | ✓ VERIFIED | Lines 147/180 (test-send), 347/365-372 (prepare + stamp), 419 (confirm by stamped FK) |
| `components/campaign/send-card.tsx` | server picker with three states | ✓ VERIFIED | Server block renders before the template gate (806a16e walkthrough fix); "Send with" Select present |
| `components/smtp/server-list.tsx` | server-list island | ✓ VERIFIED | Default/Verified badges, Make default, edit, destructive AlertDialog delete with in-dialog in-use Alert, no-default callout (line 170) |
| `components/ui/alert-dialog.tsx` | shadcn alert-dialog primitive | ✓ VERIFIED | Full primitive set exported |
| `components/smtp/smtp-wizard.tsx` | label field + client WR-09 gate | ✓ VERIFIED | `hostChangeGate` (lines 151-157) mirrors server copy; threads `createServer`-returned id to test-send step (2a1694a fix) |
| `app/(app)/settings/smtp/page.tsx` | RSC listing redacted DTOs | ✓ VERIFIED | `listSmtpConfigsForUser` → `toSmtpConfigDto` → `ServerList` |
| `app/(app)/dashboard/page.tsx` | readiness = ≥1 verified server | ✓ VERIFIED | `verified.length === 0` soft-gate; "At least one verified SMTP server" copy; "N more verified" overflow |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `getSmtpConfigByIdForUser` | `smtp_configs` | `and(eq(id), eq(userId), isNull(deleted_at))` — sole by-id path, no `eq(id)`-alone query found | ✓ WIRED |
| `lib/data/index.ts` | `lib/data/smtp.ts` | barrel re-export (compose page imports `listSmtpConfigsForUser`/`toSmtpConfigDto`; `SmtpConfigDto` imported from `@/lib/data` in send-card) | ✓ WIRED |
| actions-core blank-keep merge | `getSmtpConfigByIdForUser` | fetch by id, host compare BEFORE decrypt | ✓ WIRED |
| `deleteServer` | `countActiveSendsForConfig` → `softDeleteSmtpConfig` | guard precedes delete in `softDeleteConfigCore` | ✓ WIRED |
| `prepareCampaignCore` | `getSmtpConfigByIdForUser(userId, cfgIdParsed.data)` | resolve then stamp `cfg.id` on the draft | ✓ WIRED |
| `buildConfirmSummaryCore` | `getSmtpConfigByIdForUser(userId, campaign.smtp_config_id)` | campaign's own stamped FK | ✓ WIRED |
| `send-card.tsx` | `TestSendPanel` / `ConfirmSendDialog` | `smtpConfigId` passed as required prop, both gated behind `smtpConfigId !== null` | ✓ WIRED |
| `server-list.tsx` | `setDefaultServer` / `deleteServer` | action call + `busyId` double-submit guard + refresh | ✓ WIRED |
| settings RSC | `listSmtpConfigsForUser` + `toSmtpConfigDto` | redacted DTO list to client island | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Real Data | Status |
|----------|---------------|--------|-----------|--------|
| `server-list.tsx` | `configs` prop | settings RSC → `listSmtpConfigsForUser(userId)` (DB query) → DTO map | Yes | ✓ FLOWING |
| `send-card.tsx` picker | `configs` prop | compose RSC → `listSmtpConfigsForUser` filtered `verified_at !== null` → DTO map | Yes | ✓ FLOWING |
| dashboard readiness | `verified` | `listSmtpConfigsForUser` → filter on `verified_at` | Yes | ✓ FLOWING |
| `smtpConfigId` selection | compose-editor state | `initialSmtpConfigId(configs)` (default → sole → null), updated by picker `onSelect` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles | `npx tsc --noEmit` | exit 0, no errors | ✓ PASS |
| Full test suite | `npm test` | 212 pass / 0 fail (includes migration SC4 test, CR-01 rollback regression, WR-09 zero-verify-call reject, in-use delete block, cross-tenant test-send not_found) | ✓ PASS |
| Retired single-server surface gone | `grep -r "getSmtpConfigForUser\|upsertSmtpConfig" lib app components worker` | 0 matches | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist in this repo and none are declared by the 06.1 plans. SKIPPED (no probes defined); `npm test` is the phase's runnable verification and passes 212/212.

### Requirements Coverage

ROADMAP lists this inserted phase's requirements as "TBD (extends SMTP-01..05)". The plans declare phase-local IDs MSMTP-01..05, which do not appear in `.planning/REQUIREMENTS.md` (no orphaned REQUIREMENTS.md rows map to 06.1).

| Requirement | Source Plans | Status | Evidence |
|-------------|-------------|--------|----------|
| MSMTP-01 (multiple named servers, verify + encrypt each) | 01, 02, 04 | ✓ SATISFIED | SC1 evidence above |
| MSMTP-02 (per-campaign server choice) | 03 | ✓ SATISFIED | SC2 evidence above |
| MSMTP-03 (owner-scoped by-id load, IDOR-safe) | 01, 03 | ✓ SATISFIED | SC3 evidence above |
| MSMTP-04 (transparent migration) | 01 | ✓ SATISFIED | SC4 evidence above |
| MSMTP-05 (safe delete + in-use guard) | 01, 02, 04 | ✓ SATISFIED | SC5 evidence above |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TBD/FIXME/XXX/TODO/placeholder/empty-return patterns in any file modified by this phase | — | — |

Notes (informational only):
- `.planning/ROADMAP.md:261` still reads "3/4 plans executed" while all four plan checkboxes are checked — stale counter in a planning doc, not phase code.
- ROADMAP marks the phase `Mode: mvp` but the goal is not in User Story format; this matches the project-wide convention (Phases 7/8 identical) and the orchestrator supplied explicit success criteria, so standard goal-backward verification was applied.
- Review WARN-level findings (unlimited `sendTestEmail` recipient, verify TOCTOU window, wedged client state on rejected actions, orphaned drafts) remain open by design — none blocks a success criterion; Critical CR-01 is fixed (fc4f2f4) and regression-tested.

### Human Verification Required

None outstanding. The blocking human-verify checkpoint (06.1-04 Task 3, multi-server walkthrough against 06.1-UI-SPEC: multi-server add, WR-09 gate, default swap, compose picker, deletes and in-use guard) was performed and approved by the user on 2026-07-15. Two walkthrough-surfaced bugs were fixed on master and verified in code here: 8577916 (id-scoped label-only edit — `updateSmtpConfigMeta`/`updateMetaCore` no longer clobber siblings) and 806a16e (compose server picker renders before the template gate).

### Gaps Summary

No gaps. All five ROADMAP success criteria are observably true in the codebase, backed by the DAL/action/migration test suite (212/212), a clean `tsc --noEmit`, the fixed-and-regression-tested CR-01 default-swap rollback, and the completed human walkthrough.

---

_Verified: 2026-07-15_
_Verifier: Claude (gsd-verifier)_
