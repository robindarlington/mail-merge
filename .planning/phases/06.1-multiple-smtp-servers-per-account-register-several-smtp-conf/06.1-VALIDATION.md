---
phase: 6.1
slug: multiple-smtp-servers-per-account-register-several-smtp-conf
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-14
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Back-filled from the 4 approved PLAN.md files' `<verify>` blocks + 06.1-RESEARCH.md `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in) run via `node --import tsx --test` |
| **Config file** | none — glob in package.json `test` script (`lib/**/*.test.ts`) |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm test` (full suite)
- **Before `/gsd:verify-work`:** Full suite green + the Plan 04 Task 3 human-verify walkthrough
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 1 | SC4 | migration data-loss gate | [BLOCKING] generated SQL is `DROP INDEX` + `ADD COLUMN` shape — NO `__new_smtp_configs` rebuild; backfill sets surviving row label='Default', is_default=1 | unit (tdd) | `node --import tsx --test lib/db/migration.test.ts && grep -q "DROP INDEX" drizzle/0003_*.sql && ! grep -q "__new_smtp_configs" drizzle/0003_*.sql` | ❌ W0 | ⬜ pending |
| 06.1-01-02 | 01 | 1 | SC1, SC3 | IDOR by-id reads | id-scoped CRUD with `and(eq(id), eq(userId), isNull(deleted_at))`; DTO still omits encrypted triple; retired helpers gone | unit (tdd) | `node --import tsx --test lib/data/smtp.test.ts && ! grep -q "getSmtpConfigForUser\|upsertSmtpConfig" lib/data/smtp.ts` | ✅ rewrite | ⬜ pending |
| 06.1-02-01 | 02 | 2 | SC1, SC5 | WR-09 host-change gate; label rules | Host change + blank password → rejected BEFORE decrypt; labels required/≤60/case-insensitively unique; in-use guard seam | unit | `node --import tsx --test lib/smtp/actions.test.ts > /dev/null 2>&1 && grep -q "Re-enter the password" lib/smtp/actions-core.ts && grep -q "in_use" lib/smtp/actions-core.ts && ! grep -q "upsertSmtpConfig" lib/smtp/actions-core.ts` | ✅ extend | ⬜ pending |
| 06.1-02-02 | 02 | 2 | SC1, SC5 | auth-first wrappers | "use server" wrappers auth-guarded; per-config test-send; setDefault/softDelete flows | unit | `node --import tsx --test lib/smtp/actions.test.ts` | ✅ extend | ⬜ pending |
| 06.1-03-01 | 03 | 2 | SC2, SC3 | load-by-campaign-FK | Test-send/prepare load config by campaign.smtp_config_id owner-scoped; cross-tenant → not_found | unit | `node --import tsx --test lib/campaign/actions-core.test.ts && ! grep -q "getSmtpConfigForUser" lib/campaign/actions-core.ts && grep -q "smtpConfigIdSchema" lib/campaign/schema.ts` | ✅ extend | ⬜ pending |
| 06.1-03-02 | 03 | 2 | SC2 | — | Compose picker; auto-select single server; chosen id threaded to send card/test-send | tsc + grep gates | `npx tsc --noEmit && grep -q "Send with" components/campaign/send-card.tsx && grep -q "smtpConfigId" components/campaign/test-send-panel.tsx` | — | ⬜ pending |
| 06.1-03-03 | 03 | 2 | SC3 (Phase 6 contract) | — | 06-02-PLAN.md amended: worker loads by getSmtpConfigByIdForUser(campaign.userId, campaign.smtp_config_id) | grep gate | `! grep -q "getSmtpConfigForUser" .planning/phases/06-background-worker-live-send-progress-history/06-02-PLAN.md && grep -q "getSmtpConfigByIdForUser" .planning/phases/06-background-worker-live-send-progress-history/06-02-PLAN.md` | — | ⬜ pending |
| 06.1-04-01 | 04 | 3 | SC1 (WR-09 client) | client-side WR-09 mirror | Wizard label field; host-dirty + blank password client gate; alert-dialog added (official registry) | grep gates | `test -f components/ui/alert-dialog.tsx && grep -q "createServer" components/smtp/smtp-wizard.tsx` | — | ⬜ pending |
| 06.1-04-02 | 04 | 3 | SC1, SC5 | XSS discipline | Server list island (status/default/actions), settings RSC, dashboard readiness; escaped JSX only | grep gates + `npm test` regression | `grep -q "listSmtpConfigsForUser" "app/(app)/settings/smtp/page.tsx" && grep -q "Make default" components/smtp/server-list.tsx` | — | ⬜ pending |
| 06.1-04-03 | 04 | 3 | all SC | T-delete/no-default | [CHECKPOINT human-verify] multi-server walkthrough: add 2nd server, set default, delete default → no-default state, in-use guard, compose picker, WR-09 gate | manual checkpoint | see Manual-Only | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `lib/db/migration.test.ts` — new: migration shape + backfill assertions (06.1-01-01 creates RED-first)
- [ ] `lib/data/smtp.test.ts` — rewrite: drops the manual unique-index recreation, covers id-scoped CRUD + soft-delete + default swap + DTO redaction
- [ ] `lib/smtp/actions.test.ts` — extend: WR-09 host-change gate (incl. no-decrypt-before-check), label rules, in-use guard, wrappers
- [ ] `lib/campaign/actions-core.test.ts` — extend: load-by-campaign-FK, cross-tenant not_found
- [ ] Shared fixtures: existing temp-DB + stubTransport harness (no new framework)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Multi-server walkthrough (06.1-04-03, blocking checkpoint) | all 5 SC | UI + real verify flow | Add a 2nd server (verify), set default, delete the default → "choose a server" state (no auto-promote), attempt delete of an in-use server → guard error, compose picker auto-select/choose, host-change edit forces password re-entry |
| Migration on the live staging volume | SC4 | real /data volume | Deploy; existing single server still works, shows label "Default" + default marker with zero user action |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-14 (back-filled from the 4 plans' verify blocks after checker revision pass)
