---
phase: 07-per-row-attachments
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - .planning/phases/07-per-row-attachments/07-STAGING-NOTES.md
requirements: [ATCH-01, ATCH-02, ATCH-03]
autonomous: false
must_haves:
  truths:
    - "The per-row-attachments slice is deployed to the standing Coolify staging URL and both containers are healthy"
    - "A real send delivers the correct per-row file to each recipient over BYO SMTP on staging"
    - "A missing referenced file blocks the send at the confirm gate before anything is sent"
    - "A file deleted from the volume after enqueue fails only that row (rejected: attachment missing) and the campaign still completes"
  artifacts:
    - path: ".planning/phases/07-per-row-attachments/07-STAGING-NOTES.md"
      provides: "recorded staging deploy + attachment walkthrough evidence"
      min_lines: 20
  key_links:
    - from: "Coolify staging"
      to: "shared /data volume (uploads + WAL'd SQLite)"
      via: "web writes uploads; worker reads them at send time"
      pattern: "UPLOADS_PATH"
---

<objective>
Deploy the Phase 7 slice to the standing Coolify staging environment and verify per-row attachments there end-to-end: a real send that delivers the correct file to each recipient, the blocking pre-send validation when a referenced file is missing, and the graceful `rejected: attachment missing` single-row failure when a file vanishes from the volume mid-run — closing phase success criterion #4.

Purpose: Opaque-id storage on a shared `/data` volume, the web→worker file handoff, and the confirm-gate block only fully prove out on the real two-container VPS topology. Unit tests prove the seams; staging proves the deployed system. This is the phase's human-verified acceptance gate. It is a QUEUED checkpoint (operator preference: later work need not block on it, but Phase 7 is not shipped until it passes).
Output: `07-STAGING-NOTES.md` recording the deploy + the three verifications.
</objective>

## Phase Goal (user story)

**As a** signed-in sender, **I want to** attach a different file to each row of my CSV — with every referenced file validated as present and resolved safely — **so that** I can send personalized emails that each carry the right per-recipient file with confidence nothing is missing or unsafe.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-per-row-attachments/07-CONTEXT.md
@.planning/phases/07-per-row-attachments/07-05-SUMMARY.md
@.planning/phases/06-background-worker-live-send-progress-history/06-07-PLAN.md

<notes>
- The docker-compose already defines web + worker sharing the `/data` volume (Phase 1 skeleton) and the Coolify staging pipeline is established (Phase 6). This plan redeploys the current branch and confirms the UPLOADS_PATH volume is shared so the worker can read files the web wrote.
- CONFIRM `UPLOADS_PATH` points at a path on the shared `/data` volume for BOTH containers (e.g. `/data/uploads`) — otherwise the worker cannot see uploaded attachments. This is the one new operational dependency this phase adds.
- Use a throwaway BYO test SMTP account reachable from the VPS; never commit credentials — inject via Coolify secrets. Do not print secret values into the notes.
- Autonomy: queued human checkpoint. Per the operator's standing preference, later phases need not block on it, but it MUST be completed before Phase 7 is marked verified/shipped.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the shared uploads volume + redeploy the slice to Coolify staging</name>
  <files>.planning/phases/07-per-row-attachments/07-STAGING-NOTES.md</files>
  <read_first>
    - .planning/phases/06-background-worker-live-send-progress-history/06-07-PLAN.md (the established staging deploy + notes idiom)
    - docker-compose / Coolify config for the web + worker services and the /data volume mount
    - 07-05-SUMMARY.md (what the slice added)
  </read_first>
  <action>
    Confirm both the web and worker containers mount the shared `/data` volume and that `UPLOADS_PATH` resolves to a directory on it for BOTH (e.g. `/data/uploads`) — the worker reads at send time the exact files the web wrote at upload time. Confirm the migration `0006` will run on deploy (the migrate step). Push the current branch and trigger a Coolify redeploy of the standing staging environment. After deploy, confirm BOTH containers are healthy, migration 0006 applied cleanly, and the worker logged its readiness line. Record the staging URL, the deployed commit SHA, both container health states, the confirmed shared UPLOADS_PATH, and the migration-applied evidence in 07-STAGING-NOTES.md. Never print or commit any secret value — record only that each secret is set.
  </action>
  <verify>
    <automated>test -f ".planning/phases/07-per-row-attachments/07-STAGING-NOTES.md" && grep -qi "UPLOADS_PATH" ".planning/phases/07-per-row-attachments/07-STAGING-NOTES.md"</automated>
  </verify>
  <acceptance_criteria>
    - Notes record the staging URL, deployed commit SHA, both container health states, and the shared UPLOADS_PATH for both services.
    - Migration 0006 is confirmed applied on staging.
    - No secret value appears in the notes or any committed file.
  </acceptance_criteria>
  <done>The Phase-7 slice is deployed to staging with a confirmed shared uploads volume and migration 0006 applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Per-row attachments end-to-end: multi-file upload + column confirm on /compose, a server-authoritative confirm-gate that blocks on missing/oversize files, and a worker that attaches the correct per-recipient file (failing only the row whose file is missing) — deployed to Coolify staging.</what-built>
  <how-to-verify>
    1. Sign in on the staging URL with a verified BYO test SMTP config, a saved template, and a small CSV (5-8 rows) that has a filename column (e.g. `attachment`) naming a distinct file per row (leave one row's cell EMPTY).
    2. HAPPY PATH: on /compose, select the list, upload the files named by the CSV, confirm the auto-detected attachment column, and CONFIRM the match summary reports the right counts (matched rows + the empty-cell row "will send without one"). Clear the confirm-send dialog (it shows "Attachments: {m} of {n}…" and the all-clear check) and send. CONFIRM in the test inbox: each recipient received THEIR file (correct per-row attachment); the empty-cell recipient received an email with NO attachment.
    3. BLOCKING VALIDATION: start a new campaign; in the CSV reference a filename you do NOT upload (or remove one uploaded file after matching). CONFIRM the compose card shows the destructive "Some attachments are missing" Alert AND the confirm-send dialog disables the "Send" button with the "Missing attachments" Alert — you cannot send. Upload the missing file → the block clears → send succeeds.
    4. GRACEFUL SEND-TIME MISS (poison-pill): enqueue a valid send, then (before/while the worker runs) delete one referenced file from the `/data/uploads` volume in the worker container. CONFIRM: that one recipient's row ends "Failed" with reason "Attachment missing — the file wasn't available at send time…" (destructive), every OTHER recipient still sends, and the campaign completes (not aborted).
    5. HISTORY + EXPORT: open the campaign detail — the results table shows the "Attachment" column with each row's filename (or "—"); download the results CSV and CONFIRM it includes the attachment column and any value starting with = + - @ is quote-prefixed (formula-injection-safe).
    6. TRAVERSAL/IDOR spot-check: confirm nothing in the UI or CSV ever exposes a storage path (only original filenames); a cross-tenant campaign export id still returns 404.
    Record pass/fail for each step (2-6) in 07-STAGING-NOTES.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public staging URL → app | Real (test) users upload arbitrary files + send over a real SMTP on the VPS topology |
| web upload → worker read | Files cross containers only via the shared /data volume by opaque id, never by client path |
| Coolify secrets → containers | SMTP creds + CREDENTIAL_ENC_KEY injected at runtime, never in the image or repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering (bypass validation) | confirm gate on staging | mitigate | Step 3 confirms the send is impossible with a missing file (server-authoritative block, ATCH-02) |
| T-07-18 | Denial of Service (poison pill) | send-time missing file | mitigate | Step 4 confirms one row fails "attachment missing" and the campaign completes (ATCH-01/poison-pill) |
| T-07-19 | Information Disclosure | storage path / cross-tenant export | mitigate | Step 6 confirms only original filenames surface and a cross-tenant export id 404s |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
- Both staging containers healthy; migration 0006 applied; shared UPLOADS_PATH confirmed.
- Correct per-row delivery, blocking missing-file validation, graceful send-time miss, attachment column in results + export, and no-path-leak/IDOR all pass and are recorded in 07-STAGING-NOTES.md.
</verification>

<success_criteria>
Phase success criterion #4 met: the Phase-7 slice runs on the standing Coolify staging URL; a real send delivers the correct per-row file to every recipient, a missing file blocks the send before it starts, a file lost mid-run fails only its row, and the attachment history/export surfaces work — human-verified and recorded.
</success_criteria>

<output>
Create `.planning/phases/07-per-row-attachments/07-06-SUMMARY.md` when done.
</output>
