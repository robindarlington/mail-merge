---
phase: 07-per-row-attachments
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-03, 07-04]
files_modified:
  - components/compose/attachments-card.tsx
  - components/compose/compose-editor.tsx
  - app/(app)/compose/page.tsx
  - components/campaign/confirm-send-dialog.tsx
  - components/campaign/recipient-results-table.tsx
  - app/(app)/campaigns/[id]/page.tsx
  - app/(app)/campaigns/[id]/export/route.ts
  - lib/campaign/results-csv.ts
requirements: [ATCH-01, ATCH-02, ATCH-03]
autonomous: true
must_haves:
  truths:
    - "On /compose a user can upload files, confirm/override the auto-detected attachment column, and see a server-computed match summary"
    - "The confirm-send dialog shows attachment counts and DISABLES the confirm button when files are missing or a row is over the 15 MB limit"
    - "The campaign detail results table and CSV export show each row's attachment filename and map 'rejected: attachment missing' to a friendly reason"
    - "Every filename renders as escaped JSX text (untrusted, like a CSV cell)"
  artifacts:
    - path: "components/compose/attachments-card.tsx"
      provides: "multi-file upload island + attachment-column Select + match summary, per 07-UI-SPEC"
      min_lines: 120
    - path: "components/campaign/confirm-send-dialog.tsx"
      provides: "attachment summary lines + missing/oversize blocking disable"
      contains: "missingAttachmentCount"
  key_links:
    - from: "components/compose/attachments-card.tsx"
      to: "lib/attachments/actions"
      via: "uploadAttachment (one call per file) / deleteAttachment / confirmAttachmentColumn"
      pattern: "uploadAttachment"
    - from: "components/campaign/confirm-send-dialog.tsx"
      to: "summary.missingAttachmentCount / oversizeRowCount"
      via: "confirmDisabled extended"
      pattern: "oversizeRowCount"
    - from: "app/(app)/campaigns/[id]/export/route.ts"
      to: "listAttachmentsForCampaign"
      via: "attachment filename per send_record in the CSV"
      pattern: "attachment"
---

<objective>
Ship the user-facing attachment surfaces exactly as 07-UI-SPEC.md specifies: the "Attachments (optional)" card on `/compose` (multi-file upload → confirm/override the auto-detected column → server-computed match summary), the confirm-send dialog's attachment lines + blocking disable, and the attachment column in the campaign-detail results table and CSV export. All numbers are server-computed (Plan 03/04); the client only displays them. Zero new shadcn components, zero new npm packages.

Purpose: This is the vertical slice made visible — the backend seams (upload, validate, attach) become a coherent compose→confirm→history experience. The blocking gate that this surfaces is the server-authoritative one from Plan 03 (the dialog disables the button; it never invents the block).
Output: the attachments card + editor/page wiring, the dialog extension, and the results/export attachment column.
</objective>

## Phase Goal (user story)

**As a** signed-in sender, **I want to** attach a different file to each row of my CSV — with every referenced file validated as present and resolved safely — **so that** I can send personalized emails that each carry the right per-recipient file with confidence nothing is missing or unsafe.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-per-row-attachments/07-CONTEXT.md
@.planning/phases/07-per-row-attachments/07-UI-SPEC.md
@.planning/phases/07-per-row-attachments/07-02-SUMMARY.md
@.planning/phases/07-per-row-attachments/07-03-SUMMARY.md
@.planning/phases/07-per-row-attachments/07-04-SUMMARY.md

<interfaces>
<!-- Server actions (Plan 02). -->
uploadAttachment(formData) / listAttachments() / deleteAttachment(id) / confirmAttachmentColumn(setId, column)
// closed ActionError union: unauthenticated | wrong_type | too_large | duplicate_filename | not_found | unknown

<!-- ConfirmSummary extension (Plan 03) — client DISPLAYS these, never derives them. -->
ConfirmSummary += { attachmentColumn, rowsWithAttachment, attachmentTotal,
  missingAttachmentFilenames: string[] (cap 5), missingAttachmentCount, oversizeRowCount, sample.attachment? }

<!-- Compose host (components/compose/compose-editor.tsx). -->
ComposeEditor({ sets: EditorSet[], configs: SmtpConfigDto[], ... })
// selectedId state (line 123), memo selectedSet (180); card column renders PreviewStepper + SendCard.
// EditorSet currently: { id, filename, label, row_count, columns_json }.

<!-- Confirm dialog (components/campaign/confirm-send-dialog.tsx). -->
noneSendable = summary.sendableCount === 0
confirmDisabled = loading || !summary || noneSendable || submitting   // EXTEND

<!-- Results (components/campaign/recipient-results-table.tsx + lib/campaign/results-csv.ts). -->
RecipientResultsTable({ records: SendRecord[] })   // add Attachment column + reason mapping
INTERRUPTED_PREFIX = "interrupted:"                 // add ATTACHMENT_MISSING_PREFIX mapping
toResultsCsv(rows)                                  // add attachment column, formula-injection-safe
</interfaces>

<notes>
- Follow 07-UI-SPEC.md copy VERBATIM (card title/description, help text, match-summary lines, blocking Alert titles/bodies, results reason). Filenames always render as escaped JSX text (T-06-15 stored-XSS rule).
- The attachments card renders only when a recipient list is selected (Assumption W2); it sits between the compose card and the PreviewStepper (W1). Uploaded-file list is a `divide-y` row stack (not a Table), Paperclip + filename + muted size + ghost X (W-inventory).
- The column picker has NO "none" option (W4); switching the list keeps files but resets the column + re-detects (W3). Missing-filename lists cap at 5 + "and {k} more." (W6).
- Attachment state (uploaded files, chosen column, server match summary) lifts to ComposeEditor so the SendCard/confirm gate see it; the card call `useRouter().refresh()` idiom follows csv-uploader.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attachments card + compose-editor host + compose page threading</name>
  <files>components/compose/attachments-card.tsx, components/compose/compose-editor.tsx, app/(app)/compose/page.tsx</files>
  <read_first>
    - components/recipients/csv-uploader.tsx (the closest analog — island shape, react-hook-form + shared zod, confirm-column Select at 304-321, parseFailureFor exhaustive switch, disabled={saving} double-submit guard)
    - components/compose/compose-editor.tsx:109-135,180-181,371-395 (props, selectedId/selectedSet, the card column where the new card slots in)
    - app/(app)/compose/page.tsx:31-56 (RSC userId-scoped listing + editorSets mapping — thread attachment data the same way)
    - app/(app)/lists/page.tsx (the divide-y file-row idiom)
  </read_first>
  <action>
    Create components/compose/attachments-card.tsx (`"use client"`) per 07-UI-SPEC "Attachments card": `Card` titled "Attachments (optional)" with the exact description copy; a `Form`/`FormField` wrapping `Input type="file" multiple` (no `accept`) + an accent "Upload files" `Button` (`Upload` icon; "Uploading…" + `Loader2` + disabled while in flight). On submit, loop ONE `uploadAttachment(fd)` call per selected file (bodySizeLimit constraint) — client-pre-check each file with `uploadAttachmentSchema.safeParse({ name, size })` and field-anchor per-file too-large/duplicate errors while still uploading the valid ones (W11); map the returned closed ActionError union exhaustively to field vs destructive `Alert` copy (upload-failed / session-expired). Render the uploaded-file list as a `divide-y` stack: `Paperclip` (muted) + escaped `{filename}` + muted formatted size ("{x.y} MB", KB under 1 MB) + a trailing `ghost` `X` icon button (aria-label "Remove {filename}") calling `deleteAttachment(id)`. Below it, the attachment-column `Select` (label "Attachment column", the csv-uploader email-column idiom verbatim, NO "none" item) with the detected/not-detected help copy; on change call `confirmAttachmentColumn(selectedSetId, column)`. Render the server match-summary lines (all-matched CheckCircle2/text-success, matched-with-empties, no-rows-named muted AlertCircle, missing-files destructive Alert capped at 5 + "and {k} more.", over-size destructive Alert, unreferenced-uploads muted) — DISPLAY ONLY from server-computed data, never derived from sample rows. Fire a Sonner toast "{n} files uploaded." on success. In compose-editor.tsx: lift attachment state (uploaded files list, chosen column, server match summary) into ComposeEditor; render `<AttachmentsCard>` between the compose card and the `PreviewStepper`, ONLY when `selectedId` is truthy; on list switch keep files but reset the chosen column and re-detect. Thread the selected set's columns into the card. In compose/page.tsx: after listing sets, also `listAttachments()` (or pass initial pending uploads) and include each set's `attachment_column` in the `editorSets` mapping so the card can prefill; pass into `<ComposeEditor>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "attachments-card|compose-editor|compose/page" | grep -vq error || npx tsc --noEmit</automated>
    <human-check>On /compose with a list selected: upload two files, confirm the auto-detected column, and verify the match summary + blocking states render per 07-UI-SPEC (deferred to the Plan 06 staging walkthrough).</human-check>
  </verify>
  <done>The Attachments card uploads one-file-per-call, persists the confirmed column, and renders the server match summary; it lifts state to ComposeEditor and renders only when a list is selected; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm-send dialog attachment lines + blocking disable</name>
  <files>components/campaign/confirm-send-dialog.tsx</files>
  <read_first>
    - components/campaign/confirm-send-dialog.tsx:136-137 (noneSendable + confirmDisabled — extend), :170-210 (summary/sample/checks blocks where lines slot in)
    - 07-UI-SPEC.md "Confirm-send dialog" copy table + "Confirm gate" interaction contract
  </read_first>
  <action>
    Extend the dialog to DISPLAY the Plan-03 ConfirmSummary attachment fields (the client computes nothing). Only when `summary.attachmentColumn` is active: add the top-block line "Attachments: {m} of {n} rows include one" (m=rowsWithAttachment, n=recipientCount); add "Attachment: {filename}" (muted, under Subject:) in the sample block when `summary.sample.attachment` is set; add the all-clear check "Every attachment matched an uploaded file." (`CheckCircle2`, text-success) when nothing is missing/oversize. When `summary.missingAttachmentCount > 0`, render the destructive "Missing attachments" `Alert` with the capped filename list copy; when `summary.oversizeRowCount > 0`, the destructive "Attachments too large" `Alert` — both verbatim from the UI-SPEC. Extend `confirmDisabled` to `loading || !summary || noneSendable || submitting || summary.missingAttachmentCount > 0 || summary.oversizeRowCount > 0` (the server also blocks enqueue — Plan 03 — this just makes the gate visible). When no attachment column is active, render NOTHING new (zero clutter for the common case). All filenames escaped JSX text.
  </action>
  <verify>
    <automated>grep -q "missingAttachmentCount" components/campaign/confirm-send-dialog.tsx && grep -q "oversizeRowCount" components/campaign/confirm-send-dialog.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The dialog shows attachment counts + sample + all-clear check, renders the destructive missing/oversize Alerts, and disables confirm when either is present; no attachment lines when no column is active; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Attachment column in the results table + CSV export</name>
  <files>components/campaign/recipient-results-table.tsx, app/(app)/campaigns/[id]/page.tsx, app/(app)/campaigns/[id]/export/route.ts, lib/campaign/results-csv.ts</files>
  <read_first>
    - components/campaign/recipient-results-table.tsx:25-33,37,97-135 (status/reason mapping, INTERRUPTED_PREFIX, TableHead/TableCell layout — add an Attachment column + reason mapping)
    - lib/campaign/results-csv.ts:28-70 (ResultsCsvRow shape, headers, statusLabel prefix-mapping, formula-injection guard)
    - app/(app)/campaigns/[id]/page.tsx:66-67,167 (records fetch + RecipientResultsTable render — augment with attachment filenames)
    - app/(app)/campaigns/[id]/export/route.ts (getSendRecordsForCampaign → toResultsCsv — augment with attachment filenames)
  </read_first>
  <action>
    Give each result row its attachment filename. In both the detail page RSC and the export route, after fetching send_records, also call `listAttachmentsForCampaign(userId, campaign.id)` and build a `Map<send_record_id, filename>`; pass that into the table / CSV. In recipient-results-table.tsx: add an "Attachment" `TableHead` between "Recipient" and "Status" (W7), and a muted `TableCell` showing the escaped filename or "—" (em dash) when none; add `ATTACHMENT_MISSING_PREFIX = "rejected: attachment missing"` and map it in `reasonFor` to the UI-SPEC reason "Attachment missing — the file wasn't available at send time. This email wasn't sent." with the destructive `XCircle` "Failed" status (inherited failed idiom — NOT the muted interrupted treatment, W8). Accept the attachment map via a new optional prop. In lib/campaign/results-csv.ts: add an `attachment` column to the header + `ResultsCsvRow` (original filename or empty), run it through the SAME formula-injection guard as every exported cell; `toResultsCsv` accepts the attachment lookup so the route can supply it. Keep all existing columns/behavior intact.
  </action>
  <verify>
    <automated>grep -q "Attachment" components/campaign/recipient-results-table.tsx && grep -q "attachment" lib/campaign/results-csv.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Results table shows an Attachment column + maps the attachment-missing failure to the friendly destructive reason; the export CSV gains a formula-injection-safe attachment column; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user filename → rendered UI/CSV | The original filename is untrusted (like a CSV cell) — escaped JSX everywhere, formula-injection-safe in the export |
| client display → send gate | The confirm block is server-authoritative (Plan 03); the disabled button is a mirror, never the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering (stored XSS) | filename rendering | mitigate | Filenames render as escaped JSX text everywhere (T-06-15 rule) |
| T-07-15 | Tampering (CSV formula injection) | results-csv attachment column | mitigate | Attachment cell runs through the existing formula-injection guard (T-06-18) |
| T-07-16 | Information Disclosure (IDOR) | detail page + export attachment lookup | mitigate | listAttachmentsForCampaign is userId-scoped; export route already 404s cross-tenant ids |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages/components this phase (07-UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the changed files.
- Attachments card, dialog attachment lines/block, and results/export attachment column all present per 07-UI-SPEC (visual confirmation deferred to the Plan 06 staging walkthrough).
</verification>

<success_criteria>
The compose→confirm→history attachment experience is implemented per 07-UI-SPEC: upload + column confirm + server match summary on /compose, a blocking confirm gate, and the attachment column in results + export. ATCH-01/02/03 are surfaced to the user.
</success_criteria>

<output>
Create `.planning/phases/07-per-row-attachments/07-05-SUMMARY.md` when done.
</output>
