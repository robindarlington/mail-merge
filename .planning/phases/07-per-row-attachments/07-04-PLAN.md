---
phase: 07-per-row-attachments
plan: 04
type: tdd
wave: 3
depends_on: [07-02]
files_modified:
  - lib/core/send.ts
  - lib/worker/materialize.ts
  - lib/worker/process.ts
  - lib/worker/process.test.ts
requirements: [ATCH-01, ATCH-02]
autonomous: true
must_haves:
  truths:
    - "The worker attaches the correct file per recipient at send time, resolved by DB id → opaque storage path (never a CSV-provided path)"
    - "An attachment missing on disk at send time fails only that row (rejected: attachment missing) and never aborts the campaign"
    - "Materialize links each send_record to its attachment; an empty attachment cell sends without one"
    - "A row with no attachment sends byte-for-byte as before (default transport unchanged)"
  artifacts:
    - path: "lib/core/send.ts"
      provides: "additive optional attachments field on MailTransport message + SendArgs, forwarded only when set"
      contains: "attachments"
    - path: "lib/worker/materialize.ts"
      provides: "per-row send_record → attachment linkage (stamp send_record_id)"
      contains: "send_record_id"
    - path: "lib/worker/process.ts"
      provides: "per-row attachment resolve + presence check + fenced rejected-attachment-missing fail"
      contains: "attachment missing"
  key_links:
    - from: "lib/worker/process.ts"
      to: "resolveAttachmentPath / attachmentExists"
      via: "DB id → opaque storage path, verify presence before sendOne"
      pattern: "resolveAttachmentPath"
    - from: "lib/worker/process.ts"
      to: "send_records fenced failed branch"
      via: "missing file → status='sending'→'failed' in one synchronous transaction"
      pattern: "rejected: attachment missing"
---

<objective>
Wire the send path so the worker attaches the right file per recipient — resolving strictly by DB id → opaque storage path, verifying on-disk presence, and failing a single row `rejected: attachment missing` through the EXISTING fenced transaction if the file vanished (never a throw that aborts the batch). Extend `lib/core/send.ts` additively so a no-attachment send is byte-for-byte unchanged, and stamp the send_record↔attachment link at materialize time.

Purpose: This closes success criterion #1 (worker attaches the correct file per recipient) and the send-time half of #2/#3 (safe resolution + graceful per-row failure). It must preserve every Phase 6 invariant: per-row synchronous-transaction commits fenced on `status='sending'`, and the poison-pill rule that one bad row never crashes the campaign.
Output: extended `lib/core/send.ts`, `lib/worker/materialize.ts`, `lib/worker/process.ts` + updated `process.test.ts`.
</objective>

## Phase Goal (user story)

**As a** signed-in sender, **I want to** attach a different file to each row of my CSV — with every referenced file validated as present and resolved safely — **so that** I can send personalized emails that each carry the right per-recipient file with confidence nothing is missing or unsafe.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-per-row-attachments/07-CONTEXT.md
@.planning/phases/07-per-row-attachments/07-PATTERNS.md
@.planning/phases/07-per-row-attachments/07-02-SUMMARY.md

<interfaces>
<!-- From Plans 01-02. -->
resolveAttachmentPath(storagePath): string   // absolute path, traversal-checked
attachmentExists(storagePath): boolean
detectAttachmentColumn(columns, rows)
listAttachmentsForCampaign(userId, campaignId)
getAttachmentForSendRecord(campaignId, sendRecordId)

<!-- Existing send contract (lib/core/send.ts:56-75) — extend ADDITIVELY. -->
interface MailTransport { sendMail(message: { from; to; subject; text }): Promise<{ messageId? }>; verify?() }
interface SendArgs { transport; from; to; subject; body }
sendOne(args): Promise<SendResult>   // never throws; { ok:true, messageId } | { ok:false, error }

<!-- Materialize idiom (lib/worker/materialize.ts:36,62,95-131). -->
INVALID_ADDRESS_ERROR = "rejected: invalid address"
emailColumn = set.email_column ?? detectEmailColumn(columns, rows)
insert(send_records).values({ ...,(valid ? {} : { status:"failed", error:INVALID_ADDRESS_ERROR }) }).onConflictDoNothing().returning({ id })
// counter-reconcile transaction (lines 118-131) — synchronous.

<!-- Process idiom (lib/worker/process.ts:173-251). -->
// claim row: UPDATE status='sending' WHERE id AND status='pending' RETURNING id (fence).
// sendOne (NOT in a transaction — better-sqlite3 synchronous). Terminal write + counter bump = one
// synchronous db.transaction fenced on status='sending' (CR-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive attachments on the transport surface + send_record↔attachment linkage at materialize</name>
  <files>lib/core/send.ts, lib/worker/materialize.ts</files>
  <read_first>
    - lib/core/send.ts:56-75,85-135 (MailTransport message shape, SendArgs, sendOne — the additive-optional SmtpConfig idiom at lines 93-103 is the pattern to copy)
    - lib/worker/materialize.ts:36,45-131 (INVALID_ADDRESS_ERROR, emailColumn resolution, the conditional insert + counter-reconcile transaction)
  </read_first>
  <behavior>
    - MailTransport's sendMail message and SendArgs each gain an OPTIONAL `attachments?: { filename: string; path: string }[]`; sendOne forwards it to `transport.sendMail` ONLY when set (a send with no attachments produces a byte-for-byte identical call).
    - materializeSendRecords, after inserting a campaign's send_records, links each row to its attachment: resolve the attachment column, and for each row whose attachment cell (trimmed, case-insensitive) matches one of the campaign's attachments, stamp that attachment's send_record_id to the row's id.
    - An empty attachment cell links nothing (sends without an attachment); a non-empty cell with no matching attachment leaves the row un-linked (the confirm gate already blocked this pre-send — defense-in-depth, no crash).
  </behavior>
  <action>
    In lib/core/send.ts add `attachments?: { filename: string; path: string }[]` to the `sendMail` message object type on `MailTransport` and to `SendArgs`; in sendOne spread it onto the nodemailer call only when present (copy the `...(config.x !== undefined && { x })` additive idiom at send.ts:93-103) so the default transport call is unchanged when no attachments are passed. In lib/worker/materialize.ts, after the send_records are inserted for the campaign, resolve `attachmentColumn = set.attachment_column ?? detectAttachmentColumn(columns, rows)`; if present, load `listAttachmentsForCampaign(campaign.userId, campaign.id)` into a Map keyed by `name.trim().toLowerCase()`, then for each materialized row look up its email/id pairing and its attachment cell — when the cell is non-empty and matches an attachment, `db.update(attachments).set({ send_record_id }).where(eq(attachments.id, matched.id))` (link the attachment to the row's send_record id). Preserve the existing counter-reconcile transaction and onConflictDoNothing idempotency exactly (SEND-06). Do NOT introduce a new send_record status; attachment linkage is metadata on the attachments row, not a state change.
  </action>
  <verify>
    <automated>node --import tsx --test "lib/worker/process.test.ts"</automated>
  </verify>
  <done>send.ts carries an optional attachments field forwarded only when set; materialize links each matched row's send_record to its attachment; no-attachment send is unchanged; existing worker tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-row attachment resolution + presence check + graceful rejected-attachment-missing fail</name>
  <files>lib/worker/process.ts, lib/worker/process.test.ts</files>
  <read_first>
    - lib/worker/process.ts:156-251 (per-row loop: fenced 'sending' claim, sendOne call at 188-194, the two terminal fenced transactions at 202-251)
    - lib/worker/materialize.ts:36 (the "rejected: …" error-constant idiom to copy)
    - lib/worker/process.test.ts:26-124 (harness: temp DATABASE_PATH + CREDENTIAL_ENC_KEY, migrate, full FK seeding, stub transport recording sent[]/failOnSend), :78 (stub sendMail signature to extend with attachments)
  </read_first>
  <behavior>
    - For each claimed row, the worker resolves the row's attachment via getAttachmentForSendRecord(campaign.id, rec.id) (DB id → opaque storage_path — NEVER a CSV-provided path).
    - If a linked attachment's file is NOT present on disk (attachmentExists false), the row fails with `error: "rejected: attachment missing"` via the EXISTING fenced ('sending'→'failed') synchronous transaction (bumping failed_count) — sendOne is NOT called and nothing throws; the campaign continues to the next row.
    - If the attachment is present, sendOne is called with `attachments: [{ filename: original_name, path: resolveAttachmentPath(storage_path) }]`.
    - A row with no linked attachment sends exactly as today.
    - The stub transport's recorded sent[] can assert the attachment filename/path was passed for a matched row.
  </behavior>
  <action>
    Define `const ATTACHMENT_MISSING_ERROR = "rejected: attachment missing"` near the top of process.ts (copy the materialize INVALID_ADDRESS_ERROR idiom). In the per-row loop, AFTER the row is fenced to `status='sending'` and BEFORE sendOne: `const att = await getAttachmentForSendRecord(campaign.id, rec.id)`. If `att` and NOT `attachmentExists(att.storage_path)` → take the EXISTING failed branch (the fenced `db.transaction` at lines 228-251 that sets status='failed', bumps attempts + failed_count, fenced on `status='sending'`) but with `error: ATTACHMENT_MISSING_ERROR`, then `continue` — do NOT call sendOne, do NOT throw (poison-pill rule / T-06 lesson). If `att` present → call sendOne with `attachments: [{ filename: att.filename, path: resolveAttachmentPath(att.storage_path) }]` (resolve gives the traversal-checked absolute path). If no `att` → sendOne unchanged. Update the process.test.ts stub transport `sendMail` signature (line ~78) to accept+record `attachments`, and add cases: (a) a row linked to a real on-disk file → sent[] records the attachment filename/path and the row is `sent`; (b) a row linked to a DB attachment whose file was deleted → the row is `failed` with `rejected: attachment missing`, failed_count incremented, and the NEXT row still sends (campaign not aborted). Reuse the UPLOADS_PATH temp-dir idiom + a real written file for the happy case.
  </action>
  <verify>
    <automated>node --import tsx --test "lib/worker/process.test.ts"</automated>
  </verify>
  <done>The worker attaches the correct file per row resolved by DB id; a file missing on disk fails only that row (rejected: attachment missing) via the fenced transaction and the campaign continues; test proves both the happy attach and the graceful-miss paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB storage_path → filesystem | The worker resolves attachments ONLY from DB rows, never from a CSV-provided path |
| one bad row → the batch | A single missing/failed attachment must never abort the campaign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering (path traversal) | worker attachment resolve | mitigate | resolveAttachmentPath prefix-check on a DB-sourced opaque path; CSV cells never touch the filesystem (ATCH-03) |
| T-07-12 | Denial of Service (poison pill) | missing file at send time | mitigate | Per-row fenced failed-write + continue; never a throw that aborts the batch (Phase 6 lesson) |
| T-07-13 | Tampering (counter tear) | failed_count on attachment fail | mitigate | Terminal write + counter bump in ONE synchronous transaction fenced on status='sending' (CR-01) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (no install tasks) |
</threat_model>

<verification>
- `node --import tsx --test "lib/worker/process.test.ts"` green: happy-attach records the file; missing-on-disk fails one row and continues; no-attachment send unchanged.
</verification>

<success_criteria>
The worker attaches the correct per-recipient file resolved safely by DB id, and a missing file fails only that row without aborting the campaign. ATCH-01 (send-time attach) and the send-time half of ATCH-02/ATCH-03 are met.
</success_criteria>

<output>
Create `.planning/phases/07-per-row-attachments/07-04-SUMMARY.md` when done.
</output>
