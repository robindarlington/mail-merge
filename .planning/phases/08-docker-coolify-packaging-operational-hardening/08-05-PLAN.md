---
phase: 08-docker-coolify-packaging-operational-hardening
plan: 05
type: execute
wave: 3
depends_on: [08-04]
files_modified:
  - .planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md
requirements: [SC-2, SC-5]
autonomous: false
must_haves:
  truths:
    - "Coolify env/secrets are wired: CREDENTIAL_ENC_KEY, DATABASE_PATH=/data/app.db, Clerk keys present; HOSTNAME=0.0.0.0"
    - "The Coolify per-app Stop Grace Period is set (≥300s) if the VPS is ≥ v4.1.0; otherwise the crash-safe-resume residual is documented"
    - "The Phase 8 slice is deployed to the standing staging URL and both containers are healthy with migrations applied"
    - "A real send interrupted by a staging redeploy resumes with all data intact and no recipient double-sent"
    - "WAL checkpoint + orphan sweep are observed logging on staging (counts only)"
  artifacts:
    - path: ".planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md"
      provides: "recorded staging deploy: env/secret confirmation, grace-period setting (or residual), redeploy no-double-send evidence, maintenance-routine log evidence"
      min_lines: 25
  key_links:
    - from: "Coolify UI (env + Stop Grace Period)"
      to: "running containers"
      via: "compose ${VAR} interpolation + docker stop --time"
      pattern: "CREDENTIAL_ENC_KEY"
    - from: "staging redeploy mid-send"
      to: "resume with no double-send + data survival"
      via: "shared /data volume + phase-6 crash-safe resume"
      pattern: "no double-send"
---

<objective>
Ship and verify the Phase 8 slice on the real Coolify VPS (phase success criteria #2 + #5): confirm the env/secret wiring, set the Coolify Stop Grace Period, redeploy to the standing staging URL, and prove on the platform what the local acceptance test proved on the mechanism — an interrupted send resumes with data intact and no double-send. This is the final, QUEUED human checkpoint for the phase.

Purpose: The grace-period behavior, secret injection, and volume persistence only fully prove out on the real two-container VPS topology under a real Coolify redeploy. The local acceptance test (08-04) proves the mechanism; staging proves the deployed system.
Output: `08-STAGING-NOTES.md` recording the deploy, env confirmation, grace setting (or documented residual), the redeploy no-double-send verification, and maintenance-routine log evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

The full system is packaged for the Coolify VPS so data survives redeploys and an in-flight send resumes cleanly with no duplicates.

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-CONTEXT.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-RESEARCH.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-02-SUMMARY.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-04-SUMMARY.md
@.planning/phases/07-per-row-attachments/07-06-PLAN.md

<notes>
- This mirrors the established staging-deploy idiom (06-07 / 07-06). Autonomy: QUEUED human checkpoint per the operator's standing preference (autonomy-dont-halt) — later work need not block on it, but Phase 8 is not shipped until it passes.
- RESEARCH Finding 1 + Open Question 1: verify the Coolify version. If ≥ v4.1.0, set Advanced → Operations → Stop Grace Period to ~300s (and confirm the setting surfaces for the compose build-pack app). If the VPS is older / can't upgrade, fall back to the LOCKED decision: rely on crash-safe resume and DOCUMENT the SIGKILL residual (still correct — at most one already-committed send_record, zero duplicates). Do not block the phase on the upgrade.
- Secrets: confirm each is SET in Coolify (never print values). Base64 secrets marked "Is Literal?" (RESEARCH Finding 5). Remember a NEXT_PUBLIC_* change needs a REBUILD, not a restart (Pitfall 9).
- Backups note (Pitfall 8): any manual `app.db` copy must checkpoint first or copy all three files (app.db, -wal, -shm) — note in the staging doc (automated backups are deferred).
- Use a throwaway BYO test SMTP reachable from the VPS; inject via Coolify secrets. Never commit or print credentials.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm env/secret wiring + grace period, then redeploy the slice to staging</name>
  <files>.planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md</files>
  <read_first>
    - .planning/phases/07-per-row-attachments/07-06-PLAN.md (the staging deploy + notes idiom)
    - 08-02-SUMMARY.md (the env contract) and 08-04-SUMMARY.md (what the acceptance test proved)
    - RESEARCH.md Finding 1 (Coolify version + grace UI) and Finding 5 (secret injection)
  </read_first>
  <action>
    Confirm in Coolify (record, never print values) that these are SET: `CREDENTIAL_ENC_KEY`, `CLERK_SECRET_KEY`, the `NEXT_PUBLIC_CLERK_*` build vars, `DATABASE_PATH=/data/app.db`, `UPLOADS_PATH=/data/uploads`, `HOSTNAME=0.0.0.0`, and the maintenance tunables (or note defaults apply). Confirm base64 secrets are marked "Is Literal?". Check the Coolify version: if ≥ v4.1.0, set the app's Stop Grace Period to ~300s and confirm it appears for this compose app; else record the crash-safe-resume residual per the locked fallback. Push the current branch and trigger a Coolify redeploy of the standing staging environment. After deploy: confirm both web + worker containers are healthy, migrations applied, the worker logged its readiness line, and (over the next observation window or by lowering the cadence briefly) the WAL checkpoint + orphan sweep log their count-only lines. Record staging URL, deployed commit SHA, both container health states, env/secret confirmation, the grace-period setting (or residual), and the maintenance-log evidence in 08-STAGING-NOTES.md. Never print a secret value.
  </action>
  <verify>
    <automated>test -f ".planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md" && grep -qi "CREDENTIAL_ENC_KEY" ".planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md" && grep -qi "grace" ".planning/phases/08-docker-coolify-packaging-operational-hardening/08-STAGING-NOTES.md"</automated>
  </verify>
  <done>The slice is deployed to staging with confirmed env/secrets, the grace-period setting (or documented residual), healthy containers, applied migrations, and observed maintenance logs — all recorded with no secret values.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full system packaged for Coolify: one hardened image (bundled worker, non-root, ABI-pinned better-sqlite3), a compose with init:true + raised stop grace, WAL-checkpoint + orphan-sweep routines, and the env/secret wiring — deployed to the standing staging URL.</what-built>
  <how-to-verify>
    1. Sign in on the staging URL with a verified throwaway BYO test SMTP config, a saved template, and a CSV of 10–15 rows. Start a real send.
    2. REDEPLOY MID-SEND: while the batch is running (some rows sent, some pending), trigger a Coolify redeploy (or a stop→start) of the app. CONFIRM after it comes back: the campaign RESUMES automatically and completes; the test inbox shows each recipient received EXACTLY ONE email (no duplicate); and the campaign history/progress reflects the full run with correct sent/failed counts.
    3. DATA SURVIVAL: confirm all pre-redeploy data is intact after the redeploy — the campaign, its send_records, your saved template/list/SMTP config all persist (nothing reset by the container replacement).
    4. MAINTENANCE ROUTINES: confirm the worker logs show the WAL checkpoint ({busy,log,checkpointed}) and the orphan sweep ({deletedRows,deletedFiles,unlinkFailures}) firing as count-only lines (you may temporarily lower WAL_CHECKPOINT_MS/ORPHAN_SWEEP_MS to observe within the session).
    5. GRACE / RESIDUAL: confirm the worker logged "worker stopping — draining in-flight tick" on the redeploy stop (graceful path) if Coolify ≥ v4.1.0 with the grace set; otherwise confirm the crash-safe resume still produced no duplicate (documented residual).
    Record pass/fail for each step (2–5) in 08-STAGING-NOTES.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public staging URL → app | Real (test) user sends over a real BYO SMTP on the VPS topology |
| Coolify secrets → containers | Secrets injected at runtime, plaintext on VPS at /data/coolify/.../.env (documented residual) |
| Coolify redeploy → running send | A redeploy mid-batch must not double-send or lose data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering | redeploy mid-send double-sends a recipient | mitigate | Step 2 confirms exactly-once delivery across a real Coolify redeploy (phase-6 crash-safe resume + grace window) |
| T-08-15 | Information Disclosure | secret printed into notes or logs | mitigate | Record only that each secret is SET; never print values; base64 marked "Is Literal?" |
| T-08-16 | Denial of Service | SIGKILL residual on pre-v4.1.0 Coolify | accept | Locked fallback: crash-safe resume guarantees no duplicate; residual documented in staging notes |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package changes in this plan |
</threat_model>

<verification>
- Both staging containers healthy; migrations applied; env/secrets confirmed (values never printed).
- Redeploy mid-send resumes with exactly-once delivery and full data survival — recorded in 08-STAGING-NOTES.md.
- WAL checkpoint + orphan sweep observed logging on staging; grace setting or residual documented.
</verification>

<success_criteria>
The Phase 8 slice is deployed to the standing Coolify staging URL and works there, with env/secrets wired and a redeploy proving no-double-send data survival — phase success criteria #2 + #5 (and #3 on the real platform).
</success_criteria>

<output>
Create `.planning/phases/08-docker-coolify-packaging-operational-hardening/08-05-SUMMARY.md` when done.
</output>
