---
phase: 08-docker-coolify-packaging-operational-hardening
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - scripts/redeploy-acceptance.sh
  - scripts/acceptance-harness.ts
  - scripts/stub-smtp.ts
requirements: [SC-3]
autonomous: true
must_haves:
  truths:
    - "The scripted acceptance test builds the real image, starts compose, seeds a queued campaign, begins a send, interrupts it with `docker compose stop` (NOT restart), then `up -d`"
    - "After resume: all seeded data survived (campaign + send_records rows persist on the /data volume)"
    - "No recipient was double-sent: the stub SMTP recorded each RCPT exactly once and each (campaign_id,to_addr) has exactly one terminal send_record"
    - "A `docker kill` crash variant also resumes with no duplicate (crash-safe resume proven distinctly from graceful stop)"
    - "The script clearly marks which assertions are local-only vs what can only be proven on Coolify staging"
  artifacts:
    - path: "scripts/redeploy-acceptance.sh"
      provides: "compose-driven redeploy + crash acceptance harness using stop/up (never bare restart)"
      contains: "compose stop"
    - path: "scripts/stub-smtp.ts"
      provides: "local smtp-server sink recording every RCPT TO (dedup detector), accepts all auth"
      contains: "smtp-server"
    - path: "scripts/acceptance-harness.ts"
      provides: "seeds an encrypted smtp_config→stub + recipient_set + template + queued campaign; asserts row counts + no-duplicate"
      min_lines: 60
  key_links:
    - from: "worker (in container)"
      to: "stub SMTP (on host)"
      via: "smtp_config host=host.docker.internal; RCPT recorded per recipient"
      pattern: "host.docker.internal"
    - from: "interrupt (compose stop / docker kill)"
      to: "resume with no double-send"
      via: "phase-6 crash-safe resume over the shared /data volume"
      pattern: "send_records"
---

<objective>
Prove phase success criterion #3 locally and repeatably: a send interrupted by a redeploy resumes cleanly with all data intact and NO recipient double-sent. Script it against local compose using the correct interrupt semantics (`stop`+`up`, never bare `restart`), plus a `docker kill` crash variant, with a stub SMTP that detects any duplicate delivery.

Purpose: The phase-6 crash-safe resume (interrupted rows never double-send) is the correctness guarantee this whole phase protects. This plan turns it into an executable acceptance test that runs on the REAL hardened image + compose (plans 01–03), so a regression is caught before the staging checkpoint.
Output: A stub SMTP sink, a seed/assert harness, and the orchestrating acceptance script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

The full system is packaged for the Coolify VPS so data survives redeploys and an in-flight send resumes cleanly with no duplicates.

<context>
@.planning/PROJECT.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-CONTEXT.md
@.planning/phases/08-docker-coolify-packaging-operational-hardening/08-RESEARCH.md
@lib/worker/recover.test.ts
@lib/db/client.ts

<interfaces>
<!-- Seed helpers (already exist — see recover.test.ts imports): -->
  @/lib/data/campaigns  → createDraftCampaign(...)
  @/lib/data/recipients → createRecipientSet(...)
  @/lib/data/templates  → createTemplate(...)
  @/lib/data/smtp       → createSmtpConfig(...)   // encrypts with CREDENTIAL_ENC_KEY
  @/lib/crypto          → encrypt(...)
  @/lib/db              → db, connection, campaigns, send_records
campaigns.status transitions: draft → queued (the worker claims 'queued').
send_records UNIQUE(campaign_id, to_addr); terminal statuses 'sent'|'failed'.

smtp-server (devDependency, ^3.19.2) — construct an SMTPServer that accepts all
AUTH and records `session.envelope.rcptTo` addresses to a JSON file with timestamps.
</interfaces>

<notes>
- RESEARCH Pitfall 1 is the crux: `docker compose restart` ALWAYS uses a 10s timeout and ignores stop_grace_period — it tests the SIGKILL path unintentionally. The graceful assertion MUST use `docker compose stop` (honors the 5m worker grace) then `docker compose up -d`. Use `docker kill` SEPARATELY as the crash-path assertion.
- The worker sends over BYO SMTP from an encrypted smtp_config. Seed a config whose host is `host.docker.internal` (Docker Desktop 28.1.1, confirmed) and port = the stub's port, secure=false. The stub accepts all auth and logs every RCPT so duplicates are detectable.
- Seed via the harness run on the HOST against the same /data DB the containers use (set DATABASE_PATH to the mounted volume path, or exec the harness inside the web container). Prefer running the harness inside the web container (`docker compose exec`) so it uses the exact in-container DB + crypto key. Choose one approach and document it in the script header.
- Give the send enough rows and a large enough SEND_DELAY_MS that the interrupt reliably lands mid-batch (e.g. 10–15 recipients at SEND_DELAY_MS=1500), so some rows are sent pre-interrupt and the rest resume after.
- Assertions after resume: (1) campaign + send_records rows still present (data survived the volume across stop/up); (2) COUNT of terminal send_records == recipient count with each (campaign_id,to_addr) unique; (3) stub RCPT log has each recipient AT MOST once (no duplicate delivery). Fail loud with a nonzero exit on any violation.
- Mark clearly (comments + a printed banner) that the Coolify Stop-Grace-Period UI setting and the real VPS redeploy can only be proven at the plan 08-05 staging checkpoint — this local script proves the mechanism, not the platform.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub SMTP sink + seed/assert harness</name>
  <files>scripts/stub-smtp.ts, scripts/acceptance-harness.ts</files>
  <read_first>
    - lib/worker/recover.test.ts (how to seed smtp_config/recipient_set/template/campaign via lib/data + crypto)
    - lib/db/schema.ts (send_records unique constraint + statuses)
  </read_first>
  <action>
    `scripts/stub-smtp.ts`: start an `smtp-server` SMTPServer on a configurable port (default 2525), accept all AUTH, and append every `session.envelope.rcptTo[].address` (with a timestamp) to a JSON file path from argv/env. Expose a mode to print/scan the file for duplicates. `scripts/acceptance-harness.ts`: a small CLI with subcommands — `seed` (create an encrypted smtp_config pointing at host.docker.internal:2525, a recipient_set of N distinct addresses, a template, and a campaign transitioned to `queued`; print the campaign id) and `assert` (given the campaign id and the stub RCPT log path: verify data survived, terminal send_record count == N, every (campaign_id,to_addr) unique, and each recipient appears at most once in the RCPT log; exit nonzero on any failure). Reuse the existing lib/data + lib/crypto seed paths — do not hand-roll inserts.
  </action>
  <verify>
    <automated>node --import tsx -e "import('node:fs').then(f=>{for(const p of ['scripts/stub-smtp.ts','scripts/acceptance-harness.ts'])if(!f.existsSync(p))throw new Error('missing '+p)})" && node --import tsx --check scripts/stub-smtp.ts && node --import tsx --check scripts/acceptance-harness.ts</automated>
  </verify>
  <done>The stub SMTP records every RCPT to a file and the harness can seed a queued campaign and assert survival + no-duplicate given the log.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrating redeploy-acceptance.sh (stop/up + docker kill) and run it green</name>
  <files>scripts/redeploy-acceptance.sh</files>
  <read_first>
    - docker-compose.yml (service names, /data volume, worker command/grace)
    - scripts/stub-smtp.ts + scripts/acceptance-harness.ts (Task 1)
    - RESEARCH.md Pitfall 1 (restart vs stop)
  </read_first>
  <action>
    Write `scripts/redeploy-acceptance.sh` (bash, `set -euo pipefail`, executable) that: (1) starts the host stub SMTP (background, RCPT log to a temp file); (2) `docker compose build && docker compose up -d` on the hardened image; (3) seeds a queued campaign via the harness (`docker compose exec` into web, or host-run against the mounted DB — match the header decision); (4) waits until the worker has sent SOME but not all rows (poll send_records), then GRACEFUL interrupt with `docker compose stop` (NOT restart) followed by `docker compose up -d`; (5) waits for the campaign to complete, then runs the harness `assert` (data survived + unique terminal rows + no duplicate RCPT); (6) repeats a CRASH variant using `docker kill` on the worker mid-send and re-asserts no-duplicate resume; (7) prints a clear banner listing what was proven locally and what remains staging-only (Coolify UI grace period + real VPS redeploy). Any assertion failure exits nonzero. Tear down compose + stub at the end. Run the script and confirm it passes.
  </action>
  <verify>
    <automated>test -x scripts/redeploy-acceptance.sh && grep -q "compose stop" scripts/redeploy-acceptance.sh && grep -q "docker kill" scripts/redeploy-acceptance.sh && ! grep -Eq "compose +restart" scripts/redeploy-acceptance.sh && bash scripts/redeploy-acceptance.sh</automated>
  </verify>
  <done>The acceptance script runs against the real image, interrupts a live send with stop+up (and a docker-kill crash), and asserts data survived with zero double-sends — exiting nonzero on any violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interrupt event → in-flight send | A stop/kill mid-batch must never cause a resend of an already-delivered row |
| worker container → host stub SMTP | Test-only transport; records deliveries to detect duplicates |
| /data volume → redeploy | Data (DB + WAL) must survive container replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | test uses `compose restart` and silently tests the SIGKILL path | mitigate | script forbids bare restart (grep gate); graceful path uses stop+up, crash path uses docker kill explicitly |
| T-08-12 | Repudiation | duplicate send goes undetected | mitigate | stub SMTP records every RCPT; assert each recipient appears at most once + unique terminal send_records |
| T-08-13 | Information Disclosure | real SMTP creds used in the acceptance test | mitigate | stub SMTP accepts all auth; seed uses a throwaway encrypted config, no real credentials |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | smtp-server already a devDependency (^3.19.2); no new install |
</threat_model>

<verification>
- `scripts/redeploy-acceptance.sh` exits 0: graceful stop+up AND docker-kill crash both resume with data intact and no double-send.
- Grep confirms stop/kill usage and the absence of bare `compose restart`.
- Banner documents the staging-only residual (Coolify UI grace + VPS redeploy).
</verification>

<success_criteria>
A repeatable local acceptance test proving redeploy data-survival + no-double-send on the real hardened image — phase success criterion #3 (mechanism proven; platform proven at 08-05).
</success_criteria>

<output>
Create `.planning/phases/08-docker-coolify-packaging-operational-hardening/08-04-SUMMARY.md` when done.
</output>
