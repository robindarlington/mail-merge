---
phase: 03-csv-upload-parsing-recipient-mapping
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: false
requirements: [CSV-01, CSV-05]
user_setup:
  - service: coolify
    why: "Success criterion 5 — the phase slice (CSV upload → parse → save recipient set) must be deployed to the standing staging URL on the VPS and work there."
    env_vars:
      - name: UPLOADS_PATH
        source: "Coolify -> app -> Environment (RUNTIME; set to /data/uploads so uploads land on the shared named volume, not the ephemeral container FS)"
    dashboard_config:
      - task: "Redeploy the standing staging app from this repo, add UPLOADS_PATH=/data/uploads to the app environment, and confirm the /data volume is a LOCAL host volume."
        location: "Coolify dashboard -> Applications"

must_haves:
  truths:
    - "docker-compose.yml injects UPLOADS_PATH=/data/uploads into the web service so uploaded CSVs persist on the shared /data named volume (survive container restart), never on the ephemeral container filesystem"
    - ".env.example documents UPLOADS_PATH with a dev-default note (./data/uploads) so local dev and prod resolve the same way as DATABASE_PATH"
    - "On the staging URL a signed-in user can upload a CSV, confirm the email column, and save a recipient set that appears in their list"
    - "storage_path values remain relative (<uuid>.csv) so they resolve correctly against UPLOADS_PATH in the container (Pitfall 4)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "UPLOADS_PATH runtime env on web, pointing at the shared /data volume"
      contains: "UPLOADS_PATH"
    - path: ".env.example"
      provides: "documented UPLOADS_PATH placeholder"
      contains: "UPLOADS_PATH"
  key_links:
    - from: "docker-compose.yml web.environment UPLOADS_PATH=/data/uploads"
      to: "the existing /data named volume mount"
      via: "uploads dir lives under the same shared volume as app.db"
      pattern: "UPLOADS_PATH"
    - from: "lib/csv/storage.ts UPLOADS_DIR"
      to: "process.env.UPLOADS_PATH"
      via: "env-configured resolver (03-01) reads this at runtime"
      pattern: "UPLOADS_PATH"
---

<objective>
Deploy the Phase-3 slice to the standing Coolify staging URL (roadmap success criterion 5). The only code change is small and precise: uploaded CSV bytes must land on the shared `/data` named volume (so they survive container restarts and are reachable by the Phase-6 worker), which means the web service needs `UPLOADS_PATH=/data/uploads` at runtime — mirroring how `DATABASE_PATH=/data/app.db` already points the SQLite file at the same volume. The bulk of this plan is the human redeploy + staging smoke test of the upload→confirm→save flow on the user's VPS.

Purpose: prove the CSV upload / parse / recipient-mapping slice works on the non-localhost staging URL (CSV-01 upload accepted, CSV-05 recipient set persisted on the durable volume).
Output: `docker-compose.yml` UPLOADS_PATH runtime env, `.env.example` documentation, and a working staging deploy.
</objective>

## Phase Goal

**As a** signed-in user, **I want to** upload a CSV and confirm which column holds the email addresses, **so that** I get a validated recipient set my campaigns can draw merge fields from.

## Acceptance Criteria (this plan)
- [ ] `docker-compose.yml` `web.environment` contains `UPLOADS_PATH=/data/uploads`.
- [ ] `.env.example` documents `UPLOADS_PATH` with the dev-default note.
- [ ] `docker compose config` parses without error.
- [ ] On the staging URL, an upload → confirm-column → save round-trip persists a recipient set that survives a container restart.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-csv-upload-parsing-recipient-mapping/03-RESEARCH.md
@.planning/phases/03-csv-upload-parsing-recipient-mapping/03-PATTERNS.md
@.planning/phases/03-csv-upload-parsing-recipient-mapping/03-04-SUMMARY.md

<interfaces>
<!-- Existing infra to edit in place; the /data volume already exists from Phase 1/2. -->
- `docker-compose.yml` `web.environment` already injects `${CREDENTIAL_ENC_KEY}`, `${CLERK_SECRET_KEY}`, `DATABASE_PATH=/data/app.db`, `HOSTNAME=0.0.0.0`, and mounts the named `/data` volume. Add `UPLOADS_PATH=/data/uploads` to the SAME environment block — the directory is created on demand by `lib/csv/storage.ts`'s `mkdirSync`, so no new volume or mount is needed, only the env var pointing under the existing `/data` mount.
- `.env.example` already documents `DATABASE_PATH` (dev default `./data/app.db`) and `CREDENTIAL_ENC_KEY`; add `UPLOADS_PATH` beside it with the same dev-default convention (`./data/uploads`).
- `lib/csv/storage.ts` (03-01) resolves `process.env.UPLOADS_PATH ?? "./data/uploads"` — prod sets it explicitly, dev falls back. `storage_path` is stored RELATIVE (`<uuid>.csv`) so it resolves correctly regardless of the absolute dir (03-RESEARCH.md Pitfall 4).
- The `/data` volume must be a LOCAL host volume (WAL requirement carried from Phase 1/2 — never NFS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UPLOADS_PATH runtime env in compose + .env.example</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    - docker-compose.yml (current web.environment block with DATABASE_PATH=/data/app.db + the /data volume mount — add UPLOADS_PATH beside DATABASE_PATH; preserve the SKELETON header comments)
    - .env.example (current DATABASE_PATH / CREDENTIAL_ENC_KEY entries + dev-default convention to mirror)
    - lib/csv/storage.ts (the UPLOADS_PATH resolver + dev fallback — from 03-01; confirm the var name matches exactly)
    - .planning/phases/03-csv-upload-parsing-recipient-mapping/03-RESEARCH.md (Pattern 3 storage dir; Pitfall 4 relative storage_path portability; Environment Availability — prod requires the Coolify named volume)
  </read_first>
  <action>
    In `docker-compose.yml`, add `UPLOADS_PATH: /data/uploads` (or `- UPLOADS_PATH=/data/uploads` matching the existing list style) to the `web` service `environment` block, directly beside `DATABASE_PATH=/data/app.db`, so uploaded CSVs land under the already-mounted `/data` named volume rather than the ephemeral container filesystem. Do NOT add a new volume or mount — the existing `/data` mount covers it and `lib/csv/storage.ts` creates `/data/uploads` on demand via `mkdirSync`. Keep the compose file valid and preserve the SKELETON header comments (full operational hardening remains Phase 8). In `.env.example`, add an `UPLOADS_PATH` entry beside `DATABASE_PATH` with a comment noting the dev default is `./data/uploads` and prod (Coolify) sets `/data/uploads` — mirroring the DATABASE_PATH documentation convention. Do not commit any real secret.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docker-compose.yml','utf8');if(!/UPLOADS_PATH.*\/data\/uploads/.test(c))throw new Error('UPLOADS_PATH missing from compose');const e=fs.readFileSync('.env.example','utf8');if(!/UPLOADS_PATH/.test(e))throw new Error('UPLOADS_PATH missing from .env.example');console.log('uploads env ok')" && docker compose config >/dev/null && echo compose-valid</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` `web.environment` contains `UPLOADS_PATH` set to `/data/uploads`.
    - `.env.example` contains an `UPLOADS_PATH` entry with the dev-default note.
    - No new volume/mount was added (the existing `/data` mount is reused).
    - `docker compose config` parses without error.
  </acceptance_criteria>
  <done>The web container writes uploaded CSVs to the durable shared /data volume; the var is documented for local dev.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Redeploy to Coolify staging with UPLOADS_PATH</name>
  <what-built>The compose UPLOADS_PATH wiring is done. The remaining work is on your VPS: redeploy the standing staging app, add the UPLOADS_PATH runtime env, and confirm the volume. Coolify is your infrastructure and its UI has no scriptable path here.</what-built>
  <how-to-verify>
    1. In Coolify, open the standing staging application (the one from Phase 2's 02-07).
    2. Add to RUNTIME environment: UPLOADS_PATH=/data/uploads (alongside the existing DATABASE_PATH=/data/app.db, CREDENTIAL_ENC_KEY, CLERK_SECRET_KEY, HOSTNAME=0.0.0.0).
    3. Confirm the /data volume is a LOCAL host volume (WAL + file-persistence requirement — never NFS).
    4. Redeploy from the latest commit of this branch.
    5. If the build fails, paste the error; otherwise confirm the app starts.
  </how-to-verify>
  <resume-signal>Type "deployed" with the staging URL once the app builds and starts on Coolify, or paste the build/runtime error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the CSV-upload slice on the staging URL</name>
  <what-built>The Phase-3 slice (upload → parse → confirm email column → save recipient set) running on the standing staging URL.</what-built>
  <how-to-verify>
    1. Sign in on the staging URL and open the "Recipients" nav item.
    2. On a fresh account, confirm the empty-state "Upload your first recipient list" callout.
    3. Upload a real CSV (with a header row + an email column). Confirm the parse summary shows the correct row/column counts, the detected email column, and the invalid-or-all-valid line.
    4. Override the email column to a different column and confirm the invalid count re-computes; set it back and save. Confirm the success toast and the saved set in the list (newest first).
    5. Upload a non-CSV and an oversized file — confirm the blocking errors render (not silent failures).
    6. Restart the app/container in Coolify, reload /recipients, and confirm the saved set is STILL listed (proves the upload + row persisted on the durable /data volume, not the ephemeral FS).
    7. Confirm no CSV cell value appears in server logs.
  </how-to-verify>
  <resume-signal>Type "approved" once upload → confirm → save works on staging and the saved set survives a restart, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/Coolify env → running container | UPLOADS_PATH must arrive at runtime and point at the durable volume, not the ephemeral FS |
| public staging URL → upload action | The staging deployment is internet-reachable; the upload action's zod guards + auth() (03-03) are the enforcement layer |
| uploaded CSV bytes → shared /data volume | User CSV content persists on disk under opaque uuid names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-PERSIST | Denial of service / data loss | UPLOADS_PATH → /data volume | mitigate | UPLOADS_PATH points at the LOCAL host `/data` named volume (verified in the deploy checkpoint) so uploads and the recipient_sets rows survive restarts together; a wrong/ephemeral path would silently lose files — checked in the restart step. |
| T-3-DOS | Denial of service | public upload surface on staging | mitigate | The 03-01/03-03 zod caps (MAX_UPLOAD_BYTES, MAX_ROWS) and the 4 MB bodySizeLimit are already deployed with the slice; the staging smoke exercises the reject path. |
| T-3-STAGING | Information disclosure | public staging URL (Clerk dev instance) | accept | Carried from Phase 2 D-13: staging runs the Clerk dev instance with test data; no real user data at risk. CSV lifecycle cleanup / retention hardening is a later-phase concern (03-RESEARCH.md A6). |
</threat_model>

<verification>
- `docker compose config` parses; `docker-compose.yml` + `.env.example` contain UPLOADS_PATH (grep-asserted).
- Human deploy checkpoint: app builds + starts on Coolify with UPLOADS_PATH set.
- Human staging smoke: upload → confirm → save works on the staging URL AND the saved set survives a container restart (success criterion 5 + durable-volume proof).
</verification>

<success_criteria>
Success criterion 5 satisfied: the Phase-3 slice is deployed to the standing Coolify staging URL and works there — a signed-in user uploads a CSV, confirms the email column, and saves a recipient set that persists on the durable /data volume across restarts, with uploads written to the shared volume via UPLOADS_PATH.
</success_criteria>

<output>
Create `.planning/phases/03-csv-upload-parsing-recipient-mapping/03-05-SUMMARY.md` when done. Record the staging URL and confirm the restart-persistence check passed.
</output>
