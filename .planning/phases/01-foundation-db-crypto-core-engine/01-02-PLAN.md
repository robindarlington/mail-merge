---
phase: 01-foundation-db-crypto-core-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/db/schema.ts
  - lib/db/client.ts
  - lib/db/index.ts
  - lib/db/schema.test.ts
autonomous: true
requirements:
  - "AUTH-02 (foundation: every table carries owner userId for tenant scoping)"
  - "SMTP-04 (foundation: smtp_configs holds encrypted creds + explicit secure)"
  - "SEND-06 (foundation: send_records per-recipient state machine table)"
must_haves:
  truths:
    - "lib/db is the ONLY module that opens SQLite (D-04)"
    - "The single DB client sets journal_mode=WAL, busy_timeout=5000, synchronous=NORMAL, foreign_keys=ON in exactly one place (D-04)"
    - "The FULL v1 schema exists now: smtp_configs, recipient_sets, campaigns, send_records, templates, attachments (D-05)"
    - "Every tenant-owned table carries an owner userId column (D-05 / AUTH-02)"
    - "smtp_configs stores encrypted credential columns + an explicit secure boolean (D-05 / SMTP-04, fixes secure-from-port anti-pattern)"
    - "send_records encodes the per-recipient state machine pending→sending→sent|failed with error + timestamps (D-05 / SEND-06)"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "full v1 Drizzle schema — all 6 entities with userId scoping"
      contains: "send_records"
      min_lines: 60
    - path: "lib/db/client.ts"
      provides: "the single SQLite opener with all four mandatory pragmas"
      contains: "journal_mode"
    - path: "lib/db/index.ts"
      provides: "barrel export of db client + schema for web and worker"
  key_links:
    - from: "lib/db/client.ts"
      to: "better-sqlite3 connection"
      via: "PRAGMA statements on open"
      pattern: "busy_timeout = 5000"
    - from: "lib/db/schema.ts"
      to: "drizzle.config.ts"
      via: "drizzle-kit reads this schema path to generate migrations"
      pattern: "(sqliteTable|export)"
---

<objective>
Author the full v1 Drizzle schema (D-05) and the single WAL-configured SQLite client (D-04) that is the ONLY module in the codebase permitted to open the database. This is the structural mechanism behind Phase 1 success criterion #1 (no SQLITE_BUSY): because both the web app and the worker import this one client, they inherit identical pragmas.

Purpose: Every later phase (auth scoping, SMTP onboarding, CSV sets, campaigns, the send-record state machine, attachments) reads/writes through these typed tables. Defining the whole schema now (D-06) avoids reshaping later; tables unused until their phase are intentional.
Output: `lib/db/schema.ts` (6 entities, userId-scoped), `lib/db/client.ts` (4 pragmas), `lib/db/index.ts` barrel, and a schema-shape unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The authoritative entity model is ARCHITECTURE.md "Entities (SQLite data model)".
     Reproduce that model in Drizzle. Exact column names/types/indexes are the executor's
     call within this entity set (CONTEXT.md D-05 note), but these columns are REQUIRED: -->

smtp_configs:    id, userId (Clerk owner), host, port, secure (boolean — NOT inferred from port),
                 username, password_enc, password_iv, password_tag (AES-256-GCM parts; never plaintext),
                 from_addr, from_name, verified_at, created_at
recipient_sets:  id, userId, filename, columns_json, row_count, storage_path, created_at
templates:       id, userId, subject (may contain {{fields}}), body, created_at
campaigns:       id, userId, recipient_set_id, template_id, smtp_config_id, status
                 (draft|queued|running|completed|failed), worker_id, lease_expires_at,
                 total, sent_count, failed_count, created_at, started_at, finished_at
send_records:    id, campaign_id, to_addr, merged_subject, merged_body,
                 status (pending|sending|sent|failed), message_id, error, attempts, sent_at
                 + UNIQUE(campaign_id, to_addr)  -- idempotent materialization guard (SEND-06)
attachments:     id, campaign_id, send_record_id, filename, storage_path, created_at

Mandatory pragmas (ARCHITECTURE.md Pattern 1 / STACK.md / PITFALLS #5), set once on open:
  journal_mode = WAL
  busy_timeout = 5000
  synchronous = NORMAL
  foreign_keys = ON

DATABASE_PATH env var resolves the file location (fallback ./data/app.db for native dev — D-09).
drizzle.config.ts (from plan 01-01) already points at lib/db/schema.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the full v1 Drizzle schema (all 6 entities, userId-scoped)</name>
  <files>lib/db/schema.ts, lib/db/schema.test.ts</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md ("Entities (SQLite data model)" — the authoritative column list; "State Management (campaign lifecycle)")
    - .planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md (D-05 full schema, D-06 evolve-via-migrations, secure boolean)
    - .planning/research/PITFALLS.md (#1/#2 credential columns must be ciphertext+iv+authTag, never plaintext; #3 explicit secure; #13 owner userId on every tenant row)
    - lib/db/schema.ts (the file being created)
  </read_first>
  <behavior>
    - Test: importing the schema module exposes exactly the six table objects smtp_configs, recipient_sets, templates, campaigns, send_records, attachments.
    - Test: every tenant-owned table (smtp_configs, recipient_sets, templates, campaigns) exposes a userId column (multi-tenant scoping, AUTH-02).
    - Test: smtp_configs exposes a `secure` boolean column AND the three credential parts (password_enc, password_iv, password_tag) and exposes NO plaintext-password column.
    - Test: send_records exposes a `status` column and a unique constraint over (campaign_id, to_addr) (SEND-06 idempotent materialization).
    - Test: send_records exposes message_id, error, attempts, sent_at (per-recipient audit, SEND-03/04).
  </behavior>
  <action>
    Create `lib/db/schema.ts` using drizzle-orm `sqliteTable` definitions for all six entities exactly as enumerated in the <interfaces> block (which mirrors ARCHITECTURE.md). Use INTEGER timestamps (unixepoch) per the research model. Encode `status` columns as text with the documented state values. Make `secure` an integer-backed boolean (NOT inferred from port — fixes the CLI anti-pattern, SMTP-04/PITFALLS #3). Store SMTP credentials ONLY as the encrypted triple (password_enc + password_iv + password_tag); do NOT add any plaintext password column (PITFALLS #1/#2). Add `userId` (text, Clerk id) to every tenant-owned table (campaigns, smtp_configs, recipient_sets, templates) for AUTH-02 scoping; send_records and attachments inherit tenancy through campaign_id FK. Add the UNIQUE(campaign_id, to_addr) constraint on send_records (SEND-06). Add foreign-key references between campaigns→recipient_sets/templates/smtp_configs and send_records→campaigns and attachments→campaigns/send_records. Export drizzle `InferSelectModel`/`InferInsertModel` types for each table so downstream phases get typed rows. Write `lib/db/schema.test.ts` asserting the behaviors above by introspecting the exported table objects' column keys and the unique constraint. Do NOT open a real DB connection in this test — assert on the schema definition objects only.
  </action>
  <verify>
    <automated>npx --no-install tsc --noEmit && node --import tsx --test lib/db/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/schema.ts exports all six tables; `grep -c "sqliteTable" lib/db/schema.ts` returns 6.
    - smtp_configs has columns password_enc, password_iv, password_tag and `secure`, and has NO column whose name contains plaintext `password` without the `_enc`/`_iv`/`_tag` suffix.
    - send_records defines a unique index/constraint over (campaign_id, to_addr).
    - schema.test.ts passes (all behaviors green) under `node --import tsx --test`.
    - `npx tsc --noEmit` reports zero errors.
  </acceptance_criteria>
  <done>The full v1 schema (D-05) exists with userId scoping (AUTH-02), encrypted-only credential columns + explicit secure (SMTP-04), and the send_records state-machine table with idempotency constraint (SEND-06); schema-shape tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write the single WAL-configured SQLite client (the only DB opener)</name>
  <files>lib/db/client.ts, lib/db/index.ts</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Pattern 1 "critical config (set once, in packages/db/client.ts)" — the exact four pragmas; Anti-Pattern 1 "two processes each opening SQLite their own way")
    - .planning/research/STACK.md ("Web + worker sharing ONE SQLite file" — mandatory pragmas, busy_timeout rationale, same-host/no-NFS)
    - .planning/research/PITFALLS.md (#5 database-is-locked)
    - lib/db/schema.ts (created in Task 1 — the client wraps this schema with drizzle)
    - lib/db/client.ts (the file being created)
  </read_first>
  <action>
    Create `lib/db/client.ts` as the SINGLE place that constructs a `better-sqlite3` Database (D-04). Resolve the file path from `process.env.DATABASE_PATH` with a `./data/app.db` fallback (D-09 native dev); ensure the parent directory exists before opening. Immediately after opening, execute the four mandatory pragmas in this exact form: `journal_mode = WAL`, `busy_timeout = 5000`, `synchronous = NORMAL`, `foreign_keys = ON`. Wrap the connection with drizzle (`drizzle(betterSqlite, { schema })`) and export the typed `db` instance plus the raw connection (the migration runner in 01-05 and a future worker may need the raw handle). Guard against opening more than one connection per process (module-level singleton) so importing `db` from many call sites reuses one handle. Add a brief file-top comment stating this is the only module permitted to open SQLite (enforces D-04). Create `lib/db/index.ts` re-exporting `db`, the raw connection, and `* from './schema'` so web and worker import everything from `@/lib/db`.
  </action>
  <verify>
    <automated>grep -v '^//' lib/db/client.ts | grep -q "journal_mode = WAL" && grep -v '^//' lib/db/client.ts | grep -q "busy_timeout = 5000" && grep -v '^//' lib/db/client.ts | grep -q "synchronous = NORMAL" && grep -v '^//' lib/db/client.ts | grep -q "foreign_keys = ON" && test -f lib/db/index.ts && npx --no-install tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/client.ts contains all four pragma strings (verified ignoring comment lines): `journal_mode = WAL`, `busy_timeout = 5000`, `synchronous = NORMAL`, `foreign_keys = ON`.
    - client.ts is the only file under lib/ that imports better-sqlite3's Database constructor (D-04): `grep -rl "better-sqlite3" lib/ | grep -v client.ts` returns nothing.
    - lib/db/index.ts re-exports db + schema.
    - `npx tsc --noEmit` reports zero errors.
  </acceptance_criteria>
  <done>A single drizzle/better-sqlite3 client sets all four mandatory pragmas in one place and is the only SQLite opener (D-04); it is the structural guarantee behind the no-SQLITE_BUSY criterion, exercised under real concurrency in plan 01-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app ↔ SQLite file | All tenant data + encrypted creds persist here; access is via lib/db only |
| tenant ↔ tenant | userId column is the isolation mechanism every later query filters on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02a | Information Disclosure | smtp_configs credential columns | mitigate | Schema stores ONLY password_enc/_iv/_tag (AES-256-GCM parts) — no plaintext password column exists; test asserts absence (PITFALLS #1). |
| T-01-02b | Elevation of Privilege | cross-tenant data access | mitigate | Every tenant-owned table carries userId (AUTH-02); child tables scope via campaign_id FK. Enforcement of the filter is Phase 2's job; the column foundation is laid here (PITFALLS #13). |
| T-01-02c | Tampering | concurrent writers corrupting DB | mitigate | Single client sets WAL + busy_timeout=5000 + foreign_keys=ON in one place (D-04); prevents SQLITE_BUSY/lock corruption (PITFALLS #5). Verified under concurrency in 01-05. |
| T-01-02d | Repudiation | no per-recipient audit | mitigate | send_records carries status/error/message_id/sent_at — durable per-recipient audit trail (SEND-03/04). |
</threat_model>

<verification>
- `grep -c sqliteTable lib/db/schema.ts` == 6 (all entities present).
- All four pragma strings present in client.ts (comment-filtered grep).
- No lib/ file other than client.ts imports better-sqlite3 (D-04 single-opener).
- schema.test.ts green; `tsc --noEmit` clean.
</verification>

<success_criteria>
Full v1 schema with userId scoping, encrypted-only credentials + explicit secure boolean, and the send_records state machine exists (D-05/D-06); a single WAL'd client with all four pragmas is the sole SQLite opener (D-04), satisfying the structural half of success criterion #1.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-db-crypto-core-engine/01-02-SUMMARY.md` when done.
</output>
