---
phase: 01-foundation-db-crypto-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - .nvmrc
  - .env.example
  - .gitignore
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - postcss.config.mjs
  - components.json
  - lib/utils.ts
  - drizzle.config.ts
autonomous: true
requirements:
  - "(infrastructure scaffold — underpins AUTH-02, SMTP-04, SEND-06)"
must_haves:
  truths:
    - "Repo is ONE Next.js app with a single package.json (D-01) — no workspaces/monorepo"
    - "next dev boots the App Router app on a local SQLite file path (D-09)"
    - "All required env vars (incl. CREDENTIAL_ENC_KEY, DATABASE_PATH) are documented in a committed .env.example"
    - "Node version is pinned via engines + .nvmrc so better-sqlite3 native bindings/tsx work"
    - "next.config.ts sets output: 'standalone' (D-08)"
    - "package.json scripts run the worker via tsx in dev; the prod worker.js is bundled via esbuild/tsup in the Dockerfile (D-07)"
  artifacts:
    - path: "package.json"
      provides: "single-app manifest with pinned deps + scripts (dev, worker, db:generate, db:migrate, build)"
      contains: "\"workspaces\""  # NOTE: must be ABSENT — see verify (D-01)
    - path: ".env.example"
      provides: "committed enumeration of all required env vars"
      contains: "CREDENTIAL_ENC_KEY"
    - path: ".nvmrc"
      provides: "pinned Node version for native bindings"
    - path: "next.config.ts"
      provides: "standalone output build config"
      contains: "standalone"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at lib/db/schema.ts and the SQLite DATABASE_PATH"
  key_links:
    - from: "package.json scripts"
      to: "worker/index.ts and lib/db/schema.ts"
      via: "tsx worker, drizzle-kit generate/migrate scripts"
      pattern: "drizzle-kit (generate|migrate)"
---

<objective>
Scaffold the single Next.js 16 application that is the shared home for the web app, the worker, and the `lib/` modules (db, crypto, core). This plan creates the project shell ONLY — no business logic. It establishes the structural decisions D-01 (single app, not a workspace), D-08 (standalone output), D-09 (native dev), and the Claude-discretion items: committed `.env.example`, Node pin, and the npm scripts (`dev`, `worker`, `db:generate`, `db:migrate`, `build`) that downstream plans rely on.

Purpose: Every later plan in this phase (lib/db, lib/crypto, lib/core, migration) imports from this scaffold's installed dependencies and runs its npm scripts. This is the Wave 1 foundation.
Output: A booting Next.js App Router app, pinned dependency set, `.env.example`, `.nvmrc`/engines, `next.config.ts` (standalone), `drizzle.config.ts`, and shadcn/ui init.

NOTE (density): the two tasks each touch many files (inherent to a Next.js + shadcn scaffold; no meaningful split exists). Verify EACH acceptance criterion individually rather than assuming the scaffold "just worked" — the automated gates per task are the proof, not the absence of errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Existing repo state the executor must reconcile against (do NOT blow these away blindly). -->
Existing package.json is the OLD CLI manifest:
  name "mail-merge", type "module", deps nodemailer@^6, @types/nodemailer.
  Scripts dry/test/send invoke send-credentials.ts. KEEP send-credentials.ts on disk (lib/core lifts from it in plan 01-04) but it is no longer the app entrypoint.

Existing .gitignore currently contains only:
  node_modules/
  .env

Pinned versions to install (from STACK.md — do NOT drift):
  next@^16.2  react@^19.2  react-dom@^19.2  @clerk/nextjs@^7.5
  drizzle-orm@^0.45  better-sqlite3@^12.11  plainjob
  nodemailer@^9  papaparse@^5.5  zod@^4.4  p-queue@^9.3  pino@^10.3
  devDeps: typescript@~5.9  drizzle-kit@^0.31  @types/node@^24
           @types/better-sqlite3@^7.6  @types/papaparse@^5  @types/nodemailer@^6.4
           tailwindcss@^4.3  tsx@^4.22
  Node: 24.x LTS (matches CLI host 24.9.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the single Next.js app, pin deps, and write project config</name>
  <read_first>
    - package.json (existing CLI manifest — to be replaced/extended, NOT a fresh init that wipes send-credentials.ts)
    - .gitignore (extend, do not truncate)
    - .planning/research/STACK.md (pinned versions, "Installation" block, "What NOT to Use" — no TS 6.0, no serverless patterns)
    - .planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md (D-01 single app, D-08 standalone, D-09 native dev)
  </read_first>
  <action>
    Convert this directory into a single Next.js 16 App Router + TypeScript + Tailwind 4 application WITHOUT introducing any workspace/monorepo config (D-01: no `workspaces` key, no pnpm-workspace.yaml, no apps/ or packages/ dirs — shared code goes in `lib/`). Update package.json: keep `"name": "mail-merge"`, `"type": "module"`, `"private": true`; add `"engines": { "node": ">=24" }`; install the pinned dependency set listed in the <interfaces> block at exactly those version ranges from STACK.md. Remove the obsolete `nodemailer@^6` dependency line and bump to `nodemailer@^9` (API is unchanged per STACK.md). Add npm scripts: `dev` ("next dev"), `build` ("next build"), `start` ("next start"), `worker` ("tsx worker/index.ts"), `db:generate` ("drizzle-kit generate"), `db:migrate` ("tsx scripts/migrate.ts" — the migrate runner is authored in plan 01-05; the script entry exists now). Preserve the old CLI scripts under renamed keys `cli:dry`/`cli:test`/`cli:send` so `send-credentials.ts` (the lift source for plan 01-04) still runs. Create `.nvmrc` containing the pinned Node major (24). Create `next.config.ts` with `output: 'standalone'` (D-08). Create `tsconfig.json` targeting the Next.js App Router defaults with `~5.9`-compatible settings and a `@/*` path alias to the repo root. Extend `.gitignore` to also ignore: `/.next/`, `/data/` (the local SQLite + WAL/SHM sidecars + uploads — D-09 native dev DB lives here), `*.db`, `*.db-wal`, `*.db-shm`, and `next-env.d.ts`. Do NOT delete the existing `node_modules/` or `.env` entries.
  </action>
  <verify>
    <automated>test -f next.config.ts && grep -q "standalone" next.config.ts && test -f .nvmrc && node -e "const p=require('./package.json'); if(p.workspaces) process.exit(1); for(const s of ['dev','worker','db:generate','db:migrate','build']) if(!p.scripts[s]) {console.error('missing script '+s); process.exit(1);} if(!p.engines||!p.engines.node) process.exit(1); if(!/\\^9/.test(p.dependencies.nodemailer||'')) process.exit(1);" && grep -q "data/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - package.json has NO `workspaces` key (D-01) and HAS scripts dev, worker, db:generate, db:migrate, build (verified by the node -e exit code).
    - package.json `engines.node` is set and `dependencies.nodemailer` is `^9`.
    - next.config.ts contains the string `standalone` (D-08).
    - .nvmrc exists; .gitignore ignores `data/`, `*.db*`, `.next`.
    - send-credentials.ts still exists on disk (not deleted by the init).
  </acceptance_criteria>
  <done>The repo is a single (non-workspace) Next.js app with pinned deps, standalone config, Node pin, and the five required npm scripts; the old CLI file is preserved for the lib/core lift.</done>
</task>

<task type="auto">
  <name>Task 2: Write .env.example, drizzle.config.ts, init shadcn/ui, and a booting app shell</name>
  <read_first>
    - .env (existing CLI env — names only: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, FROM_ADDR, FROM_NAME; NEVER copy values into .env.example)
    - .planning/codebase/CONCERNS.md ("No .env.example committed" — the gap this task closes)
    - .planning/research/STACK.md (Tailwind 4 CSS-first config, shadcn init, DATABASE_PATH=/data/app.db, HOSTNAME=0.0.0.0, CREDENTIAL_ENC_KEY)
    - .planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md (Claude's-discretion: CREDENTIAL_ENC_KEY 32-byte; committed .env.example)
  </read_first>
  <action>
    Create a committed `.env.example` enumerating EVERY required env var with placeholder (never real) values and a one-line comment each: `DATABASE_PATH` (default `./data/app.db` for native dev per D-09), `CREDENTIAL_ENC_KEY` (32-byte key, document "generate with: openssl rand -base64 32" and that the app fails loudly if absent/wrong length), the Clerk publishable + secret key names (consumed in Phase 2, listed now), and the BYO-SMTP onboarding-test vars carried from the CLI if any dev seed needs them. Do NOT write actual secret values. Create `drizzle.config.ts` for drizzle-kit: dialect sqlite, schema path `./lib/db/schema.ts` (authored in plan 01-02), out dir `./drizzle`, and dbCredentials reading `DATABASE_PATH` (fallback `./data/app.db`). Run shadcn/ui init (`npx shadcn@latest init`) to scaffold `components.json`, `lib/utils.ts`, Tailwind 4 CSS-first config, and `app/globals.css`. Create a minimal App Router shell: `app/layout.tsx` (root layout importing globals.css) and `app/page.tsx` (a static placeholder "Mail Merge — foundation" page; NO auth, NO data fetching — this phase ships no user-facing features per CONTEXT.md scope). Ensure `next build` / `next dev` compiles with zero type errors.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "CREDENTIAL_ENC_KEY" .env.example && grep -q "DATABASE_PATH" .env.example && test -f drizzle.config.ts && grep -q "lib/db/schema.ts" drizzle.config.ts && test -f app/layout.tsx && test -f app/page.tsx && npx --no-install tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - .env.example exists, is committed-safe (placeholders only), and contains both `CREDENTIAL_ENC_KEY` and `DATABASE_PATH`.
    - drizzle.config.ts points its schema at `./lib/db/schema.ts` and out at `./drizzle`.
    - components.json + lib/utils.ts exist (shadcn init succeeded).
    - `npx tsc --noEmit` reports zero errors (app shell type-checks).
    - app/page.tsx renders a static placeholder with no auth/data calls.
  </acceptance_criteria>
  <done>The app boots, type-checks, has a committed env template covering all required vars (CONCERNS.md gap closed), and drizzle-kit is configured to read the schema that plan 01-02 will create.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git history | Committed config/env files must never carry real secrets |
| install → supply chain | npm packages pulled at pinned versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | mitigate | File holds placeholder values only; verify gate greps for var NAMES, never values; real `.env` stays gitignored. CREDENTIAL_ENC_KEY documented as runtime-injected, never committed (PITFALLS #1). |
| T-01-02 | Tampering | npm/drizzle-kit/next/better-sqlite3 installs | mitigate | Versions pinned to STACK.md ranges (audited list); package-lock.json committed. See T-01-SC. |
| T-01-SC | Tampering | npm installs (supply chain) | mitigate | STACK.md Package Legitimacy: all packages are well-known, registry-verified mainstream libs (next, react, drizzle-orm, better-sqlite3, nodemailer, papaparse, zod, p-queue, pino, plainjob). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking-human gate required this plan. |
</threat_model>

<verification>
- `next build` completes (standalone output) with no type errors.
- `node -e` gate confirms no `workspaces` key and all five scripts present.
- `.env.example` greps for CREDENTIAL_ENC_KEY + DATABASE_PATH; no real secret values committed.
</verification>

<success_criteria>
Single non-workspace Next.js 16 app boots and type-checks; deps pinned per STACK.md; `.env.example`, `.nvmrc`, `next.config.ts` (standalone), and `drizzle.config.ts` committed; npm scripts (dev/worker/db:generate/db:migrate/build) present for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-db-crypto-core-engine/01-01-SUMMARY.md` when done.
</output>
