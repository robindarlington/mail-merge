---
phase: 01-foundation-db-crypto-core-engine
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/migrate.ts
  - scripts/concurrency-smoke.ts
  - drizzle/
  - docker-compose.yml
  - Dockerfile
  - worker/index.ts
autonomous: true
requirements:
  - "AUTH-02 / SMTP-04 / SEND-06 (foundation: migrated tables on disk make the schema real)"
must_haves:
  truths:
    - "drizzle-kit generate produces migration SQL from lib/db/schema.ts, and db:migrate applies it to create the SQLite tables ON DISK (not just a passing type-check)"
    - "After migration, app.db contains all six tables: smtp_configs, recipient_sets, campaigns, send_records, templates, attachments"
    - "Two processes (a web-like reader + a worker-like writer) open the same WAL'd DB concurrently and complete reads+writes with NO SQLITE_BUSY error (success criterion #1, proven not just structural)"
    - "A docker-compose.yml skeleton declares web + worker services from one image sharing a named volume mounted at /data (success criterion #4)"
  artifacts:
    - path: "scripts/migrate.ts"
      provides: "db:migrate runner that applies drizzle migrations via the lib/db client"
      contains: "migrate"
    - path: "scripts/concurrency-smoke.ts"
      provides: "two-process concurrency probe asserting no SQLITE_BUSY"
    - path: "docker-compose.yml"
      provides: "web + worker skeleton sharing a named /data volume (D-10)"
      contains: "/data"
    - path: "worker/index.ts"
      provides: "minimal worker entrypoint (D-02) — opens lib/db, logs readiness, no send logic yet"
  key_links:
    - from: "scripts/migrate.ts"
      to: "drizzle/ migration files + lib/db client"
      via: "drizzle-orm better-sqlite3 migrate()"
      pattern: "migrate"
    - from: "docker-compose.yml"
      to: "named volume at /data"
      via: "both web and worker services mount the same volume"
      pattern: "/data"
---

<objective>
Make the schema REAL and PROVE the concurrency guarantee. This Wave 3 plan: (1) [BLOCKING] generates Drizzle migrations from `lib/db/schema.ts` and applies them so the six tables actually exist on disk (a build/type-check alone passes without a migrated DB — a false-positive this task closes); (2) runs a two-process concurrency smoke test that exercises the WAL'd client under simultaneous read+write to prove success criterion #1 (no SQLITE_BUSY) empirically, not just structurally; (3) creates the Docker Compose SKELETON (web + worker, one image, shared named `/data` volume) for success criterion #4 (D-10); and (4) adds the minimal `worker/index.ts` entrypoint (D-02) that the `worker` npm script and the compose worker service run.

Purpose: This plan turns the three foundation modules into a verified, runnable system and satisfies the two remaining phase success criteria (#1 empirically, #4 as a skeleton). Full production packaging/hardening is explicitly Phase 8 (D-10) — keep the compose file a skeleton.
Output: `scripts/migrate.ts`, applied `drizzle/` migrations, `scripts/concurrency-smoke.ts`, `worker/index.ts`, `docker-compose.yml`, and a minimal `Dockerfile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- From plan 01-02 (depends_on): lib/db exports `db` (drizzle), the raw better-sqlite3 connection,
     and the schema. DATABASE_PATH resolves the file (./data/app.db fallback). Client sets WAL +
     busy_timeout=5000 + synchronous=NORMAL + foreign_keys=ON.
   From plan 01-01: drizzle.config.ts (dialect sqlite, schema ./lib/db/schema.ts, out ./drizzle);
     npm scripts db:generate ("drizzle-kit generate") and db:migrate ("tsx scripts/migrate.ts");
     worker script ("tsx worker/index.ts"); next.config.ts output:'standalone'.

   Compose skeleton (STACK.md "Packaging" + ARCHITECTURE.md "Docker/Coolify packaging", D-10):
     two services (web: node server.js; worker: node worker.js) from ONE image, BOTH mounting the
     SAME named volume at /data (holds app.db + -wal/-shm + uploads). Set HOSTNAME=0.0.0.0 on web.
     Pass CREDENTIAL_ENC_KEY + DATABASE_PATH=/data/app.db as env. SKELETON ONLY — no stop_grace_period
     tuning, no SIGTERM handling, no multi-stage hardening (all Phase 8 per deferred list).
   WAL requires same host / local volume — NOT NFS (PITFALLS #5/#9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Generate + apply Drizzle migrations so the six tables exist on disk</name>
  <files>scripts/migrate.ts, drizzle/</files>
  <read_first>
    - lib/db/schema.ts (plan 01-02 — the source drizzle-kit generates from)
    - lib/db/client.ts (plan 01-02 — the migrate runner reuses this single client/connection)
    - drizzle.config.ts (plan 01-01 — schema path + out dir)
    - .planning/research/STACK.md (drizzle-kit 0.31 generate/migrate; better-sqlite3 native build note)
    - .planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md (Claude's-discretion: db:migrate script, committed migration files; [BLOCKING] schema-migration requirement)
  </read_first>
  <action>
    Author `scripts/migrate.ts` as the `db:migrate` runner: import the raw better-sqlite3 connection from `lib/db` (reusing the single WAL'd client — D-04, do NOT open a second connection) and call drizzle-orm's better-sqlite3 `migrate(db, { migrationsFolder: './drizzle' })`. Run `npm run db:generate` (drizzle-kit generate) to produce the migration SQL from `lib/db/schema.ts` into `./drizzle/` and COMMIT those generated files (Claude's-discretion: migration files committed). Then run `npm run db:migrate` against a local `./data/app.db` (DATABASE_PATH) so the tables are physically created. This is a [BLOCKING] gate: do not proceed to Task 2/3 until the migration has actually created the tables on disk — a passing `tsc`/`next build` does NOT prove the DB exists. drizzle-kit generate/migrate are non-interactive (autonomous).
  </action>
  <verify>
    <automated>npm run db:generate && ls drizzle/*.sql >/dev/null 2>&1 && npm run db:migrate && node -e "const D=require('better-sqlite3'); const db=new D(process.env.DATABASE_PATH||'./data/app.db'); const t=db.prepare(\"SELECT name FROM sqlite_master WHERE type='table'\").all().map(r=>r.name); const need=['smtp_configs','recipient_sets','campaigns','send_records','templates','attachments']; const missing=need.filter(n=>!t.includes(n)); if(missing.length){console.error('MISSING TABLES:',missing.join(','));process.exit(1)} console.log('all 6 tables present')"</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/` contains at least one generated `.sql` migration (committed).
    - `npm run db:migrate` exits 0 and creates the SQLite file at DATABASE_PATH.
    - Querying sqlite_master shows ALL six tables: smtp_configs, recipient_sets, campaigns, send_records, templates, attachments (the node -e gate exits 0).
    - The migrate runner reuses the single lib/db client (no second Database opener — D-04).
  </acceptance_criteria>
  <done>The full v1 schema is physically migrated onto disk (six tables verified via sqlite_master), closing the false-positive gap where type-checks pass without a real DB. [BLOCKING] gate satisfied before concurrency/compose work.</done>
</task>

<task type="auto">
  <name>Task 2: Worker entrypoint + two-process concurrency smoke test (prove no SQLITE_BUSY)</name>
  <files>worker/index.ts, scripts/concurrency-smoke.ts</files>
  <read_first>
    - lib/db/client.ts, lib/db/index.ts (plan 01-02 — the WAL'd client both processes open)
    - .planning/research/STACK.md ("Web + worker sharing ONE SQLite file" — many readers + one writer, busy_timeout makes contention wait not throw)
    - .planning/research/ARCHITECTURE.md (Pattern 1; success criterion #1 framing)
    - .planning/research/PITFALLS.md (#5 database-is-locked; "Looks Done But Isn't" SQLite-concurrency check)
    - worker/index.ts, scripts/concurrency-smoke.ts (the files being created)
  </read_first>
  <action>
    Create `worker/index.ts` (D-02 worker entrypoint): import `db` from `@/lib/db`, log a single structured "worker ready" line (pino or console — NEVER log secrets), and run a trivial poll/no-op loop or a one-shot readiness check (NO send/claim logic — that is Phase 6). This is the entrypoint the `worker` npm script and the compose worker service invoke. Create `scripts/concurrency-smoke.ts`: simulate the web+worker contention that causes SQLITE_BUSY — spawn or run two concurrent units against the SAME migrated `./data/app.db` through the shared lib/db client: a "writer" performing many short INSERT/UPDATE transactions on send_records (or a scratch row) in a loop, and a "reader" performing many concurrent SELECTs, overlapping in time. Assert the run completes with ZERO `SQLITE_BUSY`/"database is locked" errors (the busy_timeout=5000 + WAL config must make contended writes wait, not throw). Exit non-zero if any SQLITE_BUSY surfaces. This empirically proves success criterion #1 (the structural guarantee was set in 01-02; this is the proof).
  </action>
  <verify>
    <automated>npx --no-install tsc --noEmit && node --import tsx scripts/concurrency-smoke.ts && node --import tsx -e "import('./worker/index.ts').then(()=>{console.log('worker entrypoint imports ok')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/concurrency-smoke.ts runs a concurrent reader+writer against the migrated WAL'd DB and exits 0 with no SQLITE_BUSY (success criterion #1 proven empirically).
    - The smoke test asserts explicitly on the absence of SQLITE_BUSY / "database is locked" (fails non-zero if seen).
    - worker/index.ts imports lib/db, logs readiness, contains NO send/claim logic, and imports without error.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A two-process concurrency probe proves the WAL'd single-client design yields no SQLITE_BUSY under simultaneous read+write (success criterion #1), and the minimal worker entrypoint (D-02) exists for the worker script and compose service.</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose skeleton — web + worker sharing a named /data volume</name>
  <files>docker-compose.yml, Dockerfile</files>
  <read_first>
    - .planning/research/STACK.md ("Packaging for Coolify / Docker" — one image, two entrypoints, one shared /data volume, HOSTNAME=0.0.0.0, standalone output)
    - .planning/research/ARCHITECTURE.md ("Docker / Coolify packaging" — named volume mounted in both, same host, no NFS)
    - .planning/phases/01-foundation-db-crypto-core-engine/01-CONTEXT.md (D-07 tsx dev/esbuild prod, D-08 standalone, D-10 SKELETON only — full hardening is Phase 8)
    - next.config.ts (plan 01-01 — output:'standalone')
    - worker/index.ts (Task 2 — the worker service command target)
  </read_first>
  <action>
    Create a `docker-compose.yml` SKELETON declaring two services — `web` and `worker` — built from ONE image (single Dockerfile), BOTH mounting the SAME named volume (e.g. `appdata`) at `/data` (this shared mount is what makes web-enqueue / worker-dequeue over one SQLite file work — success criterion #4). Web service command runs the Next.js standalone server (node server.js) with `HOSTNAME=0.0.0.0`; worker service command runs the worker entrypoint. Pass `DATABASE_PATH=/data/app.db` and `CREDENTIAL_ENC_KEY` (from env, never inlined) to both. Create a minimal `Dockerfile` (single or simple multi-stage) that installs deps and builds — note in a comment that full hardening (Node-ABI pin for better-sqlite3, esbuild/tsup worker bundle per D-07, raised stop_grace_period, SIGTERM handling) is Phase 8 (D-10). Keep this a SKELETON: it must declare the correct service/volume topology and be valid compose, but is NOT expected to be a production-hardened build. Validate the compose file parses.
  </action>
  <verify>
    <automated>test -f docker-compose.yml && test -f Dockerfile && grep -q "/data" docker-compose.yml && grep -cq "worker" docker-compose.yml && grep -q "HOSTNAME" docker-compose.yml && (docker compose -f docker-compose.yml config >/dev/null 2>&1 || python3 -c "import yaml,sys; d=yaml.safe_load(open('docker-compose.yml')); svc=d.get('services',{}); assert 'web' in svc and 'worker' in svc, 'need web+worker'; mounts=str(d); assert '/data' in mounts, 'need /data mount'; print('compose topology ok')")</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml declares both `web` and `worker` services from one image.
    - Both services mount the same named volume at `/data` (grep + compose config/yaml parse confirm).
    - web sets HOSTNAME=0.0.0.0; both get DATABASE_PATH=/data/app.db and CREDENTIAL_ENC_KEY from env.
    - Compose file is valid (`docker compose config` succeeds, or the yaml-parse fallback asserts the topology).
    - A comment marks production hardening as deferred to Phase 8 (D-10).
  </acceptance_criteria>
  <done>A valid Docker Compose skeleton mounts a named /data volume shared by web + worker from one image (success criterion #4), with production hardening explicitly deferred to Phase 8 (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container ↔ /data volume | The shared volume holds app.db + WAL/SHM + (later) uploads; both containers mount the same local volume |
| env → containers | CREDENTIAL_ENC_KEY injected as env/secret, never baked into the image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05a | Tampering | concurrent web+worker writes → corruption/SQLITE_BUSY | mitigate | Concurrency smoke test empirically proves WAL+busy_timeout single-client config prevents SQLITE_BUSY (success criterion #1); fails the build if a lock error appears (PITFALLS #5). |
| T-01-05b | Information Disclosure | CREDENTIAL_ENC_KEY baked into image | mitigate | Key passed as runtime env/secret in compose, never inlined in Dockerfile/compose literals (PITFALLS #1); .env stays gitignored. |
| T-01-05c | Denial of Service | data loss on redeploy (volume not persisted) | accept (foundation) | Skeleton declares the named /data volume topology; full persistence/redeploy acceptance (WAL/SHM survival, stop_grace_period) is Phase 8 (D-10, PITFALLS #9) — deferred per ROADMAP. |
| T-01-05d | Tampering | npm/drizzle-kit/docker build supply chain | mitigate | Pinned deps from 01-01 (package-lock committed); drizzle-kit + better-sqlite3 are registry-verified mainstream packages (STACK.md). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking-human gate. |
</threat_model>

<verification>
- [BLOCKING] migration: sqlite_master shows all six tables on disk after `npm run db:migrate`.
- Concurrency smoke test exits 0 with no SQLITE_BUSY (success criterion #1, empirical).
- docker-compose.yml: web+worker services, shared /data volume, HOSTNAME set, valid compose (#4).
- `tsc --noEmit` clean; worker entrypoint imports cleanly.
</verification>

<success_criteria>
The schema is physically migrated (six tables on disk), a two-process probe proves no SQLITE_BUSY under concurrent read+write (criterion #1), the worker entrypoint exists (D-02), and a valid Docker Compose skeleton shares a named /data volume across web + worker (criterion #4) — with production hardening deferred to Phase 8 (D-10).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-db-crypto-core-engine/01-05-SUMMARY.md` when done.
</output>
