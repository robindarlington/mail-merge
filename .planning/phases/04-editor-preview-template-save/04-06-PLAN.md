---
phase: 04-editor-preview-template-save
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified: []
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-04, PREV-01, PREV-02, PREV-03]
user_setup:
  - service: coolify
    why: "Success criterion 5 — the Phase-4 slice (compose → autocomplete → preview → validation report → save template) must be deployed to the standing staging URL on the VPS and work there."
    dashboard_config:
      - task: "Redeploy the standing staging app from the latest commit on this branch. No new env vars are required this phase (the templates table already exists from the Phase-1 migration; UPLOADS_PATH=/data/uploads was wired in Phase 3). Confirm the deploy picks up the new /compose route."
        location: "Coolify dashboard -> Applications -> (staging app) -> Redeploy"
must_haves:
  truths:
    - "On the staging URL a signed-in user sees a 'Compose' nav item and can open /compose"
    - "On staging a user can pick a saved recipient list, compose a subject+body with {{-autocomplete, step the merged preview, read the validation report, and save a template — the same flow proven locally"
    - "The saved template persists on the shared /data SQLite volume (survives a container restart)"
  artifacts: []
  key_links:
    - from: "Coolify staging deploy"
      to: "/compose route + templates table on /data/app.db"
      via: "redeploy from latest commit; templates table already migrated (Phase 1), UPLOADS_PATH already set (Phase 3)"
      pattern: "compose"
---

## Phase Goal

**As a** signed-in user on the live staging environment, **I want to** compose, preview, and save a merge template end-to-end, **so that** the phase slice is proven to work off-localhost on the real VPS.

<objective>
Deploy the Phase-4 slice to the standing Coolify staging URL and smoke-test it there (roadmap success criterion 5). This phase adds NO new code to deploy config: the `templates` table was physically migrated to disk in Phase 1 (`drizzle/0000`), and `UPLOADS_PATH=/data/uploads` (which the preview's `readUpload` needs to read saved CSVs back) was wired into the web service in Phase 3 (03-05). So this plan is a human redeploy + a staging walkthrough of the compose → preview → save flow.

Purpose: prove EDIT-01/02/04 + PREV-01/02/03 work on the non-localhost staging URL and that a saved template persists on the durable `/data` volume.
Output: a working staging deploy + a confirmed staging walkthrough (no code changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-csv-upload-parsing-recipient-mapping/03-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-deploy sanity — confirm no new deploy config is needed</name>
  <files>docker-compose.yml (read-only), drizzle/*.sql (read-only)</files>
  <read_first>
    - docker-compose.yml (confirm web service already has UPLOADS_PATH=/data/uploads and DATABASE_PATH=/data/app.db on the shared volume)
    - drizzle/ (confirm the templates table is in the committed 0000 migration — no new migration this phase)
  </read_first>
  <action>
    Verify the deploy surface is unchanged: `docker compose config` parses; the web service environment already contains `UPLOADS_PATH=/data/uploads` and `DATABASE_PATH=/data/app.db`; the `templates` table exists in the committed drizzle migration (grep the migration SQL). Confirm `npm run build` produces a clean production build including the `/compose` route. If any of these is missing (e.g. a templates migration was somehow never generated), STOP and report — do not invent a migration in the deploy plan. This task changes no files; it is a gate before the human redeploy.
  </action>
  <verify>
    <automated>docker compose config >/dev/null && npm run build && grep -rl "templates" drizzle/*.sql</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0.
    - `grep -rl "CREATE TABLE .*templates" drizzle/*.sql` finds the templates table in a committed migration.
    - `grep -R "UPLOADS_PATH" docker-compose.yml` matches (already wired Phase 3).
    - `npm run build` exits 0 and the build output includes the `/compose` route.
  </acceptance_criteria>
  <done>Deploy config confirmed unchanged and complete; production build green with /compose; templates table already migrated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Redeploy to Coolify staging and walk through compose → preview → save</name>
  <what-built>The full Phase-4 compose slice (editor + {{ autocomplete + click-to-insert chips + live merged preview + per-row empty-value highlight + validation report + save template) is built and green locally. No new deploy env vars are required — templates table already on the /data volume (Phase 1), UPLOADS_PATH already set (Phase 3).</what-built>
  <how-to-verify>
    1. In the Coolify dashboard, open the standing staging app and Redeploy from the latest commit on this branch. Wait for the deploy to go healthy.
    2. Visit the staging URL and sign in with your Clerk user. Confirm a "Compose" item (pen icon) appears in the sidebar; open it.
    3. Pick a saved recipient list. In the subject and body, type `{{` and confirm the merge-field suggestion list appears and inserts a `{{column}}`; also click a merge-field chip and confirm it inserts at the caret.
    4. Step through the preview with Previous/Next: confirm each row renders the merged To/Subject/body against real CSV values (subject personalized too), and a row with a blank value shows the neutral "missing a value" note.
    5. Confirm the validation report shows the invalid-email count and missing-value aggregate (and, if you deliberately type a `{{typo}}`, the unknown-token warning at the top).
    6. Click "Save template" and confirm the "Template saved." toast.
    7. (Persistence) Trigger a redeploy/restart of the container, sign back in, and confirm the app still runs against the same /data volume (the templates table and your saved recipient lists/uploads are intact).
  </how-to-verify>
  <resume-signal>Type "approved" once the staging compose→preview→save flow works, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → staging app | The staging URL is reachable; Clerk auth gates all app routes (AUTH-03, established) |
| container FS → /data volume | Templates + uploaded CSVs must persist on the shared named volume, not ephemeral container FS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-PERSIST | Denial of Service (data loss) | /data volume | mitigate | templates on /data/app.db + uploads on /data/uploads (both already volume-mounted); persistence smoke-tested across a restart (Task 2 step 7) |
| T-4-AUTHZ | Elevation | staging routes | mitigate | Clerk middleware already gates /compose (inherited AUTH-03); verified by signing in on staging |
| T-4-SC | Tampering | deploy image | accept | no new npm dependencies added this phase; same image build path as Phases 2-3 |
</threat_model>

<verification>
- `docker compose config` + `npm run build` green pre-deploy.
- Human staging walkthrough of compose → autocomplete → preview → report → save.
- Persistence confirmed across a container restart.
</verification>

<success_criteria>
The Phase-4 slice is deployed to the standing Coolify staging URL and a signed-in user can compose, autocomplete, preview, read the validation report, and save a template there — with the template persisting on the durable /data volume (success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/04-editor-preview-template-save/04-06-SUMMARY.md` when done.
</output>
