---
phase: 04-editor-preview-template-save
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/data/templates.ts
  - lib/data/templates.test.ts
  - lib/data/index.ts
  - lib/compose/schema.ts
  - lib/compose/schema.test.ts
autonomous: true
requirements: [EDIT-01, EDIT-04]
must_haves:
  truths:
    - "createTemplate(userId, {subject, body}) persists a row whose user_id is the server-supplied userId"
    - "getTemplateForUser(USER_B, idOwnedByA) returns undefined (cross-tenant IDOR defense)"
    - "listTemplatesForUser returns only the caller's templates, newest first"
    - "composeFormSchema rejects a blank subject with 'Add a subject before saving.' and a blank body with 'Write a message before saving.'"
    - "composeFormSchema rejects a subject longer than 998 chars"
  artifacts:
    - path: "lib/data/templates.ts"
      provides: "userId-first templates DAL (create/get/list)"
      exports: ["createTemplate", "getTemplateForUser", "listTemplatesForUser", "PersistableTemplate"]
    - path: "lib/data/templates.test.ts"
      provides: "two-tenant IDOR + create/list coverage (EDIT-04)"
    - path: "lib/compose/schema.ts"
      provides: "shared subject/body zod schema (client resolver == server guard)"
      exports: ["composeFormSchema", "ComposeFormValues"]
  key_links:
    - from: "lib/data/index.ts"
      to: "lib/data/templates.ts"
      via: "barrel re-export block mirroring the recipients block"
      pattern: "createTemplate"
    - from: "lib/data/templates.ts getTemplateForUser"
      to: "and(eq(id), eq(userId))"
      via: "structural IDOR filter — never eq(id) alone"
      pattern: "and\\(eq\\(templates.id"
---

<objective>
Build the two persistence primitives behind "save a composed email as a template": a NEW userId-first `lib/data/templates.ts` DAL (a verbatim-shape copy of `lib/data/recipients.ts` against the already-existing `templates` table) and a NEW shared `lib/compose/schema.ts` (the single zod object parsed by BOTH the client RHF resolver and the server action). Both TDD.

Purpose: EDIT-04 (save subject+body as a template) needs a tenant-owned write path with the same structural IDOR defense as every other DAL, and EDIT-01 needs one shared validation contract so the editor and the server can never diverge on "subject/body required" + length caps.
Output: `lib/data/templates.ts` (+ test + barrel), `lib/compose/schema.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editor-preview-template-save/04-PATTERNS.md
@.planning/phases/04-editor-preview-template-save/04-UI-SPEC.md

<interfaces>
<!-- templates table already exists — DO NOT migrate/alter it. From lib/db/schema.ts: -->
export const templates = sqliteTable("templates", {
  id: integer("id").primaryKey({ autoIncrement: true }),
  userId: text("user_id").notNull(),
  subject: text("subject").notNull(),
  body: text("body").notNull(),
  created_at: integer("created_at").notNull().default(unixNow),
});
// import { templates, type NewTemplate } from "@/lib/db/schema";

<!-- Copy this exact shape from lib/data/recipients.ts (userId-first, server-injected ownership): -->
export type PersistableRecipientSet = Pick<NewRecipientSet, "filename" | ...>;  // OMITS userId
export function createRecipientSet(userId, values) { return db.insert(...).values({ userId, ...values }).returning(); }
export function getRecipientSetForUser(userId, id) { return db.query.recipient_sets.findFirst({ where: and(eq(id), eq(userId)) }); }
export function listRecipientSetsForUser(userId) { return db.query.recipient_sets.findMany({ where: eq(userId), orderBy: desc(created_at) }); }

<!-- Shared-zod idiom from lib/csv/schema.ts; zod ^4.4 top-level validators only (no chained zod-3 forms). -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: userId-first templates DAL with two-tenant IDOR tests (RED→GREEN)</name>
  <files>lib/data/templates.ts, lib/data/templates.test.ts, lib/data/index.ts</files>
  <read_first>
    - lib/data/recipients.ts (copy shape verbatim — Pick that omits userId, and(eq(id),eq(userId)) reads, desc(created_at) list)
    - lib/data/recipients.test.ts (copy the temp-DATABASE_PATH-before-import + migrate(before) + two-user IDOR harness verbatim)
    - lib/data/index.ts (barrel — add a templates block mirroring the recipients block)
    - lib/db/schema.ts (confirm templates columns: userId/subject/body/created_at — do NOT alter)
  </read_first>
  <behavior>
    - createTemplate(USER_A, {subject:'Hi {{name}}', body:'...'}) returns the inserted row; its user_id === USER_A (server-injected, not from values).
    - PersistableTemplate = Pick<NewTemplate, "subject" | "body"> — userId is NOT assignable through values.
    - listTemplatesForUser(USER_A) returns only USER_A rows, newest-first (desc created_at).
    - getTemplateForUser(USER_A, id) returns the owner's row.
    - getTemplateForUser(USER_B, idOwnedByA) returns undefined — the EDIT-04 IDOR assertion.
  </behavior>
  <action>
    Create lib/data/templates.ts importing `{ and, eq, desc } from 'drizzle-orm'`, `{ db } from '@/lib/db'`, `{ templates, type NewTemplate } from '@/lib/db/schema'`. Define `export type PersistableTemplate = Pick<NewTemplate, 'subject' | 'body'>;`. Implement `createTemplate(userId, values)` → `db.insert(templates).values({ userId, ...values }).returning()`; `listTemplatesForUser(userId)` → `db.query.templates.findMany({ where: eq(templates.userId, userId), orderBy: desc(templates.created_at) })`; `getTemplateForUser(userId, id)` → `db.query.templates.findFirst({ where: and(eq(templates.id, id), eq(templates.userId, userId)) })`. Write lib/data/templates.test.ts FIRST (RED), copying recipients.test.ts's temp-dir harness: set `process.env.DATABASE_PATH` to a mkdtempSync dir BEFORE the dynamic imports, `migrate(db, { migrationsFolder: './drizzle' })` in `before()`, `rmSync` in `after()`, two ids USER_A/USER_B. Extend lib/data/index.ts with an export block re-exporting createTemplate/getTemplateForUser/listTemplatesForUser + type PersistableTemplate.
  </action>
  <verify>
    <automated>node --import tsx --test lib/data/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test lib/data/templates.test.ts` exits 0 including the cross-tenant `getTemplateForUser(USER_B, idOwnedByA) === undefined` case.
    - `grep -n "Pick<NewTemplate" lib/data/templates.ts` matches (userId omitted from the persistable type).
    - `grep -cE "and\(eq\(templates.id" lib/data/templates.ts` returns 1 (single-row read is owner-filtered, never eq(id) alone).
    - `grep -n "createTemplate" lib/data/index.ts` matches.
  </acceptance_criteria>
  <done>templates DAL exports create/get/list + PersistableTemplate, IDOR test green, barrel updated, no schema migration added.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: shared compose zod schema (subject/body caps) (RED→GREEN)</name>
  <files>lib/compose/schema.ts, lib/compose/schema.test.ts</files>
  <read_first>
    - lib/csv/schema.ts (shared-schema idiom: one z.object, z.infer type, parsed on both client + server)
    - lib/csv/schema.test.ts (node:test over safeParse — copy structure)
    - lib/smtp/schema.ts (zod ^4.4 idiom header — top-level validators only, no chained zod-3 string forms)
    - .planning/phases/04-editor-preview-template-save/04-UI-SPEC.md (Copywriting Contract lines 136-137 — exact anchored messages)
  </read_first>
  <behavior>
    - composeFormSchema.safeParse({subject:'Hi', body:'Hello'}).success === true.
    - Blank/whitespace subject fails with message 'Add a subject before saving.'
    - Blank/whitespace body fails with message 'Write a message before saving.'
    - Subject of 999 chars fails (max 998, RFC 5322 line cap, A7); 998 passes.
    - ComposeFormValues = z.infer<typeof composeFormSchema>.
  </behavior>
  <action>
    Create lib/compose/schema.ts: `import { z } from 'zod';` then `export const composeFormSchema = z.object({ subject: z.string().trim().min(1, 'Add a subject before saving.').max(998, 'Keep the subject under 998 characters.'), body: z.string().trim().min(1, 'Write a message before saving.').max(50000, 'This message is too long.') });` and `export type ComposeFormValues = z.infer<typeof composeFormSchema>;`. Use sentence-case messages sourced verbatim from UI-SPEC for the min() cases. Write lib/compose/schema.test.ts FIRST (RED) covering each behavior bullet via `composeFormSchema.safeParse(...)`, asserting `.success` and, on failure, that `result.error.issues` contains the expected `message`. Do NOT create lib/compose/index.ts here (Plan 03 owns the barrel).
  </action>
  <verify>
    <automated>node --import tsx --test lib/compose/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test lib/compose/schema.test.ts` exits 0 across all behavior bullets.
    - `grep -n "Add a subject before saving." lib/compose/schema.ts` matches (exact UI-SPEC copy).
    - `grep -n "Write a message before saving." lib/compose/schema.ts` matches.
    - `grep -n "z.infer<typeof composeFormSchema>" lib/compose/schema.ts` matches.
  </acceptance_criteria>
  <done>composeFormSchema + ComposeFormValues exist with UI-SPEC-exact messages and length caps; schema test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller values → createTemplate | Caller must not be able to inject/spoof userId through the values object |
| client input → composeFormSchema | Untrusted subject/body length + emptiness validated before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-TAMPER-OWNER | Tampering | lib/data/templates.ts createTemplate | mitigate | PersistableTemplate Pick omits userId; DAL spreads `{ userId, ...values }` server-side (Task 1) |
| T-4-IDOR-TPL | Information Disclosure / Elevation | getTemplateForUser | mitigate | `and(eq(id), eq(userId))` only read path; cross-tenant get returns undefined (Task 1, tested) |
| T-4-SIZE | Denial of Service | composeFormSchema | mitigate | subject ≤ 998, body ≤ 50000 chars enforced by the shared schema on both client + server (Task 2) |
| T-4-SC | Tampering | npm installs | accept | zero new npm dependencies (drizzle + zod already installed) |
</threat_model>

<verification>
- `npm test` full suite green.
- No schema migration file added (`git status drizzle/` clean) — the templates table already exists.
- No new npm dependency (`git diff package.json` empty).
</verification>

<success_criteria>
The templates DAL and shared compose schema exist and are tested, giving Plan 03's saveTemplate action a tenant-safe write and a validation contract, and Plan 04's editor a resolver that matches the server exactly.
</success_criteria>

<output>
Create `.planning/phases/04-editor-preview-template-save/04-02-SUMMARY.md` when done.
</output>
