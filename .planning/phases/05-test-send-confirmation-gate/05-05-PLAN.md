---
phase: 05-test-send-confirmation-gate
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]
user_setup:
  - service: coolify
    why: "Roadmap success criterion 4 — the Phase-5 slice (whole-batch test-send + confirm gate) must be deployed to the standing staging URL on the VPS and work there, including a real test-send over the user's own SMTP."
    env_vars: []
    dashboard_config:
      - task: "Redeploy the standing staging app from the latest commit of this branch. No NEW env vars are required this phase — confirm the existing CREDENTIAL_ENC_KEY, DATABASE_PATH=/data/app.db, UPLOADS_PATH=/data/uploads, Clerk keys, and HOSTNAME=0.0.0.0 are still set."
        location: "Coolify dashboard -> Applications"
must_haves:
  truths:
    - "On the staging URL, a signed-in user with a saved template + verified SMTP can test-send the whole personalized batch to their own address and receive one personalized email per row (subject + body filled)"
    - "The confirmation modal on staging shows the correct recipient count, sender identity, a merged sample, and validation warnings, and cannot be dismissed by clicking outside or Escape"
    - "Confirming enqueues the campaign exactly once (its status becomes queued); a rapid double-confirm does not create a duplicate"
    - "The chunked test-send completes on staging without a reverse-proxy 504 (each request stays well under the Coolify/Traefik read timeout)"
  artifacts: []
  key_links:
    - from: "staging /compose Send card"
      to: "the user's own SMTP + inbox"
      via: "chunked test-send delivers real personalized mail end-to-end on the VPS"
      pattern: "n/a — manual staging verification"
---

<objective>
Deploy the Phase-5 slice to the standing Coolify staging URL and prove it works there end-to-end, including a REAL test-send over the user's own SMTP to their own inbox (roadmap success criterion 4). No code change ships in this plan — Phase 5 adds no new env var (it reuses `CREDENTIAL_ENC_KEY`, `DATABASE_PATH`, `UPLOADS_PATH`, and the Clerk keys already wired in Phases 2/3), and the chunked test-send was designed specifically so each request stays under the reverse-proxy read timeout. This plan is the human redeploy + staging walkthrough that closes the phase.

Purpose: automated tests deliberately never fire real email (injected fakes only); the one place a real personalized send is exercised is here, on staging, authorized by the user. This is also where the RESEARCH Pitfall-1 proxy-timeout concern is empirically retired — the client-driven chunks should complete with no 504.
Output: a working Phase-5 slice on the staging URL, verified by a real test-send + a confirm-gate walkthrough.
</objective>

## Phase Goal

**As a** signed-in user with a saved template, recipient list, and verified SMTP, **I want to** send my whole personalized batch to one test address and clear a hard confirmation gate before any live send, **so that** I can verify exactly what will go out and never fire a real send by accident.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-test-send-confirmation-gate/05-RESEARCH.md
@.planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md
@.planning/phases/05-test-send-confirmation-gate/05-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Redeploy the Phase-5 slice to Coolify staging</name>
  <what-built>Plans 01-04 shipped the campaigns DAL, the chunked test-send seam, the confirm/enqueue seams, and the /compose Send card + confirm modal. No new env var or compose change is needed — the test-send reuses the already-wired CREDENTIAL_ENC_KEY (to decrypt the saved SMTP password), DATABASE_PATH, and UPLOADS_PATH. The remaining work is on your VPS, which Coolify's UI owns.</what-built>
  <how-to-verify>
    1. In Coolify, open the standing staging application (from Phase 2's 02-07, kept current since).
    2. Confirm the existing RUNTIME env is intact: CREDENTIAL_ENC_KEY, DATABASE_PATH=/data/app.db, UPLOADS_PATH=/data/uploads, the Clerk keys, HOSTNAME=0.0.0.0. No NEW variable is required this phase.
    3. Redeploy from the latest commit of this branch.
    4. If the build fails, paste the error; otherwise confirm the app starts and the /compose page loads.
  </how-to-verify>
  <resume-signal>Type "deployed" with the staging URL once the app builds and starts, or paste the build/runtime error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real test-send + confirm-gate walkthrough on staging</name>
  <what-built>The whole-batch test-send and the hard confirmation gate are live on staging. This is the one authorized place a REAL personalized send fires — over your own SMTP, to your own inbox.</what-built>
  <how-to-verify>
    On the staging URL, signed in with a verified SMTP config and at least one uploaded recipient list (2+ rows with a name/merge column):
    1. Go to /compose, pick the recipient list, compose a subject AND body that both use a merge field (e.g. "Hi {{name}}" / "...{{name}}..."), and Save template.
    2. In the Send card, enter your own address in "Test address" and click "Send test batch". Confirm: progress advances (sent/total), it finishes with a success toast, and NO reverse-proxy 504 / hung tab occurs. Check your inbox: you received one email per row, each with the row's own name filled in BOTH the subject and the body (proves TEST-01 + the EDIT-03 subject fix on real transport).
    3. Click "Review and send". Confirm the modal shows the correct recipient count, your sender identity ("From: ..."), a merged sample for row 1, and any validation warnings; confirm it does NOT close when you click outside it or press Escape (undismissable gate).
    4. Click "Send to {N} recipients". Confirm a "queued" success toast, the modal closes, and the page shows a queued state (no live send yet — that is Phase 6).
    5. Re-open "Review and send" and confirm quickly twice (or double-click the confirm): confirm you get a benign "already queued" message and NOT a duplicate — the campaign transitions to queued only once.
    Report any 504, any missing personalization, any modal that dismisses on outside-click/Escape, or any duplicate enqueue.
  </how-to-verify>
  <resume-signal>Type "verified" if all five steps pass on staging, or describe what failed (with the step number and any error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging web container → user SMTP | The real test-send decrypts the stored password (CREDENTIAL_ENC_KEY) and sends over the user's own SMTP |
| reverse proxy → web container | Each chunked request must complete within the Coolify/Traefik read timeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-DOS | Denial of Service | chunked test-send vs proxy timeout | mitigate | Bounded per-request chunk size keeps each staging request short; step 2 empirically confirms no 504 |
| T-5-CRED | Information Disclosure | staging decrypt | mitigate | CREDENTIAL_ENC_KEY is a Coolify runtime secret; the decrypted password never leaves the container (verified by Plans 02/03 redaction gates) |
| T-5-DUPE | Tampering | staging double-confirm | mitigate | Step 5 exercises the atomic guard on the real DB — exactly one queued transition |
| T-5-SC | Tampering | deploy | mitigate | No new packages / no compose change this phase; redeploy of the same image |
</threat_model>

<verification>
- Coolify redeploy of the latest commit builds and starts (Task 1).
- Staging walkthrough (Task 2): real personalized test-send arrives per-row (subject+body filled), the confirm gate is undismissable and server-authoritative, one queued transition under double-confirm, no proxy 504.
</verification>

<success_criteria>
- The Phase-5 slice works on the standing staging URL (roadmap success criterion 4): a real whole-batch test-send delivers personalized mail, and the hard confirm gate enqueues exactly once — with no reverse-proxy timeout.
</success_criteria>

<output>
Create `.planning/phases/05-test-send-confirmation-gate/05-05-SUMMARY.md` when done.
</output>
