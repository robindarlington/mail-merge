---
phase: 05-test-send-confirmation-gate
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - components/campaign/test-send-panel.tsx
  - components/campaign/confirm-send-dialog.tsx
  - components/campaign/send-card.tsx
  - components/compose/compose-editor.tsx
  - app/(app)/compose/page.tsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "On /compose, once a template is saved AND an SMTP config exists, a Send card exposes a test-send panel and a 'Review and send' accent button"
    - "The test-send panel drives the whole batch to one address by looping the chunk action client-side, showing progress (sent/total) and a soft duration note for large sets — no hard cap"
    - "'Review and send' opens an undismissable confirmation modal showing the server-authoritative count, sender identity, a merged sample recipient, and validation warnings; the confirm button reads 'Send to {N} recipients' and disables in-flight"
    - "Confirming enqueues once (queued toast); a second confirm shows a benign 'already queued' toast, never a duplicate"
  artifacts:
    - path: "components/campaign/test-send-panel.tsx"
      provides: "Client test-send panel: address input + chunk-loop driver + progress + summary"
      contains: "sendTestBatchChunk"
    - path: "components/campaign/confirm-send-dialog.tsx"
      provides: "Undismissable confirm modal fed the server summary; enqueue on confirm"
      contains: "buildConfirmSummary"
    - path: "components/campaign/send-card.tsx"
      provides: "The Send card container + gating (needs saved template + SMTP config)"
      contains: "Review and send"
    - path: "app/(app)/compose/page.tsx"
      provides: "Server-side load of SMTP-config presence + default test email, passed to the editor"
      contains: "getSmtpConfigForUser"
  key_links:
    - from: "components/campaign/test-send-panel.tsx"
      to: "lib/campaign/actions::sendTestBatchChunk"
      via: "client while-loop over chunk offsets until done"
      pattern: "sendTestBatchChunk"
    - from: "components/campaign/confirm-send-dialog.tsx"
      to: "lib/campaign/actions::prepareCampaign + buildConfirmSummary + enqueueCampaign"
      via: "open → prepare draft → fetch summary → confirm → enqueue"
      pattern: "prepareCampaign|enqueueCampaign"
    - from: "components/compose/compose-editor.tsx"
      to: "components/campaign/send-card.tsx"
      via: "renders SendCard with the selected recipientSetId + the saved templateId"
      pattern: "SendCard|send-card"
---

<objective>
Deliver the user-facing Phase-5 slice: a "Send" card on the existing `/compose` page that (a) sends the whole personalized batch to one test address by looping the chunked action client-side with live progress, and (b) gates a live send behind an undismissable confirmation modal that renders the server-authoritative summary and enqueues exactly once. After this plan a signed-in user can, end-to-end, test-send their batch and clear the confirm gate — the phase's whole user story becomes real.

Purpose: this is the vertical slice that makes TEST-01/02/03 usable. It wires the Plan 02/03 seams to shadcn primitives that already exist (Dialog, Card, Input, Button, Alert, Collapsible, Skeleton, Sonner) — ZERO new components, ZERO new npm deps (05-UI-SPEC.md Component Inventory).
Output: `components/campaign/{test-send-panel,confirm-send-dialog,send-card}.tsx`, plus wire-in edits to `components/compose/compose-editor.tsx` and `app/(app)/compose/page.tsx`.
</objective>

## Phase Goal

**As a** signed-in user with a saved template, recipient list, and verified SMTP, **I want to** send my whole personalized batch to one test address and clear a hard confirmation gate before any live send, **so that** I can verify exactly what will go out and never fire a real send by accident.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md
@.planning/phases/05-test-send-confirmation-gate/05-PATTERNS.md
@.planning/phases/05-test-send-confirmation-gate/05-02-SUMMARY.md
@.planning/phases/05-test-send-confirmation-gate/05-03-SUMMARY.md

<interfaces>
<!-- The Plan 02/03 action surface the UI consumes (import from @/lib/campaign/actions). -->
```typescript
// Test-send chunk (loop client-side until done):
sendTestBatchChunk(input: { recipientSetId: number|string; templateId: number|string; testAddress: string; offset: number })
  : Promise<{ ok: true; data: { sent: number; failed: number; errors: string[]; nextOffset: number; done: boolean; total: number } }
          | { ok: false; error: ActionError }>;
// Confirm gate:
prepareCampaign(input: { recipientSetId; templateId }): Promise<{ ok:true; data:{ campaignId:number } } | { ok:false; error: ActionError }>;
buildConfirmSummary(input: { campaignId: number }): Promise<{ ok:true; data: ConfirmSummary } | { ok:false; error: ActionError }>;
enqueueCampaign(input: { campaignId: number }): Promise<{ ok:true; data:{ campaignId:number } } | { ok:false; error: ActionError }>;
// ConfirmSummary: { campaignId, recipientCount, senderIdentity, sample:{to,subject,body}, invalidEmailCount, rowsWithGaps, unknownTokens: string[], sendableCount }
// ActionError kinds: unauthenticated | validation | not_found | no_smtp_config | parse_error | already_queued | send_failed{raw} | unknown{raw}
```

Existing UI analogs to mirror:
- `components/smtp/step-test-send.tsx` — the test-send voice/structure: address Input, `Send`+`Loader2` button disabled while sending, `toast.success`, a `failureFor(error)` switch → human copy, a `Collapsible` "Show technical details" for `raw`, destructive `Alert` on total failure. Mirror this verbatim in `test-send-panel.tsx`.
- `components/ui/dialog.tsx` exports `Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose`. `DialogContent` accepts `showCloseButton` and forwards Radix props (`onInteractOutside`, `onEscapeKeyDown`) — read the file to confirm the exact prop names.
- `components/compose/compose-editor.tsx` — the client shell holding `selectedId` (recipientSetId) + `activeSet.row_count`; `onSave` already calls `saveTemplate(fd)` which returns `{ ok:true, data:{ id } }`. Capture that `id` into a `savedTemplateId` state on success.
- `app/(app)/compose/page.tsx` — RSC that `auth()`s and lists sets; extend it to also read SMTP-config presence + the Clerk primary email.

Copy strings + all screen states + interaction rules (undismissable gate, count-in-label confirm, soft duration warning, warning classification, hard-block on zero sendable recipients) are specified verbatim in 05-UI-SPEC.md (Copywriting Contract + Screen States). Follow them exactly — sentence case, no exclamation marks, one accent button per view.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-send panel — chunk-loop driver, progress, summary</name>
  <files>components/campaign/test-send-panel.tsx</files>
  <read_first>
    - components/smtp/step-test-send.tsx (whole file — the structure/voice to mirror: Input + Send/Loader2 button + failureFor switch + Collapsible technical details + destructive Alert)
    - lib/campaign/schema.ts (TEST_SEND_CHUNK_SIZE, TEST_SEND_DELAY_MS — the client uses total/chunk to estimate the duration note)
    - .planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md (Copywriting Contract: test-send rows; Screen States: Test-send default/large-set/invalid/in-flight/all-sent/partial-failure/total-failure)
    - .planning/phases/05-test-send-confirmation-gate/05-02-SUMMARY.md (the exact chunk result shape)
  </read_first>
  <action>
    Create `components/campaign/test-send-panel.tsx` (`"use client"`). Props: `{ recipientSetId: string; templateId: number; recipientCount: number; defaultTestEmail: string; disabled?: boolean }`. State: `to` (default `defaultTestEmail`), `sending`, `progress` (`{ sent: number; failed: number; done: number; total: number } | null`), `failure` (total-failure), `summary` (final `{ sent; failed; errors: string[] } | null`). Render (per 05-UI-SPEC): a "Send a test batch" sub-title + help; a "Test address" `Label`+`Input` (type=email); a duration note computed from `recipientCount` (`~{recipientCount} messages`, rough duration ≈ `recipientCount * (TEST_SEND_DELAY_MS/1000)` seconds, prominent `AlertTriangle` soft warning when large — NO hard cap, exact copy from the spec); a "Send test batch" `Button` (`outline`/`secondary` variant, `Send`+`Loader2`, disabled while `sending` or address invalid or `disabled`). On click: validate the address is non-empty and email-shaped (field-anchored "Enter a valid test address." otherwise); then run the CHUNK LOOP — `let offset = 0`, accumulate `{sent, failed, errors}`, `while (true) { const res = await sendTestBatchChunk({ recipientSetId, templateId, testAddress, offset }); if (!res.ok) { setFailure(failureFor(res.error)); break; } sent += res.data.sent; failed += res.data.failed; errors.push(...res.data.errors); setProgress({ sent, failed, done: res.data.nextOffset, total: res.data.total }); if (res.data.done) break; offset = res.data.nextOffset; }`. On a clean loop: `toast.success` "Sent {sent} test messages to {address}." and set the summary (all-sent CheckCircle2 / partial-failure per-message AlertCircle lines using the exact spec copy). A total failure (first chunk `!ok`) renders the destructive `Alert` mirroring `step-test-send.tsx`, with a `Collapsible` for `raw`. Copy `failureFor` from step-test-send and extend it for the campaign `ActionError` kinds (`not_found` → "that recipient list or template is no longer available", `no_smtp_config` → link to settings copy, `parse_error` → "we couldn't read that CSV", `validation` → "enter a valid test address", `send_failed`/`unknown` → server message with raw). No live progress BAR is required beyond the sent/total text (progress bar is Phase 6). Never render a cell value as HTML; never log the address list.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 | grep -qiv "Failed to compile" && grep -q "sendTestBatchChunk" components/campaign/test-send-panel.tsx && echo build-ok</automated>
  </verify>
  <acceptance_criteria>
    - `components/campaign/test-send-panel.tsx` loops `sendTestBatchChunk` over `offset` until `res.data.done`, accumulating sent/failed/errors (grep: `sendTestBatchChunk`, a `while`/loop, `nextOffset`).
    - The "Send test batch" button is `outline`/`secondary` (NOT accent) and disabled while sending or when the address is invalid.
    - A soft duration note renders (no hard cap / no row limit anywhere in the file — grep: no "cap"/"slice(0," client-side truncation).
    - Copy strings match 05-UI-SPEC.md (sentence case, no exclamation marks).
    - `npm run build` compiles.
  </acceptance_criteria>
  <done>The test-send panel sends the whole batch to one address via bounded client-driven chunks, with progress, a soft duration warning, and a per-message summary — and never truncates the batch.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm-send dialog — undismissable gate over the server summary</name>
  <files>components/campaign/confirm-send-dialog.tsx</files>
  <read_first>
    - components/ui/dialog.tsx (exact exports + `showCloseButton` + Radix `onInteractOutside`/`onEscapeKeyDown` prop names to make it undismissable)
    - components/smtp/step-test-send.tsx (the async-action + `useState(sending)` + toast flow to mirror)
    - .planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md (Copywriting Contract: every Modal row; Screen States: Review opening/loaded/warnings/no-sendable/summary-load-failed/confirm-in-flight/queued/already-queued/enqueue-failed; Interaction rules — undismissable, count-in-label confirm, warning classification, escaped-text sample)
    - .planning/phases/05-test-send-confirmation-gate/05-03-SUMMARY.md (ConfirmSummary shape + ActionError kinds)
  </read_first>
  <action>
    Create `components/campaign/confirm-send-dialog.tsx` (`"use client"`). Props: `{ recipientSetId: string; templateId: number; open: boolean; onOpenChange: (o:boolean)=>void }`. On EVERY open transition (`open` flips to `true`): reset any prior `campaignId`/`summary` state, then call `prepareCampaign({ recipientSetId, templateId })`; ALSO reset `campaignId`/`summary` in an effect keyed on `[recipientSetId, templateId]` so a review NEVER shows a summary built from a previously selected list/template (checker fix — stale-summary guard) → on ok, `buildConfirmSummary({ campaignId })`; store `campaignId` + `summary`; show `Skeleton` review rows while loading; on any failure show the destructive `Alert` with the "summary load failed" copy and keep confirm disabled. Render the modal via `Dialog`/`DialogContent` made UNDISMISSABLE: `showCloseButton={false}` and `onInteractOutside`/`onEscapeKeyDown` handlers calling `event.preventDefault()` (so it closes ONLY via Cancel or a successful enqueue). Content per 05-UI-SPEC: `DialogTitle` "Send to everyone?", `DialogDescription` the irreversibility line, then review rows — "Recipients: {N}" (append "{invalidCount} skipped — invalid email" muted when >0), "From: {senderIdentity}", a `Separator`, the "Sample — how row 1 will look" block ("To: {sample.to}", "Subject: {sample.subject}", then `sample.body` rendered as ESCAPED text via `whitespace-pre-wrap` — never `dangerouslySetInnerHTML`), a `Separator`, then the warning block using the exact classification + copy: unknown-token `AlertTriangle` line at top (if `unknownTokens.length`), invalid-email `AlertCircle`(muted)/`CheckCircle2`(text-success) line, missing-merge-value `AlertCircle`/`CheckCircle2` line. `DialogFooter`: `DialogClose` "Cancel" (`outline`/`ghost`) + the single accent confirm `Button` reading "Send to {sendableCount} recipients" (`Send` icon), `disabled` while summary loading OR `summary.sendableCount === 0` (the ONE hard block — show the no-sendable-recipients copy) OR while queuing. On confirm: `setSubmitting(true)`, `enqueueCampaign({ campaignId })`; on ok → `toast.success` "Your send is queued — {N} recipients." + `onOpenChange(false)`; on `{ kind:"already_queued" }` → benign neutral `toast` "This send is already queued." + close; on other errors → destructive `Alert` in the modal (draft untouched), stay open to retry. Warnings NEVER hard-disable confirm except the zero-sendable block (Assumption U4/U6). The sample + warnings come ONLY from the server summary — the component computes no counts itself.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 | grep -qiv "Failed to compile" && grep -q "buildConfirmSummary" components/campaign/confirm-send-dialog.tsx && grep -q "whitespace-pre-wrap" components/campaign/confirm-send-dialog.tsx && echo build-ok</automated>
  </verify>
  <acceptance_criteria>
    - The modal is undismissable: `showCloseButton={false}` and `preventDefault` on interact-outside + escape (grep: `showCloseButton`, `preventDefault`).
    - The confirm button label includes the count ("Send to {sendableCount} recipients") and disables while loading / zero-sendable / queuing.
    - The merged sample renders with `whitespace-pre-wrap` and NO `dangerouslySetInnerHTML` (grep: absent).
    - Confirm calls `enqueueCampaign`; `already_queued` yields a neutral toast, not a destructive Alert (grep: `already_queued`).
    - Every count/sample/warning comes from `buildConfirmSummary` (grep: no `countInvalidEmails`/`analyzeMerge` in this client file).
    - `npm run build` compiles.
  </acceptance_criteria>
  <done>The confirmation gate is a server-authoritative, undismissable modal with a count-bearing confirm that enqueues exactly once — a second confirm is benign.</done>
</task>

<task type="auto">
  <name>Task 3: Send card + compose-editor/page wire-in and gating</name>
  <files>components/campaign/send-card.tsx, components/compose/compose-editor.tsx, app/(app)/compose/page.tsx</files>
  <read_first>
    - components/compose/compose-editor.tsx (whole file — where `selectedId`, `activeSet.row_count`, and `onSave`→`saveTemplate` live; add `savedTemplateId` state + render the SendCard)
    - app/(app)/compose/page.tsx (whole file — the RSC to extend with SMTP presence + default email)
    - lib/data/smtp.ts (getSmtpConfigForUser — presence check) ; @clerk/nextjs/server `currentUser()` for the primary email default (see lib/smtp/actions.ts:110-129 for the exact primaryEmailAddress access)
    - .planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md (Screen States: Send card gated/ready; Copywriting Contract: Send card title/help, forward-CTA disabled copies; one-accent-per-view discipline)
  </read_first>
  <action>
    Create `components/campaign/send-card.tsx` (`"use client"`): a `Card` titled "Send" (CardTitle text-xl) rendered below the preview. Props: `{ recipientSetId: string; templateId: number | null; recipientCount: number; hasSmtpConfig: boolean; defaultTestEmail: string }`. Gating (exact copy from the spec): if `templateId === null` → muted help "Save your template above first." and both actions disabled; else if `!hasSmtpConfig` → "Review and send" disabled with the settings-link help, test-send disabled with the same reason; else ready. When ready render `<TestSendPanel .../>` and a single accent "Review and send" `Button` that toggles a `confirmOpen` state feeding `<ConfirmSendDialog open={confirmOpen} onOpenChange={setConfirmOpen} recipientSetId={recipientSetId} templateId={templateId} />`. "Review and send" is the ONE accent button in this card (the test-send button is `outline`/`secondary`) — preserve the one-accent-per-view rule.
    Edit `components/compose/compose-editor.tsx`: add `savedTemplateId` state (number|null, init null); in `onSave` success set it from `res.data.id`; add `hasSmtpConfig: boolean` and `defaultTestEmail: string` to the component props (passed from the page); render `<SendCard recipientSetId={selectedId} templateId={savedTemplateId} recipientCount={activeSet?.row_count ?? 0} hasSmtpConfig={hasSmtpConfig} defaultTestEmail={defaultTestEmail} />` at the bottom of the returned column (after `<PreviewStepper/>`). Do NOT change the existing save/preview/autocomplete behavior. Note: saving a template again creates a new standalone template row and updates `savedTemplateId` to the newest id (template save stays standalone per A1/U7).
    Edit `app/(app)/compose/page.tsx`: after `auth()`, also `const cfg = userId ? await getSmtpConfigForUser(userId) : undefined;` → `hasSmtpConfig = !!cfg`; get the default test email from `currentUser()` primary email (fallback empty string); pass `hasSmtpConfig` and `defaultTestEmail` to `<ComposeEditor .../>`. Never pass the SMTP password or the encrypted triple to the client — only the boolean + the email string.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 | grep -qiv "Failed to compile" && grep -q "getSmtpConfigForUser" "app/(app)/compose/page.tsx" && grep -q "SendCard\|send-card" components/compose/compose-editor.tsx && echo build-ok</automated>
  </verify>
  <acceptance_criteria>
    - `send-card.tsx` gates on `templateId !== null && hasSmtpConfig`, showing the exact disabled-state help copy from the spec; "Review and send" is the sole accent button; the test-send button is `outline`/`secondary`.
    - `compose-editor.tsx` captures the saved template id from `saveTemplate`'s `res.data.id` and renders `SendCard` (grep: `savedTemplateId`).
    - `page.tsx` reads SMTP presence via `getSmtpConfigForUser` and passes only a boolean + the default email to the client (grep: no `password_enc`/`decrypt` in the page).
    - `npm run build` compiles; `npm test` full suite still green.
  </acceptance_criteria>
  <done>The /compose page gains a gated Send card wiring the test-send panel and the confirm gate to the selected recipient list + saved template — the full user story is now reachable in the browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Actions | The client sends only ids + the test address; all authority stays server-side (Plans 02/03) |
| RSC → client props | The page passes only a boolean SMTP-presence flag + an email string — never the password/triple |
| server summary → modal render | The modal displays server strings; the merged sample is escaped text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-XSS | Tampering | merged sample body in the modal | mitigate | Rendered via JSX text + `whitespace-pre-wrap`; NO `dangerouslySetInnerHTML` (grep-gated) — a CSV cell can't inject HTML |
| T-5-CRED | Information Disclosure | page → client props | mitigate | Page passes only `hasSmtpConfig` boolean + default email; never `getSmtpConfigForUser`'s encrypted triple |
| T-5-DUPE | Tampering | confirm button | mitigate | Button disables in-flight (cosmetic); the real guard is the server atomic transition — a second confirm shows the benign already-queued toast |
| T-5-TAMPER | Tampering | modal counts/warnings | mitigate | Every count/sample/warning comes from `buildConfirmSummary`; the client computes none (grep: no aggregate fns in the client files) |
| T-5-DOS | Denial of Service | test-send loop | mitigate | The client loops bounded chunks; each request is short — no single long synchronous call |
| T-5-SC | Tampering | npm installs | mitigate | Zero new components / zero new npm deps (05-UI-SPEC Component Inventory) |
</threat_model>

<verification>
- `npm run build` → compiles clean (the UI compile gate from 05-VALIDATION.md).
- `npm test` → full suite green (no regression).
- Browser harness (orchestrator, no real SMTP): Send card gates until template saved + SMTP present; "Review and send" opens an undismissable modal rendering count/sender/sample/warnings; hammering confirm yields exactly one queued transition + a benign already-queued toast; the test-send button drives chunked progress.
- Grep gates: `dangerouslySetInnerHTML` absent from `components/campaign/*`; `password_enc`/`decrypt` absent from `app/(app)/compose/page.tsx`.
</verification>

<success_criteria>
- A signed-in user with a saved template + verified SMTP can, in the browser, test-send the whole batch to one address (progress + summary) and clear an undismissable confirm gate that shows the server-authoritative review and enqueues exactly once (TEST-01/02/03 UI).
</success_criteria>

<output>
Create `.planning/phases/05-test-send-confirmation-gate/05-04-SUMMARY.md` when done.
</output>
