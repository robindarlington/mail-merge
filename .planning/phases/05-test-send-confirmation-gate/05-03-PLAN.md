---
phase: 05-test-send-confirmation-gate
plan: 03
type: tdd
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - lib/campaign/actions-core.ts
  - lib/campaign/actions.ts
  - lib/campaign/actions-core.test.ts
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "A draft campaign is created at the 'review and send' moment from the selected recipient set + template + saved SMTP config (all three FKs), owned by the caller (decision A1/U7)"
    - "The confirm summary is SERVER-authoritative: recipient count, redacted sender identity (no password), one merged sample recipient (subject+body+to), and warning aggregates (invalid emails, rows with empty merge values, unknown tokens) — all recomputed server-side from the stored CSV + template"
    - "Enqueuing flips the draft campaign to queued exactly once; a second confirm returns already_queued (benign), never a duplicate transition (TEST-03)"
    - "A campaign/recipient set/template owned by another tenant can never be prepared, summarized, or enqueued (IDOR)"
  artifacts:
    - path: "lib/campaign/actions-core.ts"
      provides: "prepareCampaignCore + buildConfirmSummaryCore + enqueueCampaignCore seams"
      exports: ["prepareCampaignCore", "buildConfirmSummaryCore", "enqueueCampaignCore", "ConfirmSummary"]
    - path: "lib/campaign/actions.ts"
      provides: "\"use server\" wrappers prepareCampaign, buildConfirmSummary, enqueueCampaign"
      exports: ["prepareCampaign", "buildConfirmSummary", "enqueueCampaign"]
    - path: "lib/campaign/actions-core.test.ts"
      provides: "TEST-02 server-authoritative summary + TEST-03 double-enqueue coverage"
      contains: "buildConfirmSummaryCore"
  key_links:
    - from: "lib/campaign/actions-core.ts::enqueueCampaignCore"
      to: "lib/data/campaigns.ts::enqueueCampaign"
      via: "maps the DAL's 0-row result to { kind: 'already_queued' }"
      pattern: "already_queued"
    - from: "lib/campaign/actions-core.ts::buildConfirmSummaryCore"
      to: "countInvalidEmails + analyzeMerge + fillMessage + toSmtpConfigDto"
      via: "server recomputes every gate aggregate from the stored CSV + template (never trusts a client payload)"
      pattern: "analyzeMerge|toSmtpConfigDto"
---

<objective>
Wire the confirmation gate (TEST-02) and the single draft→queued transition (TEST-03) as three seams over the campaigns DAL from Plan 01: `prepareCampaignCore` creates the draft campaign at the "review and send" moment (decision A1/U7 — from the selected recipient set + template + saved SMTP config, since all three FKs are NOT NULL); `buildConfirmSummaryCore` returns the server-authoritative review payload the modal renders; `enqueueCampaignCore` performs the atomic transition and maps the DAL's affected-row count to a typed result. This extends the `lib/campaign/actions.ts` / `actions-core.ts` seam files created in Plan 02.

Purpose: the gate's whole value is that it cannot be bypassed. Every number the modal shows — count, sender identity, sample, warnings — is recomputed server-side from the stored CSV + template (`countInvalidEmails`, `analyzeMerge`, `fillMessage`, `toSmtpConfigDto`), so a tampered client payload can neither suppress a warning nor enqueue past the guard. The atomic `UPDATE ... WHERE status='draft'` (Plan 01) makes double-submit a benign no-op.
Output: extended `lib/campaign/actions-core.ts`, `lib/campaign/actions.ts`, `lib/campaign/actions-core.test.ts`.
</objective>

## Phase Goal

**As a** signed-in user with a saved template, recipient list, and verified SMTP, **I want to** send my whole personalized batch to one test address and clear a hard confirmation gate before any live send, **so that** I can verify exactly what will go out and never fire a real send by accident.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-test-send-confirmation-gate/05-RESEARCH.md
@.planning/phases/05-test-send-confirmation-gate/05-PATTERNS.md
@.planning/phases/05-test-send-confirmation-gate/05-02-SUMMARY.md

<interfaces>
<!-- What Plans 01 + 02 hand this plan, plus the reused aggregate primitives. -->

From Plan 01 (lib/data/campaigns.ts, re-exported via @/lib/data):
```typescript
export type PersistableCampaign = Pick<NewCampaign, "recipient_set_id" | "template_id" | "smtp_config_id">;
export function createDraftCampaign(userId: string, values: PersistableCampaign): Promise<Campaign[]>;   // .returning()
export function getCampaignForUser(userId: string, id: number): Promise<Campaign | undefined>;
export function enqueueCampaign(userId: string, id: number): Promise<{ id: number }[]>;   // length 1 = won, 0 = already-queued/not-owner
```

From Plan 02 (lib/campaign/actions-core.ts) — extend, do not duplicate: the `ActionError` union (already has `not_found`, `no_smtp_config`, `parse_error`, `already_queued`, `unknown`, `validation`, `unauthenticated`) and `campaignIdSchema`/`recipientSetIdSchema`/`templateIdSchema` from `./schema`.

Reused aggregate/redaction primitives:
```typescript
import { parseCsv, detectEmailColumn, countInvalidEmails, analyzeMerge, fillMessage } from "@/lib/core";
// analyzeMerge(template: string, row, columns): { empty: string[]; unknown: string[] }  — unknown wins over empty
import { getRecipientSetForUser, getTemplateForUser, getSmtpConfigForUser, toSmtpConfigDto } from "@/lib/data";
import { readUpload } from "@/lib/csv";
// toSmtpConfigDto(row): { host, port, secure, username, from_addr, from_name, verified_at }  — structurally omits the password triple
```

Server-authoritative summary composition (from lib/compose/actions-core.ts resolve→read→parse, extended with sender identity + sample + gap aggregates):
```typescript
const set = await getRecipientSetForUser(userId, recipientSetId);      // IDOR-safe
const { columns, rows } = parseCsv(readUpload(set.storage_path));
const emailColumn = set.email_column ?? detectEmailColumn(columns, rows);
const invalidEmailCount = emailColumn ? countInvalidEmails(rows, emailColumn) : 0;
const cfg = await getSmtpConfigForUser(userId);
const dto = toSmtpConfigDto(cfg);
const senderIdentity = dto.from_name ? `${dto.from_name} <${dto.from_addr}>` : dto.from_addr;
const sample = fillMessage({ subject, body }, rows[0]);
// gap aggregates: analyzeMerge(subject + "\n" + body, row, columns) over rows → rowsWithGaps (empty.length>0) + unknownTokens union
```

The enqueue mapping (lib/campaign PATTERNS Shared Pattern "Atomic enqueue guard"): call `enqueueCampaign(userId, id)`; `result.length !== 1` → `{ ok: false, error: { kind: "already_queued" } }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing TEST-02/TEST-03 seam tests</name>
  <files>lib/campaign/actions-core.test.ts</files>
  <read_first>
    - lib/campaign/actions-core.test.ts (the harness + fixtures from Plan 02 — EXTEND it; reuse the temp DB, temp CSV, seeded recipient set/template/smtp config)
    - lib/data/campaigns.test.ts (Plan 01 — how draft campaigns + enqueue were seeded/asserted, to mirror the double-enqueue assertion at the seam level)
    - lib/core/merge.ts (analyzeMerge semantics: unknown wins over empty) + lib/core/csv.ts (countInvalidEmails)
    - lib/data/smtp.ts (toSmtpConfigDto — the redacted shape the summary's sender identity comes from)
    - .planning/phases/05-test-send-confirmation-gate/05-UI-SPEC.md (Copywriting Contract modal rows — the exact aggregates the summary must expose: recipient count, invalidCount, rowsWithGaps, unknownTokens, sample To/Subject/body, sendableCount)
  </read_first>
  <behavior>
    - prepareCampaignCore(userId, {recipientSetId, templateId}) creates a draft campaign whose recipient_set_id/template_id/smtp_config_id match the caller's saved rows, returns `{ campaignId }`, and the row's status is "draft". With NO saved SMTP config it returns `{ kind: "no_smtp_config" }` and creates nothing.
    - prepareCampaignCore is IDOR-safe: a recipientSetId/templateId owned by another user → `{ kind: "not_found" }`, no row created.
    - buildConfirmSummaryCore(userId, {campaignId}) returns recipientCount === rows.length, senderIdentity from the redacted DTO (assert it does NOT contain the SMTP password/MARKER), a sample for row 1 with `to` === row1's email-column value, merged subject and body, invalidEmailCount over the resolved email column, rowsWithGaps (rows with ≥1 empty merge value), unknownTokens union (a `{{typo}}` not in columns appears), and sendableCount === recipientCount − invalidEmailCount.
    - buildConfirmSummaryCore recomputes server-side (client sends ONLY a campaignId): a fixture with a known-bad email row yields invalidEmailCount ≥ 1 even though the client passes nothing but the id.
    - buildConfirmSummaryCore IDOR: another tenant's campaignId → `{ kind: "not_found" }`.
    - enqueueCampaignCore(userId, {campaignId}) on a draft returns ok; a SECOND call returns `{ kind: "already_queued" }`; another tenant's campaignId returns `{ kind: "already_queued" }` (0-row guard) and leaves status unchanged.
    - Redaction: `JSON.stringify(summary)` never contains MARKER_PASSWORD.
  </behavior>
  <action>
    Extend `lib/campaign/actions-core.test.ts` with a new block of assertions for the three seams. Reuse the Plan-02 fixtures (temp CSV, seeded recipient set/template/smtp config for the primary user) and add a SECOND user (USER_B) with their own recipient set/template for the IDOR cases, plus a recipient-set fixture containing at least one invalid-email row and at least one row with a blank merge value and a template referencing a `{{typo}}` unknown token so the aggregates are non-trivial. Import `prepareCampaignCore`, `buildConfirmSummaryCore`, `enqueueCampaignCore` from `./actions-core`. Write the behavior assertions above. Tests MUST fail now (seams not implemented). Do not log secrets or cell values.
  </action>
  <verify>
    <automated>node --import tsx --test lib/campaign/actions-core.test.ts 2>&1 | grep -Eq "not a function|Cannot find|not ok" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `lib/campaign/actions-core.test.ts` gains assertions for prepare (create + no_smtp_config + IDOR), summary (server-authoritative aggregates + sample + redaction + IDOR), and enqueue (once + already_queued + cross-tenant).
    - The suite fails because the three seams are not implemented (RED).
    - A summary redaction assertion greps for MARKER_PASSWORD and asserts absence.
  </acceptance_criteria>
  <done>The failing tests pin the confirm gate as server-authoritative and the enqueue as single-transition — before either is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prepare + confirm-summary + enqueue seams to green</name>
  <files>lib/campaign/actions-core.ts</files>
  <read_first>
    - lib/campaign/actions-core.ts (Plan 02 — the ActionError union + existing resolve helpers to reuse; ADD the three seams here)
    - lib/compose/actions-core.ts lines 103-143 (previewCampaignCore resolve→read→parse→emailColumn→invalidEmailCount — the exact server-authoritative pattern to extend)
    - lib/data/campaigns.ts (Plan 01 — createDraftCampaign, getCampaignForUser, enqueueCampaign signatures)
    - lib/data/smtp.ts (toSmtpConfigDto) + lib/core/merge.ts (analyzeMerge) + lib/core/fill.ts (fillMessage)
    - .planning/phases/05-test-send-confirmation-gate/05-PATTERNS.md (`lib/campaign/actions-core.ts` confirm-summary seam + enqueue seam sections; Shared Pattern "Atomic enqueue guard" + "Credential handling")
  </read_first>
  <action>
    In `lib/campaign/actions-core.ts` add: (1) `export type ConfirmSummary = { campaignId: number; recipientCount: number; senderIdentity: string; sample: { to: string; subject: string; body: string }; invalidEmailCount: number; rowsWithGaps: number; unknownTokens: string[]; sendableCount: number }` and a `SummaryResult`/`PrepareResult`/`EnqueueResult` union (each `{ ok:true; data } | { ok:false; error: ActionError }`).
    (2) `prepareCampaignCore(userId, input)`: validate `recipientSetId`/`templateId`; resolve `getRecipientSetForUser`/`getTemplateForUser` (undefined → not_found) and `getSmtpConfigForUser` (undefined → no_smtp_config); call `createDraftCampaign(userId, { recipient_set_id, template_id, smtp_config_id })`; return `{ ok:true, data: { campaignId: created.id } }`. This is the A1/U7 timing — the draft is created HERE (review-and-send moment), not at template save.
    (3) `buildConfirmSummaryCore(userId, input)`: validate `campaignId`; `getCampaignForUser(userId, campaignId)` (undefined → not_found); resolve the campaign's recipient set / template / smtp config via the userId-scoped DALs off the campaign's FKs (not from any client value); `readUpload(set.storage_path)` + `parseCsv` (structural error → parse_error); `emailColumn = set.email_column ?? detectEmailColumn(...)`; `invalidEmailCount = emailColumn ? countInvalidEmails(rows, emailColumn) : 0`; `dto = toSmtpConfigDto(cfg)` → `senderIdentity`; `sample = { to: emailColumn ? (rows[0]?.[emailColumn] ?? "") : "", ...fillMessage({ subject: template.subject, body: template.body }, rows[0] ?? {}) }`; compute `rowsWithGaps` = count of rows where `analyzeMerge(template.subject + "\n" + template.body, row, columns).empty.length > 0`, and `unknownTokens` = de-duplicated union of `.unknown` across rows (or compute once from `extractTokens` against columns — unknown is row-independent, so a single pass over columns is sufficient and cheaper); `sendableCount = recipientCount - invalidEmailCount`. Return the ConfirmSummary. Guard `rows.length === 0` gracefully (empty sample, zero counts).
    (4) `enqueueCampaignCore(userId, input)`: validate `campaignId`; call `enqueueCampaign(userId, campaignId)`; if `result.length !== 1` return `{ ok:false, error: { kind: "already_queued" } }` (covers double-submit AND cross-tenant/not-draft); else `{ ok:true, data: { campaignId } }`.
    Keep every `raw` a string; never `console.*` a password, the DTO's secrets (there are none by construction), or CSV cell values.
  </action>
  <verify>
    <automated>node --import tsx --test lib/campaign/actions-core.test.ts 2>&1 | tail -5 | grep -q "pass" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 assertions pass (prepare, summary, enqueue).
    - `buildConfirmSummaryCore` calls `countInvalidEmails`, `analyzeMerge` (or `extractTokens`), `fillMessage`, and `toSmtpConfigDto` — the gate is recomputed server-side (grep confirms those identifiers in the function).
    - `enqueueCampaignCore` maps `enqueueCampaign(...).length !== 1` to `already_queued` (grep).
    - No `"use server"` directive in `actions-core.ts` (grep).
  </acceptance_criteria>
  <done>The confirm gate returns a fully server-recomputed summary and the enqueue is a single, double-submit-proof transition — all owner-scoped.</done>
</task>

<task type="auto">
  <name>Task 3: "use server" wrappers for prepare, summary, enqueue</name>
  <files>lib/campaign/actions.ts</files>
  <read_first>
    - lib/campaign/actions.ts (Plan 02 — the sendTestBatchChunk wrapper + the type re-export block; ADD three wrappers in the same shape)
    - lib/compose/actions.ts (the lazy-auth → delegate wrapper pattern to mirror exactly)
    - .planning/phases/05-test-send-confirmation-gate/05-PATTERNS.md (Shared Pattern "actions split" — only wrappers here)
  </read_first>
  <action>
    In `lib/campaign/actions.ts` (already `"use server"`) add three thin wrappers, each mirroring the existing `sendTestBatchChunk` shape: `prepareCampaign(input)`, `buildConfirmSummary(input)`, `enqueueCampaign(input)`. Each lazily imports `auth` from `@clerk/nextjs/server`, returns `{ ok:false, error:{ kind:"unauthenticated" } }` when there is no `userId`, otherwise delegates to the matching `*Core(userId, input)`. Add erased type re-exports for `ConfirmSummary`, `PrepareResult`, `SummaryResult`, `EnqueueResult`. Note the wrapper's `enqueueCampaign` (the action) is distinct from the DAL's `enqueueCampaign` (imported into actions-core) — the action name is the client-facing surface; keep the import in actions-core unaliased-or-aliased consistently so there is no shadowing bug.
  </action>
  <verify>
    <automated>node --import tsx -e "import('./lib/campaign/actions.ts').then(m=>{for(const f of ['sendTestBatchChunk','prepareCampaign','buildConfirmSummary','enqueueCampaign'])if(typeof m[f]!=='function')throw new Error('missing '+f);console.log('wrappers-ok')})" && grep -q '"use server"' lib/campaign/actions.ts && echo has-directive</automated>
  </verify>
  <acceptance_criteria>
    - `lib/campaign/actions.ts` exports `sendTestBatchChunk, prepareCampaign, buildConfirmSummary, enqueueCampaign` — all functions.
    - Each new wrapper re-derives `userId` via lazy `auth()` and delegates to its `*Core` (grep: `auth` + `Core` in the file).
    - `npm test` full suite green; `npm run build` succeeds (the actions module compiles under the Next server build).
  </acceptance_criteria>
  <done>The four campaign Server Actions are the only public surface; each auth-gates before delegating to a userId-scoped core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → "use server" action | Client sends only ids (recipientSetId/templateId/campaignId); every value is validated + owner-checked server-side |
| server-recomputed gate → client | The modal receives ONLY the recomputed ConfirmSummary; it cannot influence the counts |
| DAL atomic UPDATE | The single-writer transition decides the one true draft→queued flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-IDOR | Elevation of Privilege | prepare/summary/enqueue resolve | mitigate | `getCampaignForUser`/`getRecipientSetForUser`/`getTemplateForUser` userId-scoped; the enqueue WHERE carries user_id; cross-tenant ids → not_found / already_queued (tested) |
| T-5-TAMPER | Tampering | confirm summary | mitigate | Every aggregate (count, invalidEmailCount, rowsWithGaps, unknownTokens, sample) recomputed server-side from the stored CSV + template; the client passes only a campaignId (tested) |
| T-5-DUPE | Tampering / Repudiation | enqueueCampaignCore | mitigate | Maps the atomic DAL guard's 0-row result to already_queued; second confirm is benign, never a second transition (tested) |
| T-5-CRED | Information Disclosure | sender identity | mitigate | Sender identity comes from `toSmtpConfigDto`, which structurally cannot carry the password triple; summary redaction grep asserts no MARKER |
| T-5-ENDPOINT | Elevation of Privilege | core seams | mitigate | prepare/summary/enqueue cores live in a NON-"use server" module; only the auth wrappers are wire-callable |
| T-5-XSS | Tampering | merged sample fields | mitigate | The sample is returned as plain strings; the UI (Plan 04) renders them as escaped text (never HTML) — noted here as the contract |
| T-5-SC | Tampering | npm installs | mitigate | No packages added (05-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --import tsx --test lib/campaign/actions-core.test.ts` → all TEST-02 + TEST-03 seam assertions pass.
- `npm test` → full suite green; `npm run build` → clean.
- Grep gates: `grep -n "already_queued" lib/campaign/actions-core.ts`; `grep -n "toSmtpConfigDto\|analyzeMerge\|countInvalidEmails" lib/campaign/actions-core.ts`; `grep -L "use server" lib/campaign/actions-core.ts`.
</verification>

<success_criteria>
- Before a live send the user gets a server-authoritative review (count, sender, sample, warnings) that a tampered client cannot weaken (TEST-02).
- The draft campaign is created at the review-and-send moment and can transition to queued only once (TEST-03), with all three surfaces owner-scoped.
</success_criteria>

<output>
Create `.planning/phases/05-test-send-confirmation-gate/05-03-SUMMARY.md` when done.
</output>
