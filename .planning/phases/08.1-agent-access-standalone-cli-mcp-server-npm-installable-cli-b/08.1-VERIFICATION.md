---
phase: 08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b
verified: 2026-07-18T22:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Confirm the package-legitimacy gate for @modelcontextprotocol/sdk@1.29.0 (Plan 01, Task 1) that was designated `checkpoint:human-verify gate=\"blocking-human\"` and explicitly marked \"NOT auto-approvable\" in the plan"
    expected: "A human confirms the SDK identity (publisher = official MCP/Anthropic org, repo github.com/modelcontextprotocol/typescript-sdk, version 1.29.0) before it's treated as trusted in the lockfile"
    why_human: "The plan itself declares this gate non-auto-approvable, but 08.1-01-SUMMARY.md documents it was cleared autonomously by the executor (citing a standing 'don't halt overnight runs' preference) rather than by a human. The SDK looks legitimate on inspection (verified below), but the specific process control the plan mandated did not execute as designed and has not had human eyes on it since."
  - test: "Run `npm login && npm publish --access public` from packages/cli, then `npx @robindarlington/mail-merge --help` from a machine that never cloned the repo, and confirm an MCP client config using `npx -y @robindarlington/mail-merge mcp` connects"
    expected: "Package publishes under the @robindarlington scope; npx works from a clean environment; MCP client connects"
    why_human: "Requires Rob's npm credentials (npm whoami confirmed ENEEDAUTH on this machine); explicitly queued as a non-blocking human action per 08.1-04-PLAN.md Task 3 and 08.1-04-SUMMARY.md. Everything up to publish (bundle, tarball smoke test, bundled send test) is independently verified below and does not depend on this step."
---

# Phase 08.1: Agent access — standalone CLI + MCP server Verification Report

**Phase Goal:** An AI agent or developer can run mail merges without the web UI: a npm-installable CLI (bin: `mail-merge`) that reuses lib/core directly, plus a stdio MCP server exposing the same operations as tools, so agents can discover and drive the merge engine locally.
**Verified:** 2026-07-18T22:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## MVP Mode Note

ROADMAP.md declares `mode: mvp` for this phase, but the phase goal text does not conform to the strict `As a ..., I want to ..., so that ....` User Story regex (confirmed via `gsd-sdk query user-story.validate`: fails on "Must begin with 'As a '" — the ROADMAP wording is a technical goal statement, and even the PLAN docs' "Phase Goal" paraphrase ("As an AI agent or developer...") fails the literal "As a " prefix check). The phase's actual contract is 5 concrete, technical Success Criteria (SC-1..SC-5) spanning two front-ends (CLI + MCP) for two actor types (developer + AI agent) — not a single linear user journey — so a "User Flow Coverage" table would misrepresent the shape of this phase's deliverable. Per the mismatch, this report proceeds with **standard goal-backward verification against the 5 roadmap Success Criteria** (Step 2a's roadmap contract) rather than the MVP user-flow template. Recommend running `/gsd mvp-phase 08.1` to correct the mode/goal-format mismatch for future phases, or reclassifying this phase as non-MVP in ROADMAP.md — this is a process note, not a phase-goal failure.

## Goal Achievement

### Observable Truths (mapped to ROADMAP Success Criteria SC-1..SC-5)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: npx-executable CLI runs dry-run, `--test ADDR`, and `--send` modes over CSV + `{{column}}` template with SMTP from env/flags, throttled | ✓ VERIFIED | Live-ran `node --import tsx src/bin.ts --csv ... --template ...` → correct DRY RUN banner + per-row `would send ->` lines with merged subject. `send.test.ts` (14 tests) proves live delivers 1/row over stub-smtp, `--test` proofs whole batch to one address with real per-row fill, throttle applied between sends (`throttle()` from lib/core). Tarball-installed `mail-merge --help` (built from `npm pack`) runs from a clean dir, proving npx-executability. |
| 2 | SC-2: SMTP password only from env/hidden prompt, never argv, never logged | ✓ VERIFIED | `parseCliArgs` schema (args.ts) has no `password`/`smtp-pass` option; live-tested `--password x` and `--smtp-pass y` both throw "Unknown option" via strict `parseArgs`. `secrets.ts` reads `SMTP_PASS` env or falls back to `promptHidden` (no-echo). `send.test.ts` includes a capture-harness test asserting a known `SMTP_PASS` value is absent from stdout/stderr/receipts. CR-01 (STARTTLS-stripping gap, password could travel in cleartext) confirmed fixed at `secrets.ts:123-130` and `mcp.ts:89-135` (commit `4b1517c`). |
| 3 | SC-3: stdio MCP server exposes validate-csv, preview-merge, test-send, send with typed results reusing identical lib/core paths | ✓ VERIFIED | Live in-process MCP client (`InMemoryTransport` + `buildServer()`) confirms `listTools()` returns exactly `validate-csv, preview-merge, test-send, send`. `mcp.test.ts` (part of the 46-test suite) exercises typed structuredContent, the two-step confirm token (preview delivers nothing, second call with token delivers + consumes token, wrong/expired token refused), delayMs override, and receiptsPath round-trip/resume. CR-02 (fromName excaping the confirm-token hash, allowing a preview/deliver display-name swap) confirmed fixed at `mcp.ts:123-139` + preview surfacing (commit `54df4b7`). |
| 4 | SC-4: unit tests cover merge parity with lib/core and the CLI argument contract | ✓ VERIFIED | `parity.test.ts` + live spot-check: `mergeRow(tpl,row)` output is byte-identical (`JSON.stringify` equal) to `lib/core.fillMessage(tpl,row)` for a case with `$`-literal and spaces-in-key. `args.test.ts` covers dry/test/live mode derivation, `--delay-ms` NaN-safety, `--email-column` override, and the no-password-flag guarantee. Full suite: **46/46 passing** (`npm test` in packages/cli), matching the reported count. |
| 5 | SC-5: README documents both front-ends with copy-paste agent-usage examples (MCP config snippet + CLI one-liners) | ✓ VERIFIED | `packages/cli/README.md` contains the `mcpServers` MCP client config snippet (`npx -y @robindarlington/mail-merge mcp`) and CLI one-liners for dry-run/--test/--send/--resume with env-based SMTP. Documents the password-never-in-argv guarantee and honest at-least-once `--resume` semantics. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/cli/package.json` | Standalone manifest, own lockfile, pinned MCP SDK, publish metadata | ✓ VERIFIED | `@robindarlington/mail-merge`, `bin.mail-merge→dist/bin.js`, `files:[dist,README.md]`, `publishConfig.access:public`, `dependencies:{}`, `prepack:"npm run build"` (WR-09 fix), `@modelcontextprotocol/sdk:"1.29.0"` exact pin confirmed in both package.json and package-lock.json |
| `packages/cli/package-lock.json` | Own lockfile, root lockfile untouched | ✓ VERIFIED | Present; `git diff --exit-code -- package.json package-lock.json Dockerfile` at repo root is clean; `grep -c '"workspaces"' package.json` = 0 |
| `packages/cli/src/args.ts` | `parseCliArgs` strict, no password option | ✓ VERIFIED | Live-tested: `--password`/`--smtp-pass` rejected; `--env-file` removed from schema (WR-06, commit `2d409cf`) |
| `packages/cli/src/template.ts` | `loadTemplate` Subject/body split | ✓ VERIFIED | Exists, exercised by parity/args tests and live dry-run |
| `packages/cli/src/bin.ts` | Dispatch + dry-run/test/live driver + `mcp` subcommand | ✓ VERIFIED | Live dry-run confirmed; `mail-merge mcp` dispatch present (bin.ts); exit-code fix present (WR-04, `result.failed > 0 → process.exitCode = 1`) |
| `packages/cli/src/secrets.ts` | env-first SMTP intake + promptHidden, requireTLS default | ✓ VERIFIED | `readSmtpConfig`/`promptHidden` exported; `requireTLS` defaults true when `secure:false` (CR-01 fix) |
| `packages/cli/src/receipts.ts` | JSONL append+fsync, readSentSet, deriveReceiptsPath | ✓ VERIFIED | All three exported; `readSentSet` tolerates torn lines via try/catch (WR-02 fix) |
| `packages/cli/src/run.ts` | Shared `runSend` driver reused by CLI + MCP | ✓ VERIFIED | `runSend` exported; injectable `log` sink replaces the WR-01 global-console.log monkeypatch; test+resume rejected (WR-03 fix) |
| `packages/cli/src/mcp.ts` | `buildServer()` with 4 tools + confirm-token gate | ✓ VERIFIED | Live-listed exactly 4 tools; confirm-token TTL present (WR-07 fix); `fromName` bound into hash (CR-02 fix) |
| `packages/cli/README.md` | CLI + MCP copy-paste docs (SC-5) | ✓ VERIFIED | Contains `mcpServers` snippet + CLI one-liners |
| `packages/cli/dist/bin.js` | Self-contained esbuild bundle | ✓ VERIFIED | Rebuilt live: 1.6MB, starts with `#!/usr/bin/env node` on line 1, createRequire shim on line 2; gitignored (confirmed via `git check-ignore`) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `bin.ts` | `lib/core` (parseCsv, detectEmailColumn, fillMessage) | relative import `../../../lib/core` (corrected from plan's documented `../../lib/core`) | ✓ WIRED | Live parity test confirms `mergeRow` === `lib/core.fillMessage` output exactly; live dry-run resolves and merges correctly |
| `args.ts` | `node:util parseArgs` | strict-mode flag parse | ✓ WIRED | Live-tested: unknown flags (including password variants) throw |
| `run.ts` | `lib/core` (createSmtpTransport, verifyTransport, sendOne, throttle) | engine reuse | ✓ WIRED | `send.test.ts` (stub-smtp integration) proves live delivery, verify-before-loop gate, and throttle between sends |
| `run.ts` | `receipts.ts` | appendReceipt/readSentSet | ✓ WIRED | `send.test.ts` resume test confirms no duplicate RCPT after interrupt+`--resume` |
| `mcp.ts` | `run.ts` (runSend) | test-send/send delegate to shared driver | ✓ WIRED | `mcp.test.ts` confirms MCP send/test-send route through the same driver as the CLI — no second send loop found in mcp.ts |
| `bin.ts` | `@modelcontextprotocol/sdk/server/stdio.js` (StdioServerTransport) | `mail-merge mcp` subcommand | ✓ WIRED | `mail-merge mcp` dispatch present in bin.ts; live in-process MCP client successfully lists and would call tools through `buildServer()` |

### Requirements Coverage

No REQUIREMENTS.md IDs are mapped to Phase 08.1 (confirmed: `.planning/REQUIREMENTS.md` traceability table has no row for Phase 08.1, and none of the 36 v1 requirement IDs reference "08.1" or "8.1" anywhere in the file). This matches the phase's own declaration ("Requirements: none mapped — success criteria tracked as SC-1..SC-5"). No orphaned requirements found. Coverage is fully assessed via the roadmap Success Criteria in the Observable Truths table above.

### Anti-Patterns Found

None blocking. All debt markers and stub patterns previously flagged by code review (`08.1-REVIEW.md`) — 2 Critical + 9 Warning — are confirmed fixed in the current code (commits `4b1517c` through `dc3ab5c`, verified individually below). No new `TBD`/`FIXME`/`XXX` markers found in `packages/cli/src`.

**Code review fix verification (all 11 re-checked directly against current source, not just SUMMARY/REVIEW claims):**

| Finding | Fix location verified | Commit |
|---------|----------------------|--------|
| CR-01 (STARTTLS-stripping) | `secrets.ts:123-130` (`requireTLS` defaults true), `mcp.ts:89,107,135` (`requireTls` param + hash binding) | `4b1517c` ✓ present |
| CR-02 (fromName token-hash bypass) | `mcp.ts:123,139` (hash includes `input.fromName`), `mcp.ts:368,418` (preview surfaces `fromName`) | `54df4b7` ✓ present |
| WR-01 (console.log monkeypatch) | `run.ts:77` (`log?: (line:string)=>void` injectable sink); no `runSendQuietStdout` found in either file | `87e2dc7` ✓ present |
| WR-02 (readSentSet crash on torn line) | `receipts.ts:80-86` (try/catch around `JSON.parse`) | `4381400` ✓ present |
| WR-03 (test+resume skips whole batch) | `run.ts:120-125` (explicit throw for `mode==="test" && resume`) | `9089f26` ✓ present |
| WR-04 (exit 0 on all-failed) | `bin.ts:176` (`result.failed > 0 → process.exitCode = 1`) | `67dd87f` ✓ present |
| WR-05 (parseErrors ignored) | `mcp.ts:58-61` (`countStructuralParseErrors`), used in validate-csv/send/test-send/bin.ts | `8b611f5` ✓ present |
| WR-06 (`--env-file` silently ignored) | `args.ts:65-66` (comment confirms option removed; not in schema) | `2d409cf` ✓ present |
| WR-07 (tokens never expire) | `mcp.ts:51` (`CONFIRM_TTL_MS`), `mcp.ts:358` (purge on access) | `73a90b4` ✓ present |
| WR-08 (`test-send` missing receiptsWarning) | `mcp.ts:282,317` (`receiptsWarning` in outputSchema + result) | `a80f9bd` ✓ present |
| WR-09 (no prepack build) | `package.json` (`"prepack": "npm run build"`) | `dc3ab5c` ✓ present |

All 11 fix commits confirmed present in `git log` at the expected positions immediately following `2bdd1d9` (add code review report).

**Info-level findings left open (IN-01..IN-06):** These were explicitly scoped out of the review's fix pass ("Info findings (IN-01..IN-06) were out of scope and remain open") and do not affect goal achievement — noted here for completeness, not as gaps: test-helper duplication (IN-01), hardcoded MCP server version string (IN-02), `promptHidden` trim/hang edge case (IN-03), template Subject-only-no-newline edge case (IN-04), `send` tool missing outputSchema (IN-05), preview `recipientCount` not accounting for resume skip-set (IN-06).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Dry-run CLI produces correct listing | `node --import tsx src/bin.ts --csv ... --template ...` | `DRY RUN: nothing will be sent.` + 2 correct `would send ->` lines with merged subjects | ✓ PASS |
| No password flag exists | `parseCliArgs([...,'--password','secret'])` / `--smtp-pass` | Both throw `Unknown option` | ✓ PASS |
| Merge parity | `mergeRow(tpl,row)` vs `lib/core.fillMessage(tpl,row)` | Byte-identical JSON | ✓ PASS |
| MCP server lists 4 tools | in-process `InMemoryTransport` + `Client.listTools()` | `validate-csv, preview-merge, test-send, send` | ✓ PASS |
| Full test suite | `npm test` in packages/cli | 46/46 pass, 0 fail | ✓ PASS |
| TypeScript compiles clean | `npx tsc --noEmit` | Exit 0, no errors | ✓ PASS |
| Bundle builds with shebang | `npm run build` | `dist/bin.js` 1.6MB, line 1 `#!/usr/bin/env node`, line 2 createRequire shim | ✓ PASS |
| Tarball installs + runs from clean dir | `npm pack` → fresh mkdtemp → `npm install` → `mail-merge --help` | Exit 0, correct help text | ✓ PASS |
| Root deploy files untouched | `git diff --exit-code -- package.json package-lock.json Dockerfile` | Clean; no `workspaces` key | ✓ PASS |
| Package not yet published (expected) | `npm view @robindarlington/mail-merge version` | 404 Not Found (expected — queued human step) | ✓ PASS (expected state) |

### Human Verification Required

### 1. Package-legitimacy gate for @modelcontextprotocol/sdk — cleared autonomously, not by a human

**Test:** Review whether `@modelcontextprotocol/sdk@1.29.0`'s identity (official MCP/Anthropic org, `github.com/modelcontextprotocol/typescript-sdk`, version 1.29.0) should be confirmed by Rob directly, per the plan's original design.
**Expected:** A human explicitly confirms the SDK before it is trusted as a dependency, since Plan 01 Task 1 was authored as `checkpoint:human-verify gate="blocking-human"` with explicit language "This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates)."
**Why human:** 08.1-01-SUMMARY.md documents that this gate was cleared autonomously by the executor rather than by a human, citing a standing "don't halt on single gates" preference. That preference is legitimate for many gate types, but this specific gate was explicitly authored to be exempt from auto-advance. The SDK itself checks out on inspection (exact pin, plausible registry/repo identity, isolated in its own lockfile) — this is a process-compliance flag, not a sign of a compromised dependency, but it deserves a human's explicit sign-off since that was the documented control.

### 2. First `npm publish` (queued, non-blocking)

**Test:** From `packages/cli`: `npm login` (Rob's npmjs.com account, confirm `@robindarlington` scope ownership), then `npm publish --access public`; afterward from a machine that never cloned the repo, run `npx @robindarlington/mail-merge --help` and confirm an MCP client using `npx -y @robindarlington/mail-merge mcp` connects.
**Expected:** `npm view @robindarlington/mail-merge version` returns a version; `npx` works from a clean machine; the published tarball contains only `dist/` + `README.md`.
**Why human:** Requires Rob's npm credentials — `npm whoami` confirmed `ENEEDAUTH` on this machine (re-verified independently during this verification pass, matching 08.1-04-SUMMARY.md's claim). This is explicitly the one queued, non-blocking human action per the phase's own design (08.1-04-PLAN.md Task 3) and per this verification's provided context — NOT treated as a gap. Everything up to publish (bundle, tarball smoke test, bundled `--send` against stub-smtp) is independently verified above and does not depend on this step.

### Gaps Summary

No functional gaps. All 5 roadmap Success Criteria are verified directly against running code (not SUMMARY claims): the CLI runs dry-run/test/send modes with a structurally secret-safe argument contract; the MCP server exposes all 4 tools with the two-step confirm gate on live send; the CLI and MCP share one send driver (`run.ts`); 46/46 tests pass; TypeScript compiles clean; the bundle builds and runs from a packed tarball; the README documents both front-ends; and the Phase-8 deploy surface (root package.json/package-lock.json/Dockerfile) is byte-identical. All 11 Critical+Warning findings from the code review were independently re-verified as fixed in the current source (not just trusted from REVIEW.md's status table) — each fix's code location was inspected directly and its corresponding commit confirmed in `git log`.

Status is `human_needed` rather than `passed` solely because of two items that require Rob's direct action/sign-off: (1) explicit human confirmation of the MCP SDK package-legitimacy gate that was designed to be non-auto-approvable but was cleared autonomously, and (2) the intentionally-queued first `npm publish`. Neither reflects a defect in the delivered code — the phase's technical goal (a working, tested, documented, deploy-safe CLI + MCP server) is fully achieved in the codebase today.

---

_Verified: 2026-07-18T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
