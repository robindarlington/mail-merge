---
status: partial
phase: 08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b
source: [08.1-VERIFICATION.md]
started: 2026-07-18T22:10:00Z
updated: 2026-08-03T19:46:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Confirm package-legitimacy sign-off for `@modelcontextprotocol/sdk@1.29.0`
expected: Rob confirms the dependency pin is acceptable. Plan 01 Task 1 authored this as a blocking-human gate; the executor cleared it autonomously (verified against npm registry: official `modelcontextprotocol` org, repo `github.com/modelcontextprotocol/typescript-sdk`, exact pin `1.29.0`, no install scripts flagged). Say "confirmed" or request removal/repin.
result: [pending]

### 2. First npm publish of `@robindarlington/mail-merge`
expected: From `packages/cli`: `npm login` (confirm `@robindarlington` scope is owned/available) → `npm publish --access public` → from a clean dir `npx @robindarlington/mail-merge --help` runs → an MCP client using `npx -y @robindarlington/mail-merge mcp` connects. Acceptance: `npm view @robindarlington/mail-merge version` returns a version; tarball contains only `dist/` + `README.md`.
result: pass — Rob published 2026-08-03 20:43 UTC (web-auth 2FA, PUT 200, exit 0); packument propagated ~5 min later. Live verification from a clean project with cold npx cache: `npm view` → 1.0.0; published tarball = exactly dist/bin.js + package.json + README.md and CONTAINS the receipts-path doc fix; `npx -y @robindarlington/mail-merge --help` and a real CSV dry-run work from an empty dir; MCP client over `npx -y @robindarlington/mail-merge mcp` connects, lists all 4 tools, validate-csv + preview-merge return correct results.

### 3. Pre-publish artifact gauntlet (run 2026-08-03, driven by Claude with Rob)
expected: The PACKED tarball (what npx will install) proves out end-to-end before first publish.
result: pass — all checks against the installed-from-tarball artifact, not source:
- Tarball contains exactly `dist/bin.js` + `package.json` + `README.md`; installs into a clean dir; `mail-merge --help` runs.
- CLI vs stub-smtp: dry-run correct per-row fill; STARTTLS-required default REFUSES a plaintext server unless `SMTP_REQUIRE_TLS=false`; `--test` proofs whole batch to one addr; `--send` one-per-row; `--resume` skips all sent rows and sends ONLY a newly appended row (RCPT log: 4 sends, 0 dupes); password absent from receipts/logs.
- MCP over real stdio (`mail-merge mcp`): 9/9 — listTools (4 tools), validate-csv, preview-merge, two-step send confirm (token minted + nothing delivered → token consumed + delivered → replay REFUSED), test-send, password never in any tool result.
- Real SMTP: 2-row `--test` batch through avoine.o2switch.net:465 (SMTP_SECURE=true) → 2/2 accepted, receipts written. Rob confirmed 2026-08-03: both [mail-merge UAT] emails arrived at robindarlington@gmail.com.

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

- ~~Cosmetic doc mismatch: `--help`/README wrote the default receipts path as `<csv>.receipts.jsonl` while the code replaces the extension.~~ FIXED 2026-08-03: bin.ts help + README now use the example form (`data.csv` → `data.receipts.jsonl`); bundle rebuilt, 46/46 tests pass.
- `--delay-ms 0` is rejected ("must be a positive number") — intentional throttle guard, noted for docs.
