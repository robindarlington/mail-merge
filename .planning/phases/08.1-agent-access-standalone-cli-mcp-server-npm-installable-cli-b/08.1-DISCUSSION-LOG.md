# Phase 08.1: Agent access — standalone CLI + MCP server - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-18
**Phase:** 08.1-agent-access-standalone-cli-mcp-server
**Areas discussed:** Packaging & npm identity, Send safety for agents, CLI flag/config contract, Send receipts & resume

---

## Packaging & npm identity

| Option | Description | Selected |
|--------|-------------|----------|
| Separate workspace package | packages/cli with own package.json; esbuild-bundles lib/core; repo becomes npm workspace | ✓ |
| Bin entry in main package | Simplest wiring but npx pulls the whole web app tree | |
| Separate repo | Cleanest install but loses lib/core single-source reuse | |

| Option | Description | Selected |
|--------|-------------|----------|
| @robindarlington/mail-merge | Scoped — always available, brand tie; bin stays `mail-merge` | ✓ |
| Unscoped alternative name | mailmerge-cli etc. — availability lottery, weaker brand | |
| You decide | Claude picks after availability check | |

| Option | Description | Selected |
|--------|-------------|----------|
| Publish to npm | npx just works for phase 9; first publish = queued human step (Rob's npm login) | ✓ |
| npx from GitHub only | No registry needed; slower, workspace complications | |
| Local-only this phase | Defer publishing to phase 9 | |

---

## Send safety for agents

| Option | Description | Selected |
|--------|-------------|----------|
| Two-step confirm token | send returns preview + one-time token; executes only on second call with token | ✓ |
| Explicit count echo | Single call with confirm:true + exact expected count | |
| No extra gate | Tool split (test-send vs send) is the guardrail | |

| Option | Description | Selected |
|--------|-------------|----------|
| Default cap, overridable | e.g. 100 unless raised explicitly | |
| No cap | Confirm gate + SMTP provider are the limiters | ✓ |
| Hard cap | Fixed ceiling, no override | |

| Option | Description | Selected |
|--------|-------------|----------|
| 3s like the original script | DELAY_MS 3000, overridable | ✓ |
| 1s like the web worker | Matches deployed SEND_DELAY_MS default | |
| You decide | | |

---

## CLI flag/config contract

| Option | Description | Selected |
|--------|-------------|----------|
| Env for SMTP, flags for files | SMTP_* env (+ hidden prompt fallback), --csv/--template flags, dry-run default, --test, --send | ✓ |
| Full flag modernization | Everything as flags (password still env-only) | |
| Config file | mailmerge.config.json | |

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-detect + --email-column override | lib/core detectEmailColumn, same as web | ✓ |
| Always explicit | Require --email-column every run | |

| Option | Description | Selected |
|--------|-------------|----------|
| Defer attachments to a later phase | Text-only contract first; note as deferred | ✓ |
| Include now | --attachment-column with CSV-relative paths | |

---

## Send receipts & resume

| Option | Description | Selected |
|--------|-------------|----------|
| JSONL receipts + --resume | Per-recipient outcome lines; --resume skips already-sent | ✓ |
| Log lines only | Original-script parity, nothing persisted | |
| Receipts, no resume | Audit record only | |

| Option | Description | Selected |
|--------|-------------|----------|
| On by default | Derived path (csv → .receipts.jsonl), --receipts/--no-receipts | ✓ |
| Opt-in via --receipts | Only when asked | |

---

## Claude's Discretion

MCP tool result shapes / error style, flag help text, workspace tooling details,
test harness layout, npm publish mechanics, MCP registration snippet format.

## Deferred Ideas

Per-row attachments for CLI/MCP (later phase; reuse lib/core resolveAttachmentColumn).
Six keyword-matched todos reviewed and not folded (all web-app scope — see CONTEXT.md).
