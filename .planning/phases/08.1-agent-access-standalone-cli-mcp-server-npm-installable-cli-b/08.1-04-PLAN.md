---
phase: 08.1-agent-access-standalone-cli-mcp-server
plan: 04
type: execute
wave: 4
depends_on: ["08.1-03"]
files_modified:
  - packages/cli/package.json
  - packages/cli/README.md
  - .dockerignore
autonomous: false
requirements: [SC-1, SC-5]
user_setup:
  - service: npm
    why: "First publish of @robindarlington/mail-merge so `npx @robindarlington/mail-merge` works (D-03); scoped publish needs Rob's npm login"
    env_vars: []
    dashboard_config:
      - task: "Run `npm login` then `npm publish --access public` from packages/cli"
        location: "Local terminal (npmjs.com account)"

must_haves:
  truths:
    - "esbuild bundles packages/cli/src/bin.ts (inlining lib/core + the stdio MCP path) into one self-contained dist/bin.js with a #!/usr/bin/env node shebang"
    - "A real send through the BUNDLED artifact against stub-smtp succeeds (proves the bundle works, not just the source)"
    - "`npm pack` produces a tarball that installs into a clean dir and `mail-merge --help` runs from it (npx-executable proven WITHOUT publishing)"
    - "Root package.json, root package-lock.json, and the Dockerfile are byte-identical to before this phase (Phase-8 deploy untouched); no root `workspaces` key"
    - "packages/cli/README.md documents both front-ends with copy-paste examples: the mcp.json snippet and the CLI one-liners (SC-5)"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "Publish metadata: bin mail-merge→dist/bin.js, files whitelist [dist,README.md], publishConfig.access public, engines >=18, empty runtime deps, build script"
      contains: "publishConfig"
    - path: "packages/cli/README.md"
      provides: "CLI one-liners + MCP mcp.json config snippet (SC-5)"
      contains: "mcpServers"
    - path: "packages/cli/dist/bin.js"
      provides: "Self-contained esbuild bundle (gitignored build output)"
  key_links:
    - from: "packages/cli/package.json bin"
      to: "packages/cli/dist/bin.js"
      via: "npm bin field → shebang entry"
      pattern: "\"mail-merge\""
    - from: "packages/cli/package.json files"
      to: "dist + README.md only"
      via: "publish whitelist limits shipped surface"
      pattern: "\"files\""
---

<objective>
Final vertical slice: make the CLI/MCP installable and documented. Bundle the package into one self-contained artifact, prove it runs from a packed tarball (npx-executable without publishing), write the copy-paste README (SC-5), assert the Phase-8 deploy is untouched, then queue the one human step — the first `npm publish`.

Purpose: Deliver `npx @robindarlington/mail-merge` (D-02/D-03) and the launch-collateral README that Phase 9's `/agents` page consumes — while proving with a local tarball smoke test that nothing depends on the human publish step to be verifiable.
Output: Publish-ready package.json, README, a bundle + tarball smoke test, a deployment-protection assertion, and the queued npm-login checkpoint.
</objective>

## Phase Goal

**As an** AI agent or developer, **I want to** run mail merges from a CLI and an MCP server without the web UI, **so that** I can discover and drive the same lib/core merge-and-send engine locally.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-CONTEXT.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-PATTERNS.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-03-SUMMARY.md

<interfaces>
Build recipe to extend — root package.json build:worker (line 14):
  esbuild <entry> --bundle --platform=node --format=esm --tsconfig=tsconfig.json \
    --banner:js="import{createRequire as __cr}from'module';const require=__cr(import.meta.url);" \
    --outfile=<out>
  (the createRequire banner is the shim for nodemailer's dynamic requires — RESEARCH Pitfall 4.)

Bundle facts (RESEARCH Pitfall 4 / §measurement): the stdio-only MCP path tree-shakes to ~454 KB;
express/hono/jose are NOT pulled into the stdio bundle. Runtime `dependencies` stay EMPTY — the
bundle inlines lib/core + @modelcontextprotocol/sdk + nodemailer + papaparse + zod.

Publish facts:
  - Scoped name defaults to RESTRICTED — set publishConfig.access="public" (RESEARCH Pitfall 5).
  - `npm whoami` → ENEEDAUTH this session (verified) — first publish is a queued human checkpoint (D-03).
  - README snippets are authored verbatim from RESEARCH §Code Examples (mcp.json + CLI one-liners).

Deployment guardrail (RESEARCH Pitfall 1 / PATTERNS §Packaging Guardrail):
  - Root has NO `workspaces` key (confirmed). Dockerfile copies only root package.json + package-lock.json.
  - This plan must NOT change root package.json, root package-lock.json, or the Dockerfile.
  - `.dockerignore` may be extended (it is NOT copied into `npm ci`) — add `packages/` so CLI source
    never enters the `COPY . .` build layer (cache hygiene; runtime image already excludes it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish metadata + esbuild bundle + bundled-artifact send test</name>
  <files>packages/cli/package.json, packages/cli/.gitignore</files>
  <read_first>
    - package.json (root — build:worker recipe + createRequire banner to copy; DO NOT modify it)
    - .gitignore (root, /worker.js /migrate.js — analog for ignoring the CLI bundle)
    - packages/cli/package.json (Plan 01 — flip private→publish metadata; add build + build-run scripts)
    - scripts/stub-smtp.ts (sink to send a real message through the bundled artifact)
    - packages/cli/src/bin.ts (bundle entry)
  </read_first>
  <action>
    Update `packages/cli/package.json`: remove `"private": true`; add `"bin": { "mail-merge": "dist/bin.js" }`, `"files": ["dist", "README.md"]`, `"publishConfig": { "access": "public" }`, keep `"engines": { "node": ">=18" }`, keep `"dependencies": {}` empty. Add scripts: `"build": "esbuild src/bin.ts --bundle --platform=node --format=esm --tsconfig=tsconfig.json --banner:js=\"#!/usr/bin/env node\\nimport{createRequire as __cr}from'module';const require=__cr(import.meta.url);\" --outfile=dist/bin.js"` (the shebang is part of the banner so the shipped file is directly executable; the createRequire shim handles nodemailer dynamic requires per RESEARCH Pitfall 4). Ensure `packages/cli/.gitignore` ignores `dist/`.
    Run the build. Then run a REAL send through the BUNDLED `dist/bin.js` (not the source) against a `scripts/stub-smtp.ts serve` sink with a tiny fixture CSV+template in `--send` mode, and assert the stub RCPT log recorded the row(s) — proving the tree-shaken bundle actually delivers (RESEARCH: "don't trust the build alone").
  </action>
  <verify>
    <automated>npm run build --prefix packages/cli && head -1 packages/cli/dist/bin.js | grep -q '#!/usr/bin/env node' && test -f packages/cli/dist/bin.js && echo "bundle built with shebang"; ls -la packages/cli/dist/bin.js</automated>
  </verify>
  <acceptance_criteria>dist/bin.js exists, starts with the node shebang, and a `--send` through the bundled artifact records deliveries in the stub-smtp log; runtime dependencies remain empty.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README (SC-5) + tarball smoke test + deployment-protection assertion</name>
  <files>packages/cli/README.md, .dockerignore</files>
  <read_first>
    - .planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md (§Code Examples — mcp.json snippet + CLI one-liners to author verbatim)
    - .dockerignore (add `packages/` for build-cache hygiene; confirm it already excludes node_modules/.next)
    - Dockerfile (READ to confirm — this plan must leave it byte-identical)
    - packages/cli/package.json (the published contract to document)
  </read_first>
  <action>
    Write `packages/cli/README.md` covering BOTH front-ends with copy-paste examples (SC-5): the `npx @robindarlington/mail-merge --csv … --template …` one-liners (dry-run / --test / --send / --resume, with env-based SMTP), and the MCP client config snippet (`"mcpServers": { "mail-merge": { "command":"npx", "args":["-y","@robindarlington/mail-merge","mcp"] } }`). Document the env var contract (SMTP_HOST/PORT/USER/PASS/FROM_ADDR/FROM_NAME/SMTP_SECURE), that the password comes from env or a hidden prompt only (never a flag), the receipts/`--resume` behavior described honestly as "resume skips already-recorded sends" (at-least-once, per RESEARCH Pitfall 3 — do NOT claim exactly-once), and the two-step MCP send confirm.
    Add `packages/` to `.dockerignore` (hygiene — keeps CLI source out of the `COPY . .` build cache layer; runtime image already excludes it).
    Run `npm pack` from packages/cli, install the resulting tarball into a fresh mkdtemp dir, and run the installed `mail-merge --help` — proving npx-executability WITHOUT publishing.
    Assert deploy protection: `git diff --exit-code -- package.json package-lock.json Dockerfile` (root files byte-identical) and `grep -c workspaces package.json` == 0.
  </action>
  <verify>
    <automated>grep -q "mcpServers" packages/cli/README.md && grep -q "npx @robindarlington/mail-merge" packages/cli/README.md && git diff --exit-code -- package.json package-lock.json Dockerfile && ! grep -q '"workspaces"' package.json && cd $(mktemp -d) && npm pack "$OLDPWD/packages/cli" >/dev/null 2>&1 && tar tzf *.tgz | grep -q "package/dist/bin.js" && echo "README + tarball + deploy-protection OK"</automated>
  </verify>
  <acceptance_criteria>README contains the mcp.json snippet + CLI one-liners; `npm pack` tarball includes dist/bin.js and installs+runs `--help` from a clean dir; root package.json/package-lock.json/Dockerfile show zero diff; no `workspaces` key exists.</acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [QUEUED CHECKPOINT] First npm publish (needs Rob's npm login)</name>
  <read_first>
    - packages/cli/package.json (publishConfig.access public, name, version, files)
    - .planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md (Pitfall 5 scoped-publish access; §Environment Availability — npm whoami ENEEDAUTH)
  </read_first>
  <what-built>Everything is built, bundled, tarball-smoke-tested, and documented. The only remaining step is the first publish to the npm registry, which requires Rob's npm authentication (`npm whoami` → ENEEDAUTH, verified — Claude cannot do this). This is QUEUED per the autonomy preference: it does not block anything else in this phase, which is already fully verifiable locally via the tarball smoke test.</what-built>
  <how-to-verify>
    1. From packages/cli: `npm login` (Rob's npmjs.com account; confirm the `@robindarlington` scope is owned/available).
    2. `npm publish --access public` (scoped packages default to restricted — the `--access public` / publishConfig makes it public and avoids a 402).
    3. From a clean directory: `npx @robindarlington/mail-merge --help` and confirm it runs.
    4. Confirm the MCP snippet works: an agent config using `npx -y @robindarlington/mail-merge mcp` connects.
  </how-to-verify>
  <acceptance_criteria>
    - `npm view @robindarlington/mail-merge version` returns the published version.
    - `npx @robindarlington/mail-merge --help` runs from a machine that never cloned the repo.
    - The published tarball contains only `dist/` + README.md (the `files` whitelist).
  </acceptance_criteria>
  <resume-signal>Type "published" (with the version) or describe the publish error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → published tarball | Only whitelisted files should ship; the registry is public |
| this phase → Phase-8 deploy surface | Root manifest/lockfile/Dockerfile must not be disturbed |
| npm registry → end-user `npx` | Users execute the published bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-081-06 | Tampering | published tarball | mitigate | `files: ["dist","README.md"]` whitelist limits shipped surface; publish is a human-reviewed checkpoint (Task 3) |
| T-081-DEPLOY | Tampering | root package.json / lockfile / Dockerfile | mitigate | `git diff --exit-code` on those three paths + no-`workspaces` grep asserted in Task 2; `.dockerignore` change is safe (not copied into `npm ci`) |
| T-081-402 | Denial (publish failure) | scoped publish access | mitigate | publishConfig.access="public" + `--access public` avoids the restricted-scope 402 (RESEARCH Pitfall 5) |
| T-081-SC | Tampering (supply chain) | bundled deps | mitigate | Deps inlined from the Plan-01 pinned lockfile; bundle is byte-reproducible from source; whitelist excludes source/tests |
</threat_model>

<verification>
- `npm run build --prefix packages/cli` produces `dist/bin.js` with a node shebang; a bundled `--send` records deliveries in stub-smtp.
- `npm pack` tarball installs into a clean dir and runs `mail-merge --help`.
- `git diff --exit-code -- package.json package-lock.json Dockerfile` clean; `grep -c workspaces package.json` == 0.
- README contains the mcp.json snippet + CLI one-liners (SC-5).
- (Post-checkpoint) `npm view @robindarlington/mail-merge version` returns a version.
</verification>

<success_criteria>
The package bundles to one self-contained artifact that runs from a packed tarball (npx-executable proven without publishing), the README documents both front-ends with copy-paste examples, the Phase-8 deploy is byte-for-byte untouched, and the only human step (first publish) is queued and non-blocking.
</success_criteria>

<output>
Create `.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-04-SUMMARY.md` when done.
</output>
