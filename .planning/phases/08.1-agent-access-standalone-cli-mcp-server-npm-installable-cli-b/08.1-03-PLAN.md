---
phase: 08.1-agent-access-standalone-cli-mcp-server
plan: 03
type: execute
wave: 3
depends_on: ["08.1-02"]
files_modified:
  - packages/cli/src/mcp.ts
  - packages/cli/src/bin.ts
  - packages/cli/test/mcp.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

must_haves:
  truths:
    - "An MCP client connected over stdio (`mail-merge mcp`) can list four tools: validate-csv, preview-merge, test-send, send"
    - "validate-csv returns typed structured results (columns, rowCount, detectedEmailColumn, invalidEmailCount) reusing lib/core parseCsv/detectEmailColumn — same paths as the CLI"
    - "preview-merge returns the per-row merged subject/body via lib/core.fillMessage (merge parity with the CLI)"
    - "The `send` tool is gated by a two-step confirm token: the first call returns a preview summary + one-time token and delivers NOTHING; delivery happens only on a second call presenting that token"
    - "test-send and send delegate to the shared run.ts driver — the MCP and CLI send paths cannot diverge"
  artifacts:
    - path: "packages/cli/src/mcp.ts"
      provides: "buildServer() → McpServer with 4 registerTool calls + in-memory confirm-token Map"
      exports: ["buildServer"]
    - path: "packages/cli/src/bin.ts"
      provides: "`mail-merge mcp` subcommand → connect StdioServerTransport"
    - path: "packages/cli/test/mcp.test.ts"
      provides: "In-process InMemoryTransport + Client tests for all 4 tools + two-step token (SC-3/D-04)"
  key_links:
    - from: "packages/cli/src/mcp.ts"
      to: "@modelcontextprotocol/sdk/server/mcp.js (McpServer, registerTool)"
      via: "registerTool(name, {description, inputSchema}, cb) with raw zod shape"
      pattern: "registerTool"
    - from: "packages/cli/src/mcp.ts"
      to: "packages/cli/src/run.ts (runSend) + lib/core (parseCsv, detectEmailColumn, fillMessage)"
      via: "tools delegate to the same engine + shared driver as the CLI"
      pattern: "runSend|parseCsv|fillMessage"
    - from: "packages/cli/src/bin.ts"
      to: "@modelcontextprotocol/sdk/server/stdio.js (StdioServerTransport)"
      via: "server.connect(new StdioServerTransport())"
      pattern: "StdioServerTransport"
---

<objective>
Third vertical slice: expose the identical merge/send engine to AI agents over a stdio MCP server. Register four tools (validate-csv, preview-merge, test-send, send) on an `McpServer`, gate live `send` behind a two-step confirm token, and dispatch `mail-merge mcp` to a StdioServerTransport. After this slice an agent can discover and drive the whole pipeline programmatically, with the same lib/core paths and the same shared send driver as the CLI.

Purpose: Satisfy SC-3 (MCP tools with typed results reusing lib/core) and the D-04 agent-safety gate, without duplicating any merge/send logic — the tools are thin adapters over lib/core + run.ts.
Output: `mcp.ts` (buildServer factory), the `mcp` subcommand dispatch, and in-process MCP tests (socket-free, InMemoryTransport).
</objective>

## Phase Goal

**As an** AI agent or developer, **I want to** run mail merges from a CLI and an MCP server without the web UI, **so that** I can discover and drive the same lib/core merge-and-send engine locally.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-CONTEXT.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-PATTERNS.md
@.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-02-SUMMARY.md

<interfaces>
MCP SDK 1.29.0 — VERIFIED import paths + signatures (RESEARCH Pattern 1). Do NOT follow the
main-branch v2 README (`@modelcontextprotocol/server`, `zod/v4`) — that package is unreleased.
  import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
  import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
  // registerTool(name, { description, inputSchema, outputSchema?, annotations? }, cb)
  //   inputSchema / outputSchema are RAW ZOD SHAPES ({ key: z.string() }), NOT z.object(...).
  //   Use registerTool — the older `server.tool(...)` overloads are @deprecated in 1.29.0.
  // Tool result: { content: [{ type:"text", text }], structuredContent?, isError? }

Test harness (in-process, no sockets) — RESEARCH §In-process MCP test:
  import { Client } from "@modelcontextprotocol/sdk/client/index.js";
  import { InMemoryTransport } from "@modelcontextprotocol/sdk/inMemory.js";
  const [clientT, serverT] = InMemoryTransport.createLinkedPair();
  await buildServer().connect(serverT); await client.connect(clientT);
  await client.callTool({ name, arguments });

Reuse (do NOT re-implement):
  lib/core: parseCsv(text) → {columns,rows,invalidEmailCount}; detectEmailColumn(cols,rows); fillMessage(tpl,row).
  packages/cli/src/run.ts: runSend(opts) — the shared driver for test-send/send.

Result-shape + logging discipline (analog): lib/compose/actions-core.ts — typed closed-union
{ ok:true; data } | { ok:false; error }; NEVER console.* a subject/body/CSV cell; an error `raw`
field is ALWAYS a string.
</interfaces>

<mcp_input_note>
Per RESEARCH Security Domain: validate-csv and preview-merge take CSV **text** (a `csv` string arg),
NOT a filesystem path — this keeps the stdio server from reaching into the host FS on an agent's
behalf (V4 access control). test-send/send accept the SMTP config + template + CSV text via typed
inputSchema; the SMTP password is supplied through the MCP param object by the operator's own agent
config, never logged, and never echoed back in a tool result.
</mcp_input_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildServer() + read-only tools (validate-csv, preview-merge) + in-process tests</name>
  <files>packages/cli/src/mcp.ts, packages/cli/test/mcp.test.ts</files>
  <read_first>
    - .planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md (Pattern 1 registerTool + §In-process MCP test + §Code Examples)
    - lib/core/csv.ts (parseCsv/detectEmailColumn/countInvalidEmails outputs)
    - lib/core/fill.ts (fillMessage for preview-merge)
    - lib/compose/actions-core.ts (typed-result + never-log-cell discipline to mirror)
    - packages/cli/test/args.test.ts (node:test idiom already used in this package)
  </read_first>
  <behavior>
    - client.listTools() includes "validate-csv" and "preview-merge" with descriptions and input schemas.
    - callTool validate-csv { csv:"email\na@b.com\nbad\n" } → structuredContent { columns:["email"], rowCount:2, detectedEmailColumn:"email", invalidEmailCount:1 } (values via lib/core, not re-computed).
    - callTool preview-merge { csv, subject:"Hi {{name}}", body:"Code {{code}}", limit?:number } → structuredContent { rows:[{to, subject, body}, ...] } where subject/body come from fillMessage; honors an optional row limit.
    - A malformed input (missing required arg) is refused by the SDK's zod inputSchema validation (isError or a schema rejection) — not a crash.
  </behavior>
  <action>
    Create `packages/cli/src/mcp.ts` exporting `buildServer(): McpServer`. Construct `new McpServer({ name:"mail-merge", version:"1.0.0" })`. Register `validate-csv` with `inputSchema:{ csv: z.string().describe(...) }` and an `outputSchema` (raw zod shape) for typed results; the callback runs `parseCsv(csv)` + `detectEmailColumn(columns, rows)` + `countInvalidEmails` and returns `{ content:[{type:"text", text: JSON.stringify(result)}], structuredContent: result }`. Register `preview-merge` with `inputSchema:{ csv, subject, body, limit?:z.number().optional() }`; run `parseCsv` then `fillMessage({subject,body}, row)` per row (up to limit), return structuredContent `{ rows:[{to,subject,body}] }`. NEVER console.* a CSV cell / subject / body; on error return `{ content:[{type:"text",text}], isError:true }` with a string message. Export `buildServer` so the test can connect it in-process.
    Write `packages/cli/test/mcp.test.ts` using `InMemoryTransport.createLinkedPair()` + `Client` (no sockets) covering listTools + the two read-only tools per <behavior>.
  </action>
  <verify>
    <automated>node --import tsx --test packages/cli/test/mcp.test.ts 2>&1 | grep -E "validate-csv|preview-merge"</automated>
  </verify>
  <acceptance_criteria>buildServer() lists validate-csv + preview-merge; both return typed structuredContent computed by lib/core; in-process tests pass with no socket opened.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test-send + send tools with two-step confirm token (D-04)</name>
  <files>packages/cli/src/mcp.ts, packages/cli/test/mcp.test.ts</files>
  <read_first>
    - .planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md (Pattern 3 two-step confirm token)
    - packages/cli/src/run.ts (runSend contract — Plan 02)
    - packages/cli/src/secrets.ts (SmtpConfig assembly shape — reuse the type, but MCP receives config via params)
    - scripts/stub-smtp.ts (send sink for the MCP send test)
    - packages/cli/src/mcp.ts (Task 1 buildServer to extend)
  </read_first>
  <behavior>
    - test-send { csv, subject, body, smtp:{host,port,secure,user,pass}, from, testAddr } → delegates to run.ts test mode against stub-smtp; structuredContent reports { attempted, sent, failed }; the `pass` never appears in the result or any log.
    - send FIRST call { csv, subject, body, smtp, from } with NO token → returns structuredContent { preview:{ recipientCount, subject, from, delayMs }, confirmToken } and delivers NOTHING (stub RCPT log unchanged).
    - send SECOND call with the returned confirmToken → delegates to run.ts live mode; delivers one-per-row; the token is consumed (a replay of the same token is refused).
    - send with a wrong/absent token when one is required → refused (isError, no delivery).
  </behavior>
  <action>
    Extend `mcp.ts`: add a process-local `Map<string, {paramsHash, createdAt}>` for confirm tokens (RESEARCH Pattern 3 — one stdio server = one agent session, no persistence). Register `test-send` (inputSchema includes the SMTP config + template + csv + testAddr; delegate to `runSend({mode:"test", ...})` reusing Plan 02's driver; return counts). Register `send`: on a call WITHOUT a valid `confirmToken`, compute a preview (recipientCount from parseCsv, subject, from, delayMs), mint a one-time token (crypto.randomUUID) keyed to a hash of the send params, store it, and return the preview + token WITHOUT sending. On a call WITH a matching token, delete it (consume) and `runSend({mode:"live", ...})`; a non-matching/expired token → isError, no send. NEVER include `pass` in preview/result/logs; error `raw` is always a string. Keep both send tools delegating to run.ts — no second send loop.
    Extend `mcp.test.ts` with stub-smtp-backed cases for all four <behavior> items incl. the two-step token + replay refusal.
  </action>
  <verify>
    <automated>node --import tsx --test packages/cli/test/mcp.test.ts</automated>
  </verify>
  <acceptance_criteria>send first-call returns a token and delivers nothing; second call with the token delivers one-per-row and consumes the token; wrong/absent token is refused; test-send routes through run.ts; the SMTP password is absent from every tool result and log line.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire `mail-merge mcp` subcommand → StdioServerTransport</name>
  <files>packages/cli/src/bin.ts</files>
  <read_first>
    - packages/cli/src/bin.ts (Plan 01/02 dispatch; the `argv[0]==="mcp"` stub to replace)
    - packages/cli/src/mcp.ts (buildServer)
    - .planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-RESEARCH.md (Pattern 1 connect StdioServerTransport)
  </read_first>
  <action>
    Replace the `mcp` stub in bin.ts: when `argv[0] === "mcp"`, `const server = buildServer(); await server.connect(new StdioServerTransport());` and do NOT print to stdout (stdout is the JSON-RPC channel — any banner must go to stderr). Keep the CLI dispatch (dry/test/live) untouched. Ensure the top-level ERROR catch writes to stderr only.
  </action>
  <verify>
    <automated>node --import tsx packages/cli/src/bin.ts mcp </dev/null & MCP_PID=$!; sleep 1; kill $MCP_PID 2>/dev/null; echo "mcp subcommand started without crashing on stdout"; node --import tsx --test packages/cli/test/mcp.test.ts</automated>
  </verify>
  <acceptance_criteria>`mail-merge mcp` starts a stdio MCP server that connects without writing to stdout; the full mcp.test.ts suite is green.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (stdio JSON-RPC) → MCP server | Untrusted tool arguments cross here; validated by zod inputSchema |
| MCP send tool → user's SMTP | Agent-initiated live delivery — the abuse surface |
| tool params → SMTP password | Password arrives in a tool param object; must never be echoed/logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-081-04 | Elevation / abuse | mcp.ts `send` tool | mitigate | Two-step confirm token (D-04): first call returns preview + one-time token, no delivery; delivery only on second call with the token; per-send throttle from run.ts |
| T-081-05 | Input validation | registerTool inputSchema | mitigate | Every tool has a zod inputSchema; malformed args refused by the SDK before the callback runs |
| T-081-02 | Information disclosure | tool results / logs | mitigate | `pass` never in structuredContent, preview, or logs; error `raw` is a string; no console.* of CSV cells/subject/body (actions-core discipline) |
| T-081-FS | Access control | validate-csv / preview-merge | mitigate | These tools take CSV TEXT, not a path — the stdio server never reads the host FS on the agent's behalf |
| T-081-SDK | Tampering (supply chain) | @modelcontextprotocol/sdk import | mitigate | Pinned 1.29.0 (Plan 01 legitimacy gate); verified 1.29.0 import paths, not the unreleased v2 README |
</threat_model>

<verification>
- `node --import tsx --test packages/cli/test/mcp.test.ts` green (all four tools + two-step token).
- `grep -RnE "structuredContent|preview" packages/cli/src/mcp.ts | grep -i pass` returns nothing (no password in results).
- `mail-merge mcp` writes nothing to stdout before the JSON-RPC handshake.
</verification>

<success_criteria>
A stdio MCP client can list and call validate-csv / preview-merge / test-send / send with typed results computed by lib/core; live `send` requires a two-step confirm token; the MCP and CLI send paths share run.ts; the SMTP password never leaks into a tool result or log.
</success_criteria>

<output>
Create `.planning/phases/08.1-agent-access-standalone-cli-mcp-server-npm-installable-cli-b/08.1-03-SUMMARY.md` when done.
</output>
