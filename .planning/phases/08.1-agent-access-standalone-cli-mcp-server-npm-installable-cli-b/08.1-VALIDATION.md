---
phase: 08.1
slug: agent-access-standalone-cli-mcp-server
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-18
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 08.1-RESEARCH.md "## Validation Architecture" — the planner fills the
> per-task map; success criteria map to: CLI contract tests (node:test, parseArgs),
> merge parity vs lib/core, MCP tool tests over InMemoryTransport.createLinkedPair()
> + Client (in-process, socket-free), and live send-path tests against the existing
> scripts/stub-smtp.ts sink (receipts + resume assertions).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (Node built-in — matches repo; sub-package runs its own suite) |
| **Config file** | none — packages/cli/package.json `test` script (Wave 0 scaffolds) |
| **Quick run command** | `npm test --prefix packages/cli` |
| **Full suite command** | `npm test && npm test --prefix packages/cli` |
| **Estimated runtime** | ~15 seconds (root 380+ tests ≈ 12s + cli suite ≈ 3s) |

---

## Sampling Rate

- **After every task commit:** Run `npm test --prefix packages/cli`
- **After every plan wave:** Run `npm test && npm test --prefix packages/cli`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| P01-T1 | 01 | 1 | T-081-SC | supply-chain | SDK identity verified before install (blocking-human) | checkpoint | manual: npmjs.com/package/@modelcontextprotocol/sdk | n/a | ⬜ pending |
| P01-T2 | 01 | 1 | SC-1/SC-2/SC-4 | T-081-DEPLOY | standalone lockfile; root deploy files untouched; RED tests | unit (RED) | `node --import tsx --test "packages/cli/test/*.test.ts"` (expect fail) + `git diff --exit-code -- package.json package-lock.json Dockerfile` | ✅ Wave 0 | ⬜ pending |
| P01-T3 | 01 | 1 | SC-1/SC-2/SC-4 | T-081-01/03/05 | dry-run; no password flag; merge parity | unit | `node --import tsx --test "packages/cli/test/*.test.ts"` | ✅ Wave 0 | ⬜ pending |
| P02-T1 | 02 | 2 | SC-2/D-11/D-12 | T-081-02/DUP | env/prompt intake; receipts append+fsync; read-sent-set; no secret in receipt | unit+integration | `node --import tsx --test packages/cli/test/send.test.ts` | ✅ Wave 0 | ⬜ pending |
| P02-T2 | 02 | 2 | SC-1/SC-2 | T-081-02/TLS/DUP | test-send parity; live one-per-row; resume no-dup; pass absent from output | integration (stub-smtp) | `node --import tsx --test packages/cli/test/send.test.ts` | ✅ Wave 0 | ⬜ pending |
| P03-T1 | 03 | 3 | SC-3 | T-081-05/FS | validate-csv/preview-merge typed results via lib/core | integration (InMemoryTransport) | `node --import tsx --test packages/cli/test/mcp.test.ts` | ✅ Wave 0 | ⬜ pending |
| P03-T2 | 03 | 3 | SC-3/D-04 | T-081-04/02 | test-send/send delegate run.ts; two-step confirm token; replay refused | integration (stub-smtp) | `node --import tsx --test packages/cli/test/mcp.test.ts` | ✅ Wave 0 | ⬜ pending |
| P03-T3 | 03 | 3 | SC-3 | T-081-SDK | `mail-merge mcp` connects StdioServerTransport; no stdout noise | smoke+integration | `node --import tsx --test packages/cli/test/mcp.test.ts` | ✅ Wave 0 | ⬜ pending |
| P04-T1 | 04 | 4 | SC-1 | T-081-SC | bundled dist/bin.js with shebang; real send via bundle | build+integration | `npm run build --prefix packages/cli` + bundled `--send` vs stub-smtp | ✅ Wave 0 | ⬜ pending |
| P04-T2 | 04 | 4 | SC-5 | T-081-06/DEPLOY | README snippets; tarball smoke; root deploy byte-identical | smoke+assertion | `git diff --exit-code -- package.json package-lock.json Dockerfile` + `npm pack` install-from-tarball | ✅ Wave 0 | ⬜ pending |
| P04-T3 | 04 | 4 | SC-1 | T-081-06/402 | first npm publish (queued human) | checkpoint | manual: `npm publish --access public` then `npx @robindarlington/mail-merge --help` | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `packages/cli/` scaffold with `package.json` (own lockfile, node:test script) — Plan 01 Task 2
- [x] `packages/cli/test/` harness able to import bundled/unbundled entry seams — Plan 01 Task 2
- [x] In-process MCP test fixture (SDK `Client` + `InMemoryTransport.createLinkedPair()`) — Plan 03 Task 1
- [x] Stub SMTP reuse path (scripts/stub-smtp.ts) reachable from cli tests for send-path assertions — Plan 02 Task 1

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| First npm publish of @robindarlington/mail-merge | SC-1 (npx-executable) | Requires Rob's npm login (ENEEDAUTH verified) | `npm login` then `npm publish --access public` from packages/cli; then `npx @robindarlington/mail-merge --help` from a clean dir |
| Hidden password prompt shows no echo | SC-2 | TTY behavior | Run CLI with SMTP_PASS unset in a real terminal; type password; confirm no echo |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-filled 2026-07-18 (execution pending)
